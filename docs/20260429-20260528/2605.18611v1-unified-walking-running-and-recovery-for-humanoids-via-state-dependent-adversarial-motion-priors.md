---
title: "Unified Walking, Running, and Recovery for Humanoids via State-Dependent Adversarial Motion Priors"
title_zh: 基于状态依赖的对抗性运动先验的人形机器人统一行走、跑步与恢复
authors: "Yidan Lu, Yichao Zhong, Liu Zhao, Wanyue Li, Peng Lu"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18611v1"
tags: ["query:rl-robotics"]
score: 10.0
evidence: 基于强化学习的人形机器人运动控制
tldr: 针对人形机器人需要多策略切换控制行走、跑步和跌倒恢复的问题，提出统一强化学习框架，通过状态依赖门控将训练数据路由到恢复判别器或速度条件动作判别器，仅用三个参考动作片段即可覆盖全部行为。在Unitree G1机器人上部署单一ONNX策略，无需显式模式切换，硬件实验成功实现俯卧和仰卧跌倒恢复及走跑平滑过渡。该方法简化了部署逻辑，提升了复杂运动任务下的通用性与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18611v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18611v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1835, \"height\": 803, \"label\": \"Figure\"}]"
motivation: 人形机器人在复杂场景需同时具备行走、跑步及跌倒恢复能力，现有方法依赖多策略切换或复杂分层架构，部署效率低且鲁棒性差。
method: "扩展AMP为状态依赖对抗运动先验，用固定门控阈值（投影重力|g_z+1|>0.6）动态切换恢复判别器与速度条件动作判别器，仅需LAFAN1三个参考片段正则化。"
result: 在Unitree G1上单一策略以50Hz执行，硬件成功实现俯/仰卧摔倒恢复及走跑无缝切换，无需运行时模式逻辑。
conclusion: 该统一框架通过状态依赖门控和条件判别器，以极简参考数据实现多模式运动控制，为通用人形机器人运动策略部署提供高效方案。
---

## 摘要
我们提出了一种统一的强化学习框架，使得单一策略能够在Unitree G1人形机器人上执行行走、跑步和摔倒恢复，并在物理硬件上得到验证，部署时无需任何显式的模式切换指令。该框架扩展了对抗性运动先验（AMP），用状态依赖的门控替代传统的全局参考分布，将每个训练转换路由到两个判别器之一：一个专用的恢复判别器和一个联合覆盖行走和跑步的速度条件运动判别器。该门控由投影重力上的单一固定阈值定义：当身体倾斜超过垂直方向约37°（|g_z+1|>0.6）时，激活恢复判别器；否则使用运动判别器，其中归一化命令速度作为条件，选择步行和跑步片段之间的适当参考轨迹。仅需三个LAFAN1参考片段即可正则化完整行为集。部署时，单个冻结的ONNX策略以50 Hz执行，无需运行时模式逻辑；硬件实验表明，同一控制器下成功实现了从俯卧和仰卧摔倒的恢复以及平滑的走跑转换。

## Abstract
We propose a unified reinforcement learning framework that enables a single policy to perform walking, running, and fall recovery on the Unitree G1 humanoid robot, validated on physical hardware without any explicit mode-switching command at deployment. The framework extends Adversarial Motion Priors (AMP) by replacing the conventional global reference distribution with a state-dependent gate that routes each training transition to one of two discriminators: a dedicated recovery discriminator and a velocity-conditioned locomotion discriminator that jointly covers walking and running. The gate is defined by a single fixed threshold on projected gravity: the recovery discriminator is activated when body tilt exceeds approximately $37^\circ$ from vertical ($|g_z+1|>0.6$); otherwise the locomotion discriminator is used, with the normalized commanded velocity serving as a condition that selects the appropriate reference trajectory between walk and run clips. Only three LAFAN1 reference clips are required to regularize the complete behavior set. At deployment, a single frozen ONNX policy executes at 50\,Hz with no runtime mode logic; hardware experiments demonstrate successful recovery from both prone and supine falls and smooth walk-to-run transitions under the same controller.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：人形机器人在非结构化环境中需要同时具备行走、跑步和摔倒恢复三种截然不同的运动能力。传统方法采用模块化架构，为每种行为设计独立控制器，并通过有限状态机协调切换。这种方式存在手工调参、模式边界脆弱、过渡阶段运动质量下降等实际限制。
- **整体含义**：本文旨在证明可以训练单一强化学习策略，无需运行时模式切换逻辑，即可在物理硬件上统一实现行走、跑步和摔倒恢复行为，同时保持各模式风格一致性和运动合理性。
- **研究背景**：最近强化学习方法（如DeepMimic、AMP）已经证明参考运动先验能显著改善学习行为的物理合理性和风格一致性。但AMP天然适合单一行为模式或共享运动分布，难以同时覆盖步行/跑步的周期性速度跟踪与恢复的多接触姿态重定向这类本质不同的运动学特征。

## 2. 论文提出的方法论
- **核心思想**：扩展对抗性运动先验（AMP），用**状态依赖门控**替代传统的全局参考分布。在训练时基于机器人当前身体姿态将每个状态转换路由到两个判别器之一，从而为不同行为模式提供模式一致的风格正则化信号。部署时仅使用单一冻结策略网络，无需任何运行时模式逻辑。
- **关键技术细节**：
  - **两个判别器**：
    - 恢复判别器 \(D_{\phi}^{\text{rec}}\)：仅在摔倒恢复参考片段上训练。
    - 速度条件运动判别器 \(D_{\phi}^{\text{loco}}\)：额外输入归一化命令速度 \(\hat{v}_t \in [0,1]\)，同时在走路和跑步参考片段上训练，通过 \(\hat{v}_t\) 插值选择适当参考分布。
  - **门控函数（式5）**：基于投影重力 \(g_z\) 的单一固定阈值。当 \(|g_z + 1| > 0.6\)（对应身体倾斜约37°）时，路由到恢复判别器；否则路由到运动判别器。该阈值在经验分布的低密度区域，因此鲁棒且无需学习。
  - **AMP风格奖励（式3）**：根据门控输出，使用对应判别器计算 \(-\log(1 - D(s_t, s_{t+1}))\)。
  - **总奖励（式4）**：任务奖励 \(R_t\) 加上权重 \(\lambda_{\text{amp}} = 0.5\) 的AMP风格奖励。
  - **策略网络**：三层MLP，输入96维观测（膝关节、投影重力、命令速度、关节位置/速度、上一动作），叠加4帧历史得到384维输入；输出29维动作（目标关节位置），由低层PD控制器跟踪。
  - **参考片段**：仅使用LAFAN1数据集中的三个片段进行重定向：walk1_subject1、run1_subject2、fallAndGetUp2_subject2。恢复片段只训练恢复判别器；运动判别器更新时以概率 \((1-\hat{v}_t)\) 采样走路片段、以概率 \(\hat{v}_t\) 采样跑步片段。
  - **训练流程**：门控仅在训练时激活；部署时策略为单一ONNX模型，无任何模式变量。

## 3. 实验设计
- **数据集/场景**：
  - **仿真训练**：在Isaac Lab物理模拟器中使用PPO进行训练。
  - **参考运动**：LAFAN1数据集（人类运动捕捉数据）中的三个片段，通过逆运动学重定向到Unitree G1人形机器人关节空间。
  - **硬件部署**：Unitree G1真实机器人，C++有限状态机执行策略（50 Hz），读取关节状态并发出PD位置目标。
- **基准/方法对比**：论文未与其他方法（如标准AMP、模块化FSM、多策略切换）进行定量对比。仅展示了自身方法的可行性，未报告对比实验结果。
- **评估内容**：
  - **行走/跑步**：在正常模式（速度命令范围[-0.5, 1.0] m/s）和快速模式（[-1.5, 3.0] m/s）下跟踪前向速度命令，快慢模式通过显式操作命令切换（作为安全约束）。观察步态特征：走路有规律脚跟-脚尖接触周期，跑步有更长的步幅和减小的双支撑相时长。
  - **摔倒恢复**：从俯卧和仰卧两种初始姿态成功恢复至稳定直立站姿。
  - **统一行为连续性**：展示完整硬件序列（恢复→走→跑），验证三种行为被稳定编码在单一策略中，且过渡连贯。

## 4. 资源与算力
- 文中**未明确说明**使用的GPU型号、数量、训练时长。仅提及训练在Isaac Lab模拟中进行，使用PPO算法。未提供任何算力相关细节。

## 5. 实验数量与充分性
- **实验数量**：仅有一组主要实验（在Unitree G1硬件上演示两次恢复+走跑序列），未做跨机器人、跨环境、多次重复性实验。未提供定量统计（如恢复成功率、跟踪误差、步态周期等）。
- **消融研究**：未进行任何消融实验（如移除恢复判别器、使用单一判别器、更改阈值、不使用速度条件等），无法证明每个组件的必要性。
- **公平性与客观性**：缺乏与基线方法的比较，无法判断该方法相比现有技术的优势。实验展示了个例成功，但未报告失败案例或鲁棒性边界。评估以定性演示为主，客观性不足。

## 6. 论文的主要结论与发现
- 通过状态依赖门控将训练数据路由到两个专属判别器，仅需三个LAFAN1参考片段，即可训练单一策略同时覆盖行走、跑步和摔倒恢复。
- 基于投影重力的固定阈值门控函数简单有效，训练时自动选择模式一致的正则化信号，部署时无需任何模式逻辑。
- 在Unitree G1真实硬件上成功实现了从俯卧/仰卧恢复到行走再到奔跑的完整序列，证明了仿真到现实的泛化能力。
- 方法强调“结构分离（恢复与运动的前置分离）比参考多样性更重要”——三个片段即可获得高质量、多模式行为。

## 7. 优点
- **极简参考数据**：仅用三个运动捕捉片段，显著降低了对大量高质量参考数据的依赖。
- **可解释性与鲁棒性**：门控基于固定物理阈值（投影重力），无需学习，避免了训练噪声或过拟合。
- **部署简洁**：单一ONNX模型，无需运行时模式选择器，降低了工程复杂度。
- **连续行为过渡**：速度条件判别器使得从走到跑的速度区间内风格奖励连续插值，避免了硬切换带来的突变。
- **硬件验证**：在真实人形机器人上成功演示，验证了工程可行性。

## 8. 不足与局限
- **实验规模不足**：仅展示少量硬件演示，缺乏系统性的定量评估（如成功率、速度跟踪精度、恢复时间、能耗等）。
- **缺乏基线对比**：未与模块化方法、标准AMP、多策略切换等现有工作进行比较，难以客观评价性能提升。
- **缺乏消融研究**：未分析去掉速度条件、改用单判别器、改变阈值等设计选择的影响，贡献分解不充分。
- **手动安全约束**：快速模式仍需显式操作命令（operator command），并非完全自主的“统一”切换。
- **泛化性未知**：仅测试了两种标准摔倒姿态（俯卧、仰卧），对非典型摔倒（侧倒、扭曲姿态）是否有效未验证。
- **仿真到现实的差距**：虽然未做专门域随机化，但标准随机化可能仅覆盖部分变化，地面摩擦、光照等实际变化未讨论。
- **缺少计算资源报告**：未说明训练所需算力，限制了可复现性评估。
- **应用限制**：固定阈值假设重力信号是双峰的，但机器人摔倒时可能在中间过渡区域停留，阈值附近的行为质量未考察。

（完）
