---
title: Curriculum reinforcement learning with measurable task representation learning
title_zh: 基于可度量任务表示学习的课程强化学习
authors: "Yongyan Wen, Siyuan Li, Mingjian Fu, Yiqin Yang, Xun Wang, Peng Liu"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23372v1"
tags: ["query:rl-robotics"]
score: 8.0
evidence: 课程强化学习用于具有挑战性的导航任务
tldr: 课程强化学习面临非欧几里得任务空间中相似度量困难的问题，导致自动课程生成失效。本文提出基于可度量任务表示学习的CRL方法，通过变分自编码器将任务空间映射到潜在空间，学习能反映奖励和状态转移相似性的嵌入表示。基于该表示，自动生成与目标任务渐近相似的中间任务。在复杂导航任务中，该方法在样本效率和最终性能上显著优于基于插值和生成对抗网络的最新方法。该工作为复杂任务场景下的自动课程生成提供了有效范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23372v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 851, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1130, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1385, \"height\": 1112, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1420, \"height\": 720, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1434, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1392, \"height\": 1435, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 687, \"height\": 443, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1397, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1343, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1338, \"height\": 1385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23372v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1280, \"height\": 506, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.23372v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1220, \"height\": 1065, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23372v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1039, \"height\": 1705, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23372v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 774, \"height\": 1286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23372v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1298, \"height\": 544, \"label\": \"Table\"}]"
motivation: 现有CRL方法假设任务空间存在有意义距离度量，但非欧几里得导航任务破坏此假设，需要能自动度量任务相似性的表示学习方法。
method: 采用变分自编码器同时编码奖励和状态转移，学习保持任务相似性的潜在表示，并基于该表示渐进生成与目标任务最相似的中间任务。
result: 在多种挑战性导航任务中，所提方法在收敛速度和最终成功率上均超越基于插值和GAN的CRL基线方法。
conclusion: 可度量任务表示学习有效解决了非欧几里得任务空间的自动课程生成问题，提升了CRL在复杂场景下的迁移效率。
---

## 摘要
在课程强化学习（CRL）中，智能体通过一系列任务（即课程）逐步积累知识，学习过程旨在利用积累的知识最终解决具有挑战性的目标任务。早期CRL工作侧重于对候选任务进行排序，而近期研究则探索自动课程生成。在丰富的CRL文献中，基于插值的CRL范式是主体部分，它通过在任务空间中使用有意义的距离度量（即能衡量任务相似性）对初始任务分布与目标任务分布进行插值，自动生成中间任务。然而，在具有挑战性的导航任务中，非欧几里得上下文（任务）空间使这一假设失效。为了在复杂任务中实现自动课程生成，我们提出了一种基于可度量任务表示学习的自动课程生成新方法。为更好地衡量相似性，我们提议将任务空间转换到潜在空间。通过一种对奖励和状态转移进行编码的变分自编码器结构，我们实现了具有任务相似性度量属性的潜在任务表示，其中两个相近的任务嵌入对应于奖励和状态转移方面相似的两个任务。基于学习到的任务表示，我们进一步开发了一种自动课程生成方案，该方案能有效生成与目标任务越来越相似的新任务。我们在多种具有挑战性的导航任务中评估了该方法，实验结果表明，所提出的方法超越了基于插值和生成对抗网络的最先进CRL方法。

## Abstract
In curriculum reinforcement learning (CRL), an agent incrementally accumulates knowledge over a sequence of tasks (i.e., a curriculum), and the learning process is aimed at using the accumulated knowledge to finally solve a challenging target task. While early CRL works focus on sequencing candidate tasks, recent research explores automatic curriculum generation. Among the rich CRL literature, the interpolation-based CRL paradigm is a main body, which automatically generates intermediate tasks by interpolating between the initial task distribution and the target task distribution in task space with meaningful distance metrics (i.e., can measure the task similarity). However, in challenging navigation tasks, the non-Euclidean context (task) space invalidates this assumption. To achieve automatic curriculum generation in complex task, we propose a novel automatic curriculum generation approach based on measurable task representation learning. To better measure the similarity, we propose to transform the task space to a latent space. Through a variational autoencoder structure that encodes the reward and the state transitions, we achieve a latent task representation with a task similarity measurement property, and two close task embeddings correspond to two similar tasks in terms of rewards and state transitions. Based on the learned task representation, we further develop an automatic curriculum generation scheme, which can effectively generate new tasks more and more similar to the target task. We evaluate our method in a variety of challenging navigation tasks, and the experiment results indicate that the proposed approach surpasses state-of-the-art CRL approaches based on interpolation and generative adversarial networks.

---

## 论文详细总结（自动生成）

# 基于可度量任务表示学习的课程强化学习——论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：课程强化学习（CRL）通过自动生成一系列由易到难的中间任务，帮助智能体逐步掌握复杂的目标任务。现有主流方法（如基于KL散度或Wasserstein距离的插值方法）假设任务空间具有可度量的欧几里得距离，但在具有复杂几何结构（如墙壁、障碍物）的导航任务中，任务空间呈现非欧几里得性质，直接插值生成的中间任务往往不可行（如路径被墙壁阻挡），导致自动课程生成失效。
- **研究动机**：需要一种新的机制，能够自动学习任务之间的相似性，而不依赖于任务空间本身的几何假设，从而在复杂环境中生成有效的课程。
- **整体含义**：本文提出了一种基于可度量任务表示学习的自动课程生成框架ACRL，通过变分自编码器（VAE）将任务映射到潜在空间，在该空间中任务距离能真实反映任务难度和奖励/转移相似性，进而基于该潜在空间生成渐近接近目标任务的中间任务分布，显著提升了CRL在复杂导航场景中的学习效率和最终性能。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 利用VAE对任务轨迹进行编码，学习一个**可度量任务相似性**的潜在表示空间，将任务上下文（context）映射到该空间，使得在潜在空间中距离近的任务在奖励函数和状态转移上更相似。
- 在此基础上，设计两种自动课程生成策略：**Latent Space Prediction (LSP)** 和 **Exploration Bound Update (EBU)**，分别用于目标任务已可解和尚未可解的情况，渐进更新任务分布。

### 关键技术细节

#### (a) 任务表示学习
- 使用VAE，编码器输入单个转移 `(s_t, a_t, r_t, s'_t)`，输出高斯分布 `q_ϕ(z|τ_c,t)`，潜在变量 `z_t` 采样自该分布。
- 解码器包含两部分：转移解码器 `p^T_θ(s'|z)` 预测下一状态，奖励解码器 `p^R_θ(r|z)` 预测奖励。
- 轨迹的表示由所有转移嵌入的乘积得到：`q_ϕ(z|τ_c) = ∏_{t=0}^{H-1} q_ϕ(z_t|τ_c,t)`，实现置换不变性。
- 额外引入任务解码器 `p^c_θ(z)`，将潜在变量映射回原始上下文空间 `c`，用于生成新任务。
- 训练时仅使用**回合回报高于阈值δ**的轨迹，确保表示学习基于有意义的行为。

#### (b) 自动课程生成
- 维护一个任务缓冲区T，以**采样比率λ**从T中采样（概率λ）或从均匀分布U(C)中采样（概率1-λ），平衡探索与利用。
- 当策略在当前分布上的平均回报超过阈值G时，触发课程更新：
  - 若目标任务上的平均回报仍低于目标阈值δ_tar，使用**EBU**：从VAE缓冲区中选取高回报任务，在潜在空间中按距离目标任务的距离降序排序，从指数分布中采样索引，选取边界任务，并在上下文空间添加高斯噪声生成新任务。
  - 若目标任务已达到阈值，使用**LSP**：在潜在空间中对目标任务嵌入与当前任务嵌入进行线性插值 `z' = α z_tar + (1-α)z`，再通过任务解码器映射回上下文空间。
- 两种方法逐步使任务分布从初始位置向目标位置迁移，且生成的中间任务能避开障碍物。

### 算法流程（文字描述）
1. 初始化源任务分布p(c)、均匀分布U(c)、目标任务c_tar、策略π_ψ等参数。
2. 循环训练步骤：
   - 以概率λ从任务缓冲区T中采样任务c，否则从U(c)中采样。
   - 在任务c上执行策略，收集轨迹存入VAE缓冲区B。
   - 更新策略π_ψ。
   - 每隔nu步评估平均回报`\bar{G}`：
     - 若`\bar{G} > G`，则更新任务表示（编码器、解码器）。
     - 若目标任务平均回报`\bar{G}_tar < δ_tar`，使用EBU更新任务分布；否则使用LSP。
3. 输出最终策略。

## 3. 实验设计

### 使用场景与数据集
- **MiniGrid**：离散动作空间，具有不同难度设置（Easy-A/B/C, Medium-A/B/C, Hard-A/B），任务包括导航、捡钥匙、开门、避开熔岩等。上下文空间为二维或四维（目标位置或目标+钥匙位置）。障碍物（墙壁）导致欧几里得距离失效。
- **U-Maze**：连续动作空间（MuJoCo实现），连续上下文空间。目标是穿过U形障碍到达目标位置，中央有墙壁阻挡直线路径。

### Benchmark
- 评估指标：目标任务上的**回合回报**和**训练步数-回报曲线**（反映学习效率和渐近性能）。
- 基线方法：
  - **Default**：直接在目标任务上训练（无课程）
  - **Random**：均匀采样上下文
  - **ALP-GMM**：基于绝对学习进度的GMM采样
  - **Goal GAN**：基于GAN的自动目标生成
  - **VDS**：基于价值函数不确定性的课程
  - **PLR**：优先级别回放
  - **CURROT**：基于最优运输（Wasserstein距离）的插值方法

## 4. 资源与算力

论文在实验设置部分**没有明确说明使用了多少GPU型号、数量或训练时长**。通常此类强化学习实验可在单块GPU（如NVIDIA GTX 1080或RTX系列）上运行，但作者未提供具体信息。因此，从论文中无法获知准确的算力开销。

## 5. 实验数量与充分性

### 实验数量
- **MiniGrid环境**：在8个不同难度的任务（Easy-A/B/C, Medium-A/B/C, Hard-A/B）上进行了测试，每个任务5个随机种子，重复5次取平均值。
- **U-Maze环境**：1个连续控制任务，同样5个随机种子。
- **消融实验**：包括采样比率λ的消融（5种λ值）、课程参数（δ, δ_tar, G）的敏感性分析、以及解码器组件（去掉奖励解码器或转移解码器）的消融。
- **表示学习分析**：通过多维缩放（MDS）和Gram矩阵特征值分析验证潜在空间结构。
- **课程可视化**：在Easy-B和U-Maze上展示了任务分布随训练的变化。

### 实验充分性与客观性评价
- **充分性**：覆盖了离散/连续动作空间、多种难度、多种障碍结构，与7种基线方法（含无课程、均匀采样、多种先进CRL方法）进行了对比，还进行了多角度消融和敏感性分析，实验设计较为全面。
- **客观性**：使用相同的底层RL算法（PPO）和稳定基线库（Stable-Baselines3），所有方法在相同条件下运行，基线参数按原始论文推荐设置，对比公平。结果以平均曲线和方差展示，具有统计意义。
- **局限性**：未在更复杂的环境（如高维图像输入、稀疏奖励极长任务）上测试，也未与非参数化任务定义（如语言描述）进行对比。

## 6. 论文的主要结论与发现

- 所提出的ACRL方法**在MiniGrid和U-Maze的所有任务上均显著优于所有基线**，尤其是在中等和困难任务上，学习效率提升明显，渐近回报更高。
- 课程可视化表明，ACRL生成的中间任务分布能**绕过墙壁等障碍物**，逐步从初始位置向目标位置迁移，而基于插值的CURROT则可能直接指向墙壁，导致无效课程。
- 潜在空间分析显示，学习到的任务表示**近似为二维欧几里得空间**（Gram矩阵仅两个显著正特征值），说明VAE成功将非欧几里得任务空间映射为可度量的潜在空间。
- 消融实验表明：采样比率λ=0.25效果最佳；两个解码器（奖励和转移）均不可或缺，去掉任一都会降低性能；课程参数在一定范围内稳定。

## 7. 优点

- **方法创新性**：首次将任务表示学习（VAE）与自动课程生成有机结合，解决了非欧几里得任务空间的距离度量难题。
- **通用性**：兼容任意RL算法（如PPO、SAC），适用于离散和连续上下文空间，无需外部奖励或手工课程设计。
- **课程质量高**：生成的中间任务分布能自然适应环境几何结构，提供平滑的迁移路径，避免无效插值。
- **实验充分**：覆盖多种任务类型，对比基线全面（7种），并提供详细消融和敏感性分析，结论可靠。
- **可视化有效**：通过任务分布演化图和潜在空间MDS分析，直观展示了方法的有效性。

## 8. 不足与局限

- **依赖参数化上下文**：方法假设上下文由数值参数表示，无法直接处理非参数化任务定义（如符号或语言描述）。未来工作可考虑引入语义编码器。
- **初始阶段依赖成功轨迹**：表示学习仅使用高回报轨迹，在极稀疏奖励环境中，初期可能缺乏可用数据，需要依赖EBU进行探索，收敛可能较慢。
- **未报告计算资源**：缺乏GPU型号、训练时间等硬件信息，难以评估实际计算成本。
- **环境多样性有限**：虽覆盖多种障碍，但所有测试环境均为栅格迷宫或简单U形迷宫，未在真实机器人任务、高维图像输入或更长时域任务上验证。
- **可重现性**：附录给出了超参数，但未提供完整代码仓库或随机种子列表（仅提及5个种子），可能影响精确复现。
- **潜在风险**：课程生成依赖于策略的学习进度阈值，阈值设定可能影响稳定性，尽管敏感性实验显示在一定范围内鲁棒。

（完）
