---
title: Learning Bilevel Policies over Symbolic World Models for Long-Horizon Planning
title_zh: 基于符号世界模型的双层策略学习用于长时域规划
authors: "Dillon Z. Chen, Till Hofmann, Toryn Q. Klassen, Sheila A. McIlraith"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15975v2"
tags: ["query:world-model"]
score: 9.0
evidence: 动作条件世界模型用于长时域规划与控制
tldr: 长周期规划是具身AI的挑战，模仿学习擅长低层控制但难生成长期计划。本文提出BISON，通过双层策略结合高层符号抽象与低层模仿学习，利用符号归纳泛化构建高层策略。实验表明BISON在长周期和大规模问题上优于VLA等方法，且训练推理更高效，高层策略可秒级求解含万级对象的问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15975v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1434, \"height\": 466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15975v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1459, \"height\": 702, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15975v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1416, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15975v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 449, \"height\": 215, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15975v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1443, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15975v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 443, \"height\": 243, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15975v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 452, \"height\": 180, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15975v2/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1421, \"height\": 397, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.15975v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 404, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.15975v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1459, \"height\": 899, \"label\": \"Table\"}]"
motivation: 解决模仿学习难以生成长期计划的问题，结合低层控制与高层符号抽象实现可靠长周期规划。
method: 提出双层策略，包括从低层演示学习神经策略和从符号抽象归纳泛化构建高层符号策略，实现BISON系统。
result: 在扩展MetaWorld基准上，BISON在长周期和更多物体问题上优于VLA和端到端方法，训练推理更省时省内存，高层策略可一分钟内解决万级物体问题。
conclusion: BISON有效结合模仿学习与符号规划，实现了高效且可扩展的长周期规划能力。
---

## 摘要
我们致力于解决构建能够可靠解决长时域规划问题的具身AI智能体这一挑战。从示范中学习模仿已被证明在训练机器人解决需要精细运动控制和低层（LL）连续环境操作的各种复杂任务方面是有效的。然而，仅凭模仿学习生成长期规划仍然是一项艰巨的任务。相比之下，高层（HL）符号抽象促进了高效且可解释的长期规划。我们提出将LL模仿学习用于操作和控制，以及HL符号抽象用于长期规划的优势相结合。我们通过形式为$(π^{\mathrm{hl}}, π^{\mathrm{ll}})$的双层策略实现这一想法，该策略包括从LL示范中学习的神经策略$π^{\mathrm{ll}}$，以及从LL示范的符号抽象结合归纳泛化构建的HL符号策略$π^{\mathrm{hl}}$。我们在BISON系统中实现了这些想法。在扩展的MetaWorld基准上的实验表明，BISON能够泛化到比VLA和端到端方法解决的时间更长、对象数量更多的问题，并且在训练和推理中更节省时间和内存。值得注意的是，当忽略LL执行时，BISON的HL策略可以在不到一分钟内解决包含10,000个相关对象的HL问题。项目页面：https://dillonzchen.github.io/bison

## Abstract
We tackle the challenge of building embodied AI agents that can reliably solve long-horizon planning problems. Imitation learning from demonstrations has shown itself to be effective in training robots to solve a diversity of complex tasks requiring fine motor control and manipulation over low-level (LL), continuous environments. Yet, it remains a difficult endeavour to generate long-horizon plans from imitation learning alone. In contrast, high-level (HL), symbolic abstractions facilitate efficient and interpretable long-horizon planning. We propose to combine the strengths of LL imitation learning for manipulation and control, and HL symbolic abstractions for long-horizon planning. We realise this idea via \emph{bilevel policies} of the form $(π^{\mathrm{hl}}, π^{\mathrm{ll}})$, consisting of a neural policy $π^{\mathrm{ll}}$ learned from LL demonstrations, and an HL symbolic policy $π^{\mathrm{hl}}$ that is constructed from symbolic abstractions of the LL demonstrations combined with inductive generalisation. We implement these ideas in the BISON system. Experiments on extended MetaWorld benchmarks demonstrate that BISON generalises to long horizons and problems with greater numbers of objects than those solved by VLA and end-to-end methods, and is more time and memory efficient in training and inference. Notably, when ignoring LL execution, BISON's HL policies can solve HL problems with 10,000 relevant objects in under a minute. Project page: https://dillonzchen.github.io/bison

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长时域规划是具身AI（Embodied AI）的核心挑战。现有方法要么依靠模仿学习处理低层（LL）精细控制但难以生成长期计划，要么依靠高层（HL）符号抽象进行高效规划但难以适应连续环境和开放世界。
- **研究动机**：提出一种结合低层模仿学习与高层符号抽象优势的框架，使智能体既能执行灵活的低层操作，又能利用可解释、可泛化的符号策略进行长期规划。
- **背景**：符号规划器（如STRIPS、PDDL）在长期规划中非常有效，但不擅长处理连续状态/动作和感知噪声；模仿学习和强化学习擅长学习多任务控制，但难以组合长时域步骤。本文试图弥补二者之间的鸿沟。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：学习**双层策略**（Bilevel Policy），形式为 $(π^{hl}, π^{ll})$：
  - 高层策略 $π^{hl}$：从低层演示的符号抽象中，通过**目标回归（Goal Regression）**和**归纳泛化（Inductive Generalization）**提取一阶条件-动作规则（first-order condition-action rules），支持开放世界和任意数量对象。
  - 低层策略 $π^{ll}$：使用**图神经网络（GNN）**从低层演示中通过行为克隆（Behavior Cloning）学习，参数少于33k，用于在低层连续动作空间中实现高层动作。
- **关键技术细节**：
  1. **高层策略学习**（图2、算法1）：
     - 步骤1：利用标注函数 $L$ 将低层演示映射为高层轨迹（$\mathcal{T}^{hl}$）。
     - 步骤2：对每个高层轨迹，从后向前递归执行**目标回归**，计算使动作能达到目标的充分条件，得到**ground规则**（条件-动作对）。
     - 步骤3：通过**对象替换为变量**（lifting），将ground规则泛化为**一阶规则**，并赋予优先级（离目标越近优先级越低）。
     - 执行时：在给定高层状态和未完成目标条件下，选择可适用且优先级最高的ground规则对应的动作。
  2. **低层策略学习**（图3）：
     - 输入：低层状态 $s^{ll}$（对象中心、自我中心表示）、高层动作 $a^{hl}$、高层目标 $g^{hl}$。
     - 网络结构：一个全局节点、动作节点、对象节点；经过初始化嵌入、L轮消息传递、读出层输出低层动作 $a^{ll}$。
     - 损失函数：最小化预测低层动作与演示动作的均方误差（MSE）。
- **关键公式**：
  - 双层策略联合执行：$\pi(a^{ll}| s^{ll}, g^{hl}) = \sum_{a^{hl}} \pi^{ll}(a^{ll}| s^{ll}, a^{hl}, g^{hl}) \cdot \pi^{hl}(a^{hl}| L(s^{ll}), g^{hl})$。
  - 目标回归定义（公式4）和 lifting 定义（公式5）。
- **理论保证**（Theorem 1）：在满足**C-有界目标独立性**条件下，有限演示集即可学习到能解决任意多对象HL问题的有限策略。

## 3. 实验设计

- **环境与基准**：在**扩展的MetaWorld**模拟环境上评估，包含8个环境（每个环境有**静止（S）** 和**噪声（N）** 两种变体）：
  - `Blocks S/N`：将彩色方块放到对应颜色目标位置。
  - `Factory S/N`：每放置一个方块即引入新方块。
  - `Colour S/N`：将棕色方块通过着色平台染色后放到对应颜色托盘。
  - `Gacha S/N`：通过抽奖盒随机产生指定颜色方块，并放置到对应托盘。
  - 噪声变体（N）：物体可能随机传送，引入状态不确定性。
- **对比方法**（共9种）：
  - **Oracle**：手工编码专家策略（上界）。
  - **SmolVLA**：开源0.45B参数VLA模型，分为预训练和微调版（SmolVLA MW）。
  - **PureNN**：纯GNN，无高层信息。
  - **PddlNN**：GNN输入包含高层事实但无高层动作。
  - **DetPlan / NdtPlan**：单次确定性/非确定规划后固定执行。
  - **DetReplan / NdtReplan**：执行失败时重规划。
  - **BISON**：本文方法。
- **训练数据**：每个环境3个随机种子，使用200条**3个对象**的成功演示轨迹训练。
- **评估条件**：测试对象数1~10个，每个配置10个episodes，每个episode最多$2048 \times n$步。总评估episode数：$9 \text{ (方法)} \times 8 \text{ (环境)} \times 3 \text{ (种子)} \times 10 \text{ (对象数)} \times 10 \text{ (episodes)} = 21,600$。

## 4. 资源与算力

- **训练硬件**：一张NVIDIA L40 GPU（48GB内存）。
- **推理硬件**：Intel Xeon Gold 6338 CPU（2.00GHz）。
- **说明**：文中未详细报告每个方法的精确训练时长（如GPU小时数），但给出了训练时间和推理时间的对比图（图5）。BISON训练时间约几百秒量级，推理每步约0.004秒，远优于重规划和端到端方法。高层策略求解10,000对象问题仅需几秒（图6）。

## 5. 实验数量与充分性

- **数量**：21,600 episodes评估，覆盖3个种子、8个环境、1~10个对象，与8种基线对比（含VLA、端到端GNN、规划、重规划）。
- **充分性**：
  - 实验设计系统性，包含消融（比较有无高层动作、有无示教HL政策等）。
  - 涵盖多种不确定性类型（外生、内生、状态部分可观测性）。
  - 对比VLA基线失败，验证了符号抽象对长时域规划的必要性。
  - 但环境限为MetaWorld模拟，未在真实机器人或更复杂3D场景中验证；未与基于LLM的规划方法（如LLM+P）直接对比。
- **公平性**：VLA使用图像/文本输入，其他方法使用预处理的低层状态表示，对比可能存在不公平，但文中对此有说明。所有基线均使用相同训练数据。

## 6. 论文的主要结论与发现

1. **BISON在长时域和更大规模问题上显著优于基线和VLA**：尤其在对象数≥6时，BISON成功率远高于PddlNN、PureNN等端到端方法，与Oracle上界接近。
2. **高层策略可泛化到任意对象数**：符号规则可秒级求解10,000对象问题，而传统PDDL规划器（LAMA）在几百对象时即超时。
3. **效率优势**：BISON训练和推理时间均优于重规划方法（DetReplan, NdtReplan），且优于端到端方法（PureNN, PddlNN）。
4. **VLA方法在本基准上完全失败**：预训练和微调模型成功率为0，验证了纯端到端VLA难以处理变对象数和长时域任务。
5. **重规划方法（NdtReplan）是第二强基线**，但受到复杂环境（Colour, Gacha）限制，且不支持开放世界。

## 7. 优点

- **方法创新**：
  - 首次提出**从低层演示归纳泛化出高层符号策略**，避免传统符号规划需要手工定义的搜索空间，且支持开放世界和部分可观测。
  - 采用**目标回归**和**lifting**生成紧凑、可解释的一阶条件-动作规则，可跨对象数泛化。
  - 低层GNN参数极少（<33k），训练高效。
- **实验亮点**：
  - 系统化评估多种不确定性（外生/内生/状态噪声）。
  - 明确与VLA、端到端、规划、重规划等各类方法对比，验证各个组件必要性。
  - 展示高层策略的可解释性（附录C.4用自然语言翻译了各环境的符号规则）。
- **实用性**：高层策略可脱离低层执行独立测试，推理速度快，适合大规模或实时应用。

## 8. 不足与局限

- **对高层抽象的强依赖**：假设预先给定领域理论 $D$ 和标注函数 $L$，实际机器人场景中学习这些抽象仍是一个开放问题（文中提及但未解决）。
- **LL策略泛化有限**：演示仅3个对象，LL策略面对更多对象时存在协变量偏移（covariate shift），导致少数环境（如Colour N）性能下降。作者建议可通过RL或DAgger改善。
- **无最优性保证**：HL和LL策略均无最优性保证，可能产生次优动作序列。
- **环境局限**：所有实验在模拟器（MetaWorld）中进行，未在物理机器人上验证；也未与其他最新的基于LLM的规划方法比较。
- **VLA对比公平性存疑**：VLA输入为RGB图像，其他方法输入为对象状态向量，信息量不同，可能导致VLA表现夸大性差。
- **半监督假设**：需要每步低层动作的标注（演示），在实际场景中获取成本高。

（完）
