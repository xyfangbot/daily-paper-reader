---
title: "ProgVLA: Progress-Aware Robot Manipulation Skill Learning"
title_zh: "ProgVLA: 进度感知的机器人操作技能学习"
authors: "Seungsu Kim, Jinyoung Choi, Seungmin Baek, Jean-Michel Renders"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28231v1"
tags: ["query:vla-robotics"]
score: 9.0
evidence: 紧凑型进度感知VLA模型用于操纵
tldr: 针对资源受限下机器人操作长序列建模困难，提出ProgVLA紧凑视觉-语言-动作模型。通过两阶段Perceiver重采样压缩多模态序列为固定上下文token，并引入离线强化学习进展头估计任务进度，实现优势加权流匹配模仿学习。0.1B参数模型在多个基准上媲美甚至超越更大预训练模型，长时域任务提升显著。消融证实重采样和进展感知训练为核心贡献，真实厨房环境验证有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 539}, {\"url\": \"assets/figures/arxiv/2605.28231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 791, \"height\": 503}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 869}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1168, \"height\": 367}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 1023}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1087, \"height\": 671}]"
motivation: 计算和内存受限下，机器人操作需要紧凑模型高效处理长多模态序列，现有方法对任务进展感知不足。
method: 提出ProgVLA，包含多模态编码器的两阶段Perceiver重采样压缩序列，以及用离线RL训练的进展头提供进度估计，进行优势加权流匹配模仿学习。
result: 0.1B参数模型在多个基准上与大模型竞争，长时域和更难任务超越；真实厨房环境验证成功。
conclusion: 紧凑的进展感知VLA模型有效处理长序列操作任务，重采样和进度训练是核心，为资源受限机器人学习提供新思路。
---

## 摘要
我们提出了ProgVLA，一种紧凑的视觉-语言-动作（VLA）模型，专为在严格的计算和内存预算下实现可靠的机器人操作而设计。该模型特别关注通过维护任务在长时间跨度上的显式进度表示，高效处理长多模态序列。为此，ProgVLA集成了两个关键组件。首先，采用两阶段感知器重采样方案的多模态编码器，将可变长度的视觉、语言和本体感受流压缩为一组固定的控制就绪上下文令牌，在保持跨模态对齐的同时大幅减少序列长度。其次，一组辅助进度头通过离线强化学习（RL）目标进行训练，以联合学习针对归一化剩余时间目标的评判器。这为策略提供了任务进度的内部估计，并实现了优势与成功加权的流匹配模仿学习。在两个完善的多任务机器人操作基准上，一个0.1B参数的ProgVLA模型达到了与更大预训练基线相竞争的成功率，并且在长周期和更困难的任务层次上超过了它们。消融实验表明，学习的上下文重采样器和任务自适应视觉微调是最大的单独贡献因素，而进度感知训练提供了持续的额外增益，主要集中在长周期和多物体任务上。我们还在真实世界的玩具厨房环境中进一步验证了该方法。

## Abstract
We present ProgVLA, a compact vision-language-action (VLA) model designed for reliable robot manipulation under tight compute and memory budgets. The model specifically focuses on efficiently processing long multi-modal sequences by maintaining an explicit representation of task progress over extended horizons. To this end, ProgVLA integrates two key components. First, a multi-modal encoder with a two-stage Perceiver resampling scheme compresses variable-length visual, language, and proprioceptive streams into a fixed set of control-ready context tokens, substantially reducing sequence length while preserving cross-modal grounding. Second, an auxiliary set of progress heads is trained with offline reinforcement learning (RL) objectives to jointly learn critics over normalized remaining-horizon targets. This provides the policy with an internal estimate of task progress and enables advantage- and success-weighted flow-matching imitation learning. On two well-established multi-task robot manipulation benchmarks, a 0.1B-parameter ProgVLA model reaches success rates that are competitive with, and on long-horizon and harder task tiers exceed, substantially larger pretrained baselines. Ablations indicate that the learned context resampler and task-adaptive visual fine-tuning are the largest single contributors, while progress-aware training provides a consistent additional gain that is concentrated on long-horizon and multi-object tasks. We further validate the approach in real-world toy-kitchen environments.

---

## 论文详细总结（自动生成）

# ProgVLA: 进度感知的机器人操作技能学习——论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有的视觉-语言-动作（VLA）模型通常依赖数十亿参数的大模型和大规模跨本体机器人预训练，计算和内存成本高昂，难以在受限硬件上部署。小模型（如SmolVLA）虽可运行，但在长时域（long-horizon）任务上表现不佳。作者旨在回答：能否仅使用目标基准的示范数据训练一个紧凑模型，在不依赖大规模机器人预训练的情况下，匹配甚至超越更大的预训练基线？
- **整体含义**：提出ProgVLA，一种0.1B参数的紧凑VLA模型，通过高效的多模态序列压缩和内部进度感知训练，解决长时域操作任务。研究表明，通过精心设计的架构（两阶段Perceiver重采样、通用视觉骨干微调、进度头）可以替代跨本体机器人预训练，在多个基准上实现竞争力性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将多模态观测（视觉、语言、本体感受）压缩为少量固定大小的上下文令牌，并联合训练辅助进度头来估计任务进度，以此加权模仿学习损失，使策略关注对成功关键的时间段。
- **关键技术细节**：
  - **多模态编码器**（四阶段）：
    1. 模态特定编码器：视觉用DUNE（ViT-Small），语言用冻结T5，本体用MLP投影。
    2. 每个模态后接一个Perceiver重采样器，将可变长度特征压缩为固定数量令牌（每图像8个，语言16个）。
    3. 共享Transformer融合所有模态令牌（自注意力）。
    4. 后融合重采样器将融合序列进一步压缩为4个上下文令牌 cₜ。
  - **动作专家**：采用SmolVLA风格的流匹配模型，预测动作块（预测长度16，执行前8步）。训练时使用Beta(2,2)采样噪声时间τ，推理时用10步Heun ODE积分。
  - **辅助进度头**：
    - **进度目标**：rₜ = max(0, 1 − (T−t)/T_M)，其中T是示范长度，T_M=500，是纯时间进度信号。
    - **Q头**：ˆQ(cₜ, aₜ) 回归rₜ（Huber损失），使用动作aₜ作为条件。
    - **V头**：期望分位数回归（ρ=0.8），得到乐观基线。
    - **成功头**：ˆS(cₜ) 二分类，标签在rₜ ≥ r_succ（≈0.966）时激活。
    - **加权方案**：优势Aₜ = ˆQ − ˆV（分离梯度），权重 w_A = min(exp(Aₜ/β), C)，成功权重 w_S = 0.5+0.5·σ(ˆS)。总模仿损失为 w_A·w_S·ℓ_FM。
  - **总训练损失**：L_total = 加权流匹配损失 + λ_V L_V + λ_Q L_Q + λ_S L_S。
  - **完全从零训练**：仅视觉骨干（DUNE）和T5使用预训练权重，其余参数（重采样器、融合Transformer、动作专家、进度头）随机初始化，在目标基准数据上训练。

## 3. 实验设计：数据集、基准、对比方法

- **数据集与基准**：
  - **LIBERO**：40个任务（分Spatial, Object, Goal, Long四类），每任务50个示范，共2000个。官方评估协议，每任务50个随机初始状态。
  - **Meta-World MT50（LeRobot版本）**：49个任务（缺失1个），每任务50示范，分为easy, medium, hard, very hard四个难度等级。
  - **真实世界**：10个任务（8个玩具厨房，2个白色桌面），每任务50个遥操作示范，在相同环境中评估100次。
- **对比方法**：
  - 模拟基准：Octo（0.09B）、π₀（PaliGemma-3B和3.3B）、OpenVLA（7B）、SmolVLA（0.24B和2.25B）。基线分数引用自[10,21]。
  - 真实实验：仅展示ProgVLA自身结果，无基线对比。

## 4. 资源与算力

- **硬件**：单张NVIDIA H100 GPU。
- **训练时长**：
  - LIBERO：25小时
  - Meta-World：32小时
  - 真实世界模型：约8.5小时
- **参数**：完整模型109M参数（双相机输入），其中74M可训练；Meta-World版本100M（单相机）。
- **训练配置**：500 epoch，batch size 256，AdamW优化器，初始学习率1e-4，余弦衰减至1e-6，梯度裁剪1.0，EMA衰减0.75。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个模拟基准的主实验：LIBERO（40任务×50 ep=2000次评估），Meta-World（49任务，每任务50次评估）。
  - 消融实验：在LIBERO上4个变体（去除进度目标、去除后融合重采样器、更换DINOv3骨干、冻结DUNE），每变体同样评估。
  - 真实世界：10任务×10次=100次评估，并分析32次失败的失败模式。
- **充分性与公平性**：
  - **正面**：消融实验系统，覆盖架构关键组件；真实世界实验验证可迁移性。
  - **客观性不足**：
    - 所有结果基于**单次训练种子**，作者承认约±1-2成功点的方差，但未提供多种子统计。
    - 基线结果引用自其他论文，**非严格复现**，存在协议差异（LIBERO上ProgVLA使用2000个示范（原始较低分辨率图像），而SmolVLA使用过滤后的1693个示范（较高分辨率图像）；评估次数也不同（ProgVLA 50次/任务 vs 某些基线10次/任务）。作者指出这些差异的方向可能有利有弊，但未重新运行基线。
    - 真实实验无基线对比，仅展示绝对成功率。
  - **总体**：实验设计合理、消融充分，但单种子和基线协议差异使得定量对比需谨慎解读。

## 6. 论文的主要结论与发现

- **核心发现**：
  1. ProgVLA（0.1B）在LIBERO上平均成功率91.1%，超越OpenVLA（7B）的76.5%和SmolVLA（2.25B）的88.75%，尤其在Long-horizon任务上优势显著（88.6% vs SmolVLA 77%）。
  2. 在Meta-World上，ProgVLA平均78.5%，远超SmolVLA 2.25B的68.24%，在medium/hard/very hard难度上分别高出20.9/7.0/15.6个百分点。
  3. **消融结论**：后融合重采样器是最大贡献者（移除后平均下降16.0点），其次是视觉骨干微调（冻结DUNE下降13.5点），进度感知训练贡献较小但一致（移除后下降2.3点，集中在Object和Long类别）。
  4. 真实世界实现68%成功率，主要失败模式为场景遮挡、夹爪超时、抓错物体。
- **总体结论**：在大幅压缩参数和数据需求的情况下，紧凑型VLA可通过有效的序列压缩和强视觉先验匹配甚至超越大模型性能；内部进度监督额外提升长时域和复杂任务表现。

## 7. 优点：方法或实验设计上的亮点

- **架构创新**：两阶段Perceiver重采样设计，先归一化模态令牌再融合后二次压缩，同时保持跨模态对齐，显著降低序列长度（约一个数量级），使长序列处理可行。
- **进度头设计**：不依赖外部成功检测器，而是将进度估计集成到策略内部，通过离线RL风格的Q/V头学习优势，并将其作为模仿损失权重，实现更精细的credit assignment。
- **数据高效**：单模型从头训练，无需大规模跨本体机器人预训练，仅使用基准级示范数据（LIBERO 2000条），大大降低了数据需求。
- **真实验证**：在物理机器人上运行10个任务，同架构迁移，验证了方法的实用性。
- **消融系统**：清晰分离了每个组件的贡献，尤其是重采样器和视觉微调的关键作用。

## 8. 不足与局限

- **实验充分性**：
  - **单种子**：报告均为单次运行，缺乏方差分析，小改动可能不显著。
  - **基线对比不严格**：因计算限制未重新运行基线，存在数据量、图像分辨率、评估次数等差异，定量比较有偏差风险。
  - **超参数敏感性未探究**：未对T_M、ρ、β、C等进行扫参，也未分离Q/V/S头进行单独消融。
  - **进度头尚未直接验证**：未分析ˆV的单调性、p_succ的校准性等，主要依赖成功率间接证明。
- **应用限制**：
  - **进度目标完全时间性**：rₜ仅取决于剩余步数，不反映语义子目标，可能不适用于长度变化大或阶段不均衡的任务。
  - **仅使用成功示范**：未利用失败轨迹，优势信号在严格离线RL意义上弱识别。
  - **真实环境规模小**：仅单一6-DOF手臂、10个任务、同环境评估，无跨平台或跨物体泛化验证。
  - **部署安全**：失败分析显示场景遮挡、抓错物体等问题，建议安全措施。
- **其他**：未提供推理延迟、控制频率等效率基准；未开源代码和数据集（但表示将发布）。

（完）
