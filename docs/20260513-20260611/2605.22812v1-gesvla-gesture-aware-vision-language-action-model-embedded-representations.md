---
title: "GesVLA: Gesture-Aware Vision-Language-Action Model Embedded Representations"
title_zh: GesVLA：嵌入表示的姿态感知视觉-语言-动作模型
authors: "Wenxuan Guo, Ziyuan Li, Meng Zhang, Yichen Liu, Yimeng Dong, Chuxi Xu, Yunfei Wei, Ze Chen, Erjin Zhou, Jianjiang Feng"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22812v1"
tags: ["query:vla-robotics"]
score: 9.0
evidence: 手势感知的视觉-语言-动作模型用于机器人操作
tldr: 现有视觉-语言-动作(VLA)模型依赖文本指令，在复杂场景中面临空间歧义问题，难以区分相似物体。本文提出手势感知VLA模型GesVLA，将手势作为并行指令模态编码到潜空间，采用双VLM架构实现手势与动作策略的紧密耦合。通过将手模型渲染到真实图像构建高质量手势数据，并采用两阶段训练策略。实验表明，手势输入显著提升了目标定位精度和人机交互效率，尤其在杂乱环境中表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22812v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 759, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1824, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1836, \"height\": 691, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1835, \"height\": 616, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 816, \"height\": 655, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22812v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1786, \"height\": 403, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22812v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22812v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22812v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 887, \"height\": 179, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22812v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 869, \"height\": 201, \"label\": \"Table\"}]"
motivation: 文本指令在复杂场景中空间歧义性强，手势可提供直观空间线索，弥补文本不足。
method: 将手势特征编码到潜空间，采用双VLM架构耦合手势与动作，通过渲染手模生成数据，两阶段训练。
result: 在多种机器人任务中，手势输入显著提升目标定位与交互效率，尤其适用于杂乱环境。
conclusion: 手势有效增强VLA模型的空间理解，提升人机交互的自然性和鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）模型通过统一感知和动作，在通用机器人操作中展现出巨大潜力。然而，现有VLA系统主要依赖文本指令，在包含多个相似物体的复杂场景中难以解决空间歧义问题。为克服这一局限，我们引入姿态作为并行指令模态，并提出一种姿态感知的视觉-语言-动作模型（GesVLA）。该方法将姿态特征直接编码到潜在空间中，使其能够同时参与高层推理和低层动作生成，并采用双VLM架构实现姿态表示与动作策略之间的紧密耦合。在数据层面，我们构建了一个可扩展的姿态数据生成管线，通过将手部模型渲染到真实场景图像上，缩小了仿真到真实的视觉差距，同时生成具有多样化运动模式和相应指向标注的丰富数据。此外，我们采用两阶段训练策略，使模型同时具备姿态感知和动作预测能力。我们在多个真实机器人任务中评估了该方法，包括用于验证的受控积木操作任务，以及更实用的场景如产品与农产品挑选。实验结果表明，加入姿态一致性地提高了目标定位精度和人机交互效率，尤其是在复杂和杂乱的环境中。项目页面：https://gwxuan.github.io/GesVLA/。

## Abstract
Vision-Language-Action (VLA) models have shown strong potential for general-purpose robot manipulation by unifying perception and action. However, existing VLA systems primarily rely on textual instructions and struggle to resolve spatial ambiguity in complex scenes with multiple similar objects. To address this limitation, we introduce gesture as a parallel instruction modality and propose a Gesture-aware Vision-Language-Action model (GesVLA). Our approach encodes gesture features directly into the latent space, enabling them to participate in both high-level reasoning and low-level action generation, and adopts a dual-VLM architecture to achieve tight coupling between gesture representations and action policies. At the data level, we construct a scalable gesture data generation pipeline by rendering hand models onto real-world scene images. This reduces the sim-to-real visual gap while producing rich data with diverse motion patterns and corresponding pointing annotations. In addition, we employ a two-stage training strategy to equip the model with both gesture perception and action prediction capabilities. We evaluate our approach on multiple real-world robotic tasks, including a controlled block manipulation task for validation and more practical scenarios such as product and produce selection. Experimental results show that incorporating gesture consistently improves target grounding accuracy and human-robot interaction efficiency, especially in complex and cluttered environments. Project page: https://gwxuan.github.io/GesVLA/.

---

## 论文详细总结（自动生成）

# 论文总结：GesVLA：嵌入表示的姿态感知视觉-语言-动作模型

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有视觉-语言-动作（VLA）模型主要依赖纯文本指令，在包含多个相似物体的杂乱场景中面临严重的**空间歧义**，例如“拿起这个”或“放在那里”无法唯一指定目标。
- **动机**：人类在交流中自然使用**指向手势**来提供直观的空间线索。将手势作为并行指令模态引入VLA，有望从根本上弥补文本指令在空间定位上的不足，提升人机交互的自然性和准确性。
- **整体含义**：本文首次系统地将“手势”作为与视觉、语言并列的一级指令模态整合进VLA框架，并通过端到端的潜空间融合、可扩展数据生成和两阶段训练，实现鲁棒的姿态感知操作。

## 2. 方法论：核心思想、关键技术、公式/算法
- **核心思想**：将手势特征**直接编码到潜空间**，使其参与高层意图推理和低层动作生成；采用**双VLM架构**实现手势表示与动作策略的紧密耦合。
- **关键技术细节**：
  - **手势编码**：从手势视频中通过运动动态选取关键帧（停顿帧），使用MediaPipe提取腕部及食指3个关节共4个关键点（共12维），经MLP投影到潜空间得到手势嵌入 \( z_{gi} = \phi(\text{Pose}(g_i)) \)。
  - **双VLM架构**：
    - **VLM_int**（意图推理）：接收手势 \( G \) 和语言指令 \( T \)，输出推理结果 \( y \)（文本描述+可视化提示）及缓存键值对 \((K_{int}, V_{int})\)。
    - **VLM_per**（在线感知）：接收场景观测 \( O \)、语言 \( T \) 和推理输出 \( y \)，通过**交叉注意力**直接利用VLM_int的潜表示，产生动作条件 \((K_{per}, V_{per})\)。
    - **动作专家 \( F_\theta \)**：基于流匹配（Flow Matching），从噪声 \( x_0 \) 迭代去噪生成动作块 \( a_{1:K} \)。
  - **非对称交互**：信息流仅为 \( VLM_{int} \to VLM_{per} \to F_\theta \)，VLM_int不关注下游，使得其KV缓存可一次性计算并在后续复用，计算比例为 \( 1 : T : T \times N \)（T为控制步数，N为去噪步数）。
- **数据生成管线**：
  - 将真实场景RGB-D图像作为背景，使用GroundingDINO检测候选物体，随机选择目标并添加抖动，利用深度图恢复3D点。
  - 生成手部指向运动：从随机方向逼近目标点 \( p_h = p_t - d(\tau + k\Delta) \)，并在目标间移动时加入抛物线抬升项 \( p_{vis} = p_h + h_{max}(1-(2\alpha-1)^2)n_{up} \)，使运动更自然。
  - 最终产出约16k半合成样本，每个样本包含手势视频、语言指令、推理监督信号。
- **训练策略**（两阶段）：
  - **Stage 1**：在合成数据上预训练VLM_int，使用自回归交叉熵损失 \( \mathcal{L}_{int} \) 同时预测文本和离散化坐标。
  - **Stage 2**：冻结VLM_int，在真实机器人演示数据上联合训练VLM_per和动作专家，使用流匹配损失 \( \mathcal{L}_{action} = \mathbb{E}[\|v_\theta(x_t,t,c) - (x_1 - x_0)\|^2] \)。

## 3. 实验设计
- **数据集/场景**：
  - **意图推理测试集**：88个真实场景（≥7个物体），人为提供指姿+固定指令“Pick this up and put it there”，评估目标定位准确率。
  - **机器人操作任务**：三个真实任务，每个重复20次：
    1. **Pick-and-Place Block**：从多个积木中抓取指定积木放到指定盘子。
    2. **Select Jelly**：按指向顺序抓取多个果冻杯放到目标盘。
    3. **Select Fruit/Vegetable**：从两个篮子中按指向顺序抓取指定果蔬放到篮子。
- **评测基准**：
  - **意图推理**：对比Baseline-1（Prompted MLLM：Qwen3.5-plus）和Baseline-2（几何管线：MediaPipe+GroundingDINO+DepthAnythingV2）。
  - **机器人操作**：对比Text-only VLA（将手势替换为文本描述）、MLLM+VLA（用MLLM预测目标）、几何管线+VLA，以及自身消融变体（解耦双VLM，无交叉注意力）。
- **评估指标**：准确率（意图推理）、成功率（操作任务），以及进度分数。

## 4. 资源与算力
- **训练硬件**：4块 **H20 GPU**。
- **推理硬件**：单张 **RTX 4090 GPU**。
- **训练时长**：论文未明确给出具体训练时间。
- **模型规模**：VLM_int和VLM_per均使用 **PaliGemma-2B** 作为骨干；动作专家从零训练。

## 5. 实验数量与充分性
- **实验数量**：
  - 意图推理：88样本（固定场景+固定指令）+消融实验（见表III）。
  - 机器人操作：3个任务×20次=60次重复；消融实验涵盖训练策略（表IV）、模型架构（表V）、数据引擎操作（表IIIa、IIIb）。
- **充分性与公平性**：
  - 对比基线覆盖了**无姿态**、**MLLM推理**、**几何管线**等典型替代方案，且训练数据、超参数保持一致。
  - 消融实验系统性地验证了关键设计（坐标抖动、手势MLP、数据增强、两阶段训练、交叉注意力等）的必要性。
  - 实验在真实环境中进行，具有实际参考价值。
  - 但仅测试了**指向手势**这一种姿态类型，未涵盖其他手势（如抓取、划圈等）。

## 6. 主要结论与发现
1. **引入手势显著提升空间定位**：在意图推理任务上，GesVLA准确率达94.3%，远高于几何管线（59.1%）和MLLM（38.6%）。
2. **端到端潜空间融合优于外部维管**：完整GesVLA在机器人操作总平均成功率为83.3%，优于解耦双VLM变体（61.7%）和所有基线（最高41.7%）。
3. **半合成数据+两阶段训练有效**：合成数据预训练足以支持VLM_int的稳定推理（冻结时表现最好），且无需额外真实姿态标注。
4. **复杂场景提升更明显**：在困难子集（≥5个物体/多对象顺序任务）中，GesVLA的优势尤为突出（如Block任务从45%提升至95%）。

## 7. 优点
- **方法创新**：首次将手势作为一级模态嵌入VLA的潜空间，通过双VLM架构实现高效的非对称信息流。
- **数据生成高效**：渲染手模型到真实场景图像，兼顾真实感与精确标注，且易于扩展新物体、新任务。
- **实验设计严谨**：在多个真实任务、多种基线、消融实验上进行系统对比，充分验证每个设计决策的有效性。
- **部署友好**：推理时只需单张RTX 4090，KV缓存复用降低计算开销。
- **开源与可复现**：提供项目页面和代码（虽未在正文中明确开源链接，但项目页可追踪）。

## 8. 不足与局限
- **手势类型单一**：仅支持指向手势，未涉及抓取、滑动、画圈等其他常见手势，限制了交互的丰富性。
- **场景局限**：实验在固定桌面环境进行，未测试移动机器人、动态场景或非合作人手（如多人交互）。
- **合成数据依赖**：虽然降低了sim-to-real gap，但生成的手势仍可能缺乏真实手柄的细微变化（如肤色、光照阴影），存在微小域偏移。
- **未考虑时间连续性和动态手势**：当前仅使用关键帧，忽略了指向过程中的动态意图变化（如先指左边再指右边）。
- **实验规模有限**：操作任务每个仅重复20次，可能无法覆盖所有失败模式；仅3个任务，泛化性待更多场景验证。

（完）
