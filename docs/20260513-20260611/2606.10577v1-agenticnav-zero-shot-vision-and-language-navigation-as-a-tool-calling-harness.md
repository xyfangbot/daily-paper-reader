---
title: "AgenticNav: Zero-Shot Vision-and-Language Navigation as a Tool-Calling Harness"
title_zh: AgenticNav：零样本视觉与语言导航作为工具调用框架
authors: "Yijian Li, Changze Li, Hantian Shi, Jiaying Luo, Jiyuan Cai, Ming Yang, Tong Qin"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10577v1"
tags: ["query:vtt"]
score: 9.0
evidence: 零样本VLN-CE作为工具调用接口，利用深度和记忆
tldr: 现有零样本连续环境视觉语言导航（VLN-CE）方法受限于航点预测器和低效记忆处理，无法充分利用深度信息。AgenticNav将导航重构为VLM与环境的工具调用框架，通过动作工具直接选择像素移动、深度工具按需查询距离、记忆工具提供紧凑地图与召回功能。在R2R-CE上取得零样本新SOTA，真实环境验证泛化性。该方法摆脱传统航点预测器限制，实现更高效的视觉-语言导航。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10577v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1453, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1363, \"height\": 808, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1445, \"height\": 279, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1440, \"height\": 291, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10577v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1387, \"height\": 912, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10577v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10577v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1350, \"height\": 346, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10577v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 304, \"label\": \"Table\"}]"
motivation: 现有零样本VLN-CE方法依赖航点预测器限制动作空间且未有效利用深度，记忆处理引入冗余，削弱零样本能力。
method: AgenticNav将导航架构成VLM可调用工具集：动作工具直接选择RGB像素，深度工具按需查询，记忆工具提供紧凑地图与召回。
result: 在R2R-CE基准上达到零样本方法新SOTA，真实环境验证泛化能力，消融证明各工具均有显著贡献。
conclusion: AgenticNav通过工具调用范式使VLM直接与环境交互，为零样本导航提供更灵活高效的框架。
---

## 摘要
连续环境中的零样本视觉与语言导航（VLN-CE）最近随着大型视觉语言模型（VLM）的发展而变得可行。然而，现有方法通常依赖学习到的航路点预测器来提议可执行的行动，这严重限制了模型的动作空间，并且未能有效利用深度输入。此外，记忆通常通过积累包含大量无关上下文的冗长文本或视觉历史，或通过检索跨场景经验来处理，这削弱了零样本设置的特性。在本文中，我们重新将零样本VLN-CE视为VLM与环境之间的智能体接口，并提出了AgenticNav，一个轻量级导航框架，将动作、深度和记忆暴露为可调用的工具。动作工具允许VLM直接从RGB观测中选择目标像素，并将其转换为可执行的运动，而不是从预测的航路点中选择。深度通过按需像素深度工具暴露，使VLM能够在关键位置请求精确的度量距离。对于记忆，AgenticNav提供一张紧凑的地图图像，总结历史轨迹，并配有一个召回工具，允许VLM选择性地回顾过去的视觉观测，而不会使提示上下文过载。在R2R-CE基准测试中，AgenticNav在相同VLM骨干网络下，建立了零样本方法中的最新性能（SOTA）。实际环境验证进一步凸显了其相比先前方法的零样本泛化能力。消融实验表明，我们的动作工具设计优于传统航路点预测器，且深度工具和智能体记忆进一步促进了导航性能。

## Abstract
Zero-shot vision-and-language navigation in continuous environments (VLN-CE) has recently become feasible with large vision-language models (VLMs). However, existing methods typically rely on learned waypoint predictors to propose navigable actions. This severely limits the model's action space and fails to leverage depth inputs effectively. Moreover, memory is commonly handled by accumulating long textual or visual histories with substantial irrelevant context, or by retrieving cross-episode experiences, which weakens the zero-shot setting. In this paper, we rethink zero-shot VLN-CE as an agentic interface between the VLM and the environment, and present AgenticNav, a lightweight navigation harness that exposes action, depth, and memory as callable tools. Instead of choosing from predicted waypoints, the action tool allows the VLM to directly select a target pixel in RGB observations, converting it into executable motion. Depth is exposed through an on-demand pixel-depth tool, enabling the VLM to request precise metric distances only where they matter. For memory, AgenticNav provides a compact map image summarizing the historical trajectory, paired with a recall tool that allows the VLM to selectively revisit past visual observations without overwhelming the prompt context. On the R2R-CE benchmark, AgenticNav establishes new state-of-the-art (SOTA) performance among zero-shot methods given the same VLM backbone. Real-world validation further highlights its zero-shot generalization compared to prior methods. Ablations show that our action tool design outperforms traditional waypoint predictors, and that depth tool and agentic memory further contribute to navigation performance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：AgenticNav: Zero-Shot Vision-and-Language Navigation as a Tool-Calling Harness

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：连续环境下的零样本视觉语言导航（VLN-CE）要求智能体在未见过的3D场景中，仅根据自然语言指令导航，而不依赖预定义的导航图或训练数据。近年来，大型视觉语言模型（VLM）作为高层决策器取得进展，但现有方法普遍存在三个关键瓶颈：
  - **动作空间受限**：依赖学习得到的航点预测器（waypoint predictor），只能从有限的候选点中选择，无法让VLM自由选择任何可见位置。
  - **深度信息利用不足**：深度图通常作为航点预测器的输入，但并未显式暴露给VLM进行空间推理。
  - **记忆效率低下**：常见做法是累积长文本或视觉历史到提示中，引入大量无关上下文；或检索跨片段经验，破坏零样本设定。
- **整体含义**：作者认为零样本VLN-CE的核心是VLM与环境的接口设计问题。通过将导航重新构想为“工具调用框架”（tool-calling harness），可以让VLM以更灵活、更高效的方式与感知、记忆和动作模块交互，从而突破上述限制。

## 2. 论文提出的方法论

### 2.1 核心思想
- 将导航过程建模为VLM对四个可调用工具（Tool）的交互过程：`query_depth`（深度查询）、`recall`（视觉回忆）、`move_to`（像素级动作）、`stop`（终止）。VLM在每一步可以多次调用工具，获取证据后再决定下一步动作。

### 2.2 关键技术细节
- **深度工具（Depth Tool）**：VLM可以指定一批候选像素坐标（视角索引+归一化坐标）来查询深度信息。返回内容包括：该像素的度量深度值、5×5邻域的深度统计（最小、均值、中值），以及如果提交该像素作为动作目标时对应的相对偏航角和可执行前进距离。该工具不移动机器人，仅提供数值证据。
- **动作工具（Action Tool）**：VLM通过`move_to(k, u, v)`选择RGB图像中一个像素作为目标。工具将像素反投影到3D空间，计算地面平面上的偏航角和可执行距离，并通过几何安全检测（检查机器人扫过的走廊内是否包含障碍物）决定执行或拒绝（返回Reselect）。这完全避开了航点预测器，让VLM能直接选择任何可见位置。
- **智能体记忆（Agentic Memory）与回忆工具（Recall Tool）**：内存包含两部分：紧凑轨迹地图图像（Map Image）和有限历史图像缓存。轨迹地图始终包含在提示中；而具体的历史帧仅在VLM主动调用`recall(p, k)`时被加载，从而避免上下文过长。
- **安全检测**：对于`move_to`提议的动作，在安全检测中仅考虑身体高度范围内的点，并检查动作路径是否与障碍物碰撞。被拒绝的动作返回Reselect，VLM可以重新选择。

### 2.3 公式或算法流程（文字说明）
- **动作执行流程**：
  1. VLM输出像素坐标 (k, u, v)。
  2. 从对应深度图获取深度 d.
  3. 利用相机内参和外参将像素反投影到3D点 pt。
  4. 计算地面偏航角 θ 和前进距离 ρ（考虑停止裕量、最大步长和步长离散化）。
  5. 对所有有效深度点进行反投影，筛选身体高度范围内的点作为障碍物。
  6. 检查在机器人扫过的走廊内是否有障碍物（坐标为0到ρ+机器人半径沿前进方向，侧向距离小于机器人半径）。
  7. 若安全则执行动作 (θ, ρ)；否则返回Reselect。

## 3. 实验设计

### 3.1 数据集与场景
- **模拟环境**：Habitat模拟器，R2R-CE数据集的val unseen子集，使用与先前零样本工作相同的100个评估片段（100-episode protocol）。
- **真实世界实验**：作者在实验室、办公室和室外庭院三种场景中定义了30个真实世界导航片段，涵盖标志阅读、长距离导航、模糊指令和精确定位等挑战。机器人使用全向四轮平台、Intel RealSense D435 RGB-D相机、LiDAR、FAST-LIO定位。

### 3.2 Benchmark
- **模拟实验指标**：成功率(SR)、Oracle成功率(OSR)、成功加权路径长度(SPL)、归一化动态时间扭曲(nDTW)、导航误差(NE)。
- **真实世界实验指标**：SR和NE。

### 3.3 对比方法
- **有监督方法**：CMA、RecBERT、BEVBert、ETPNav（用于参考）。
- **零样本方法**：Random、LXMERT、MapGPT-CE、DiscussNav、NavGPT-CE、Open-Nav（多种VLM）、EvoNav、SmartWay。标记了哪些方法使用了训练模块、跨片段经验等。特别注明了相同VLM骨干的重现版本（*）以确保公平比较。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。作者仅提及使用VLM API（如GPT-5.5、Gemini-2.5-Pro等）进行推理，没有涉及模型训练。因此，无法从文中获取具体算力需求信息。

## 5. 实验数量与充分性

- **模拟实验**：主结果表格（表1）对比了约15种方法，涵盖了同一VLM骨干（GPT-5.5、Gemini-2.5-Pro）的公平比较。
- **消融实验**（表3）：系统性地移除每个工具组件（动作工具替换为航点预测器、移除深度工具、移除深度工具但添加深度图像作为输入、移除智能体记忆、仅保留地图图像等），共5组消融。
- **VLM骨干消融**（表4）：测试了5种不同VLM（GPT-5.5、Gemini-2.5-Pro、Gemini-3-Flash、MiMo-V2.5、Qwen3.6-27B）在同一工具框架下的性能。
- **真实世界实验**（表2）：对比了SmartWay与AgenticNav不同视角数（1视图、4视图）的性能，共3组对比。
- **公平性**：*标注了相同的VLM骨干复现版本；EvoNav因使用了跨片段经验而被排除在零样本对比之外；所有实验在相同评估协议下进行。
- **充分性**：实验设计覆盖了消融、VLM变化、真实世界验证，对比了多种SOTA方法，结论支持充分。

## 6. 论文的主要结论与发现

1. **AgenticNav在R2R-CE上取得零样本新SOTA**：以GPT-5.5为骨干，SR达到55%，SPL达到48.41%，比相同骨干的SmartWay分别高11和13.37个百分点。
2. **工具接口优于传统航点预测器**：将动作工具替换为智能航点预测器后SR/SPL下降5/4.42。
3. **按需深度查询比全图深度输入更有效**：移除深度工具导致SR降至42%，而强制加入深度图像也只能部分恢复到53%，低于完整版（55%）。
4. **选择性记忆优于完全上下文累积**：移除整个智能体记忆后SR降至41%，仅保留地图图像为51%，完整版达到55%——表明回忆工具有额外增益。
5. **工具设计本身具有跨VLM鲁棒性**：不同VLM骨干（包括开源模型Qwen3.6-27B）均在工具框架下实现合理性能，表明框架不依赖于特定VLM。
6. **真实世界验证**：AgenticNav在复杂真实场景中SR达到46.7%（4视图），是SmartWay（23.3%）的两倍，展示了从仿真到真实的泛化能力。

## 7. 优点

- **方法创新**：首次从工具调用角度系统重新设计VLM与环境接口，完全摆脱航点预测器，赋予VLM像素级选择自由。
- **设计简洁高效**：深度工具按需查询避免模型解析全深度图；记忆工具通过紧凑地图+选择性回忆避免上下文过长，既保留必要信息又减少冗余。
- **实验严谨**：进行了大量公平对比（相同VLM骨干重现基线）、消融实验涵盖所有核心组件、真实世界验证弥补了模拟与现实的鸿沟。
- **泛化性强**：真实世界实验在不同场景（实验室、办公室、室外）均优于强基线，且开源VLM亦有效，体现方法对硬件和领域的鲁棒性。

## 8. 不足与局限

- **VLM自身局限性**：作者在失败分析中指出，88.9%的模拟失败和71.4%的真实失败源自VLM的错误决策（如错误分支/房间/实例决策、过早停止、未识别目标）。工具框架虽然改善了动作空间，但无法解决VLM推理本身的缺陷。
- **系统依赖**：假设准确的定位、可靠的深度传感器和稳定的VLM API连接。实际部署中API延迟（占真实失败21.4%）和网络不稳定可能影响实时性。
- **计算开销**：虽然采用API减少了本地算力需求，但大型VLM的推理成本较高且延迟不可控。论文未讨论完整的延迟分析或成本估算。
- **动作执行粒度**：动作工具将VLM选择的像素反投影为单步前进距离，没有涉及复杂的可通行区域规划或局部避障能力，可能需要在复杂动态环境中进一步改进。
- **实验覆盖局限**：模拟仅在R2R-CE一个数据集上验证，未在更多VLN-CE基准（如RxR等）上评估。真实世界实验规模较小（30个场景），可能不足以全面反映泛化性能。

（完）
