---
title: "OSCAR: Obstacle Survival Curves for Adaptive Robot Navigation"
title_zh: "OSCAR: 自适应机器人导航的障碍物生存曲线"
authors: "Hshmat Sahak, Aoran Jiao, Nicholas Rhinehart, Tim Barfoot"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.00990v1"
tags: ["query:rl-robotics"]
score: 9.0
evidence: 自适应生存模型用于临时障碍物环境下的机器人导航
tldr: "移动机器人在图导航中遇到临时障碍时，固定等待或绕行策略效率低下。OSCAR框架利用生存分析在线学习不同障碍物类别的清除时间分布，并结合记忆与图规划器计算自适应等待阈值。仿真中，学习策略在20次观测内收敛到与先知策略相差1%以内，真实机器人50次部署后性能持续提升。该方法首次将右删失数据的生存模型用于导航等待决策，实现自适应平衡等待与绕行。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00990v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 942, \"height\": 609, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1429, \"height\": 520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1451, \"height\": 286, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1445, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1444, \"height\": 435, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 869, \"height\": 1104, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1461, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1083, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00990v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1443, \"height\": 432, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00990v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 354, \"label\": \"Table\"}]"
motivation: 固定等待或绕行规则无法适应不同障碍物持续时间的变化，导致导航效率低下。
method: 在线学习类条件残差清除时间分布，处理右删失观测，结合障碍物记忆与图规划计算耐心阈值。
result: "仿真中策略在20次观测内收敛到oracle的1%误差内，真实机器人50次后优于所有启发式基线。"
conclusion: 生存建模框架能自适应平衡等待与绕行，在线优化导航决策。
---

## 摘要
移动机器人在已知路径图上导航时，若临时障碍物阻塞关键边，可能产生代价高昂的导航错误：在停放的推车后等待过久浪费时间，但在几秒后就会移动的人周围立即改道同样低效。标准的反应式避障处理障碍物周围的局部运动，而固定的等待或改道规则忽略了不同障碍物类型通常持续的时间。我们提出OSCAR：一种基于图的导航自适应生存建模框架，用于处理临时阻塞。假设在遇到障碍物时能获得类别标签，机器人从在线经验中学习类别条件化的剩余清除时间分布，包括在清除发生前改道时产生的右删失观测。这些生存模型被集成到一个时间依赖的图规划器中，该规划器维护障碍物记忆，并在每个被阻塞的边上计算一个耐心阈值：在采取替代路线之前等待多长时间。该方法跨情节持续更新其清除估计，并使用它们来平衡等待与改道。我们在仿真中以及在大学中庭（障碍物包括人、椅子、垃圾桶和管道）的真实移动机器人上评估了该方法。在仿真中，学习到的策略的到达时间收敛到与能够访问真实清除分布的原型相比在1%以内，每个障碍物类别只需不到20次观测，优于所有启发式基线。实际部署证实了该策略在线改进，在50次导航情节中根据经验调整其耐心阈值。

## Abstract
A mobile robot following a graph of known routes can make costly navigation errors when a temporary obstacle blocks a critical edge: waiting too long behind a parked cart wastes time, but immediately rerouting around a person who would move in a few seconds is also inefficient. Standard reactive obstacle avoidance addresses local motion around obstacles, while fixed wait-or-reroute rules ignore how long different obstacle types tend to persist. We propose OSCAR: an adaptive survival-modeling framework for graph-based navigation with temporary blockages. Assuming obstacle class labels are available at encounter time, the robot learns class-conditioned residual clearance-time distributions from online experience, including right-censored observations when it reroutes before observing clearance. These survival models are integrated into a time-dependent graph planner that maintains obstacle memory and computes a patience threshold at each blocked edge: how long to wait before taking an alternate route. The method continuously updates its clearance estimates across episodes and uses them to balance waiting against rerouting. We evaluate the approach in simulation and on a real mobile robot in a university atrium with obstacles including people, chairs, bins, and tubes. In simulation, the learned policy's time-to-goal converges to within 1% of an oracle with access to ground-truth clearance distributions after fewer than 20 observations per obstacle class, outperforming all heuristic baselines. Real-world deployment confirms that the policy improves online, adapting its patience thresholds from experience across 50 navigation episodes.

---

## 论文详细总结（自动生成）

# OSCAR: 自适应机器人导航的障碍物生存曲线——详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：移动机器人在已知路径图上导航时，经常遇到临时障碍物（如人、椅子、垃圾桶、管道）阻塞关键边。传统方法要么采用局部反应式避障（仅处理微观运动），要么采用固定规则（如“总是等待”或“总是绕行”），但忽略了不同障碍物类型具有不同的持续时间分布。
- **核心问题**：如何在线学习不同类别障碍物的清除时间分布，并据此自适应决策“等待多久再绕行”，以最小化到达目标的期望时间。
- **整体含义**：将生存分析（Survival Analysis）首次引入图导航的等待-绕行决策，通过在线学习障碍物清除时间的生存曲线，结合障碍物记忆和时间依赖图规划，实现导航策略的持续自适应优化。

## 2. 论文提出的方法论

### 核心思想
- 将障碍物清除视为“时间到事件”（time-to-event）预测问题，为每个障碍物类别学习一个生存模型（Survival Model），描述障碍物保持阻塞的概率随时间的变化。
- 在遇到阻塞边时，利用当前生存模型计算“耐心阈值”（Patience Threshold）\(W^*\)，即最优等待时间；若在\(W^*\)内障碍物清除则继续原路径，否则绕行。
- 策略在线更新：从每次遭遇中收集清除时间或右删失（reroute前未清除）观测，更新Kaplan-Meier生存估计。

### 关键技术细节
1. **生存模型学习**：使用Kaplan-Meier非参数估计器为每个障碍物类别\(k\)拟合生存函数\(S_k(\tau) = P(R^{(k)} > \tau)\)，支持右删失数据。
2. **时间依赖边代价**：边代价\(c(e,t) = w(e) + \Delta(e,t)\)，其中\(\Delta(e,t)\)为期望额外延迟。对于无记忆的边，\(\Delta_{\text{new}} = p_{\text{block}} \sum_k p_k \mu_k\)；对于有记忆的边（之前观测到阻塞），利用条件概率计算。
3. **等待-绕行决策**：给定耐心阈值\(W\)，期望剩余到达时间：
   \[
   J(W) = \int_0^W A_{\text{clear}}(c) dF_k(c) + S_k(W) A_{\text{avoid}}(W)
   \]
   其中\(A_{\text{clear}}(c)\)为障碍物在\(c\)秒后清除的剩余时间，\(A_{\text{avoid}}(W)\)为等待\(W\)秒后绕行的剩余时间。通过枚举有限候选集（KM事件时刻和0, \(W_{\max}\)）找到使\(J(W)\)最小的\(W^*\)。
4. **时间依赖全局规划**：使用满足FIFO性质的边到达函数，保证Dijkstra算法仍然适用；在等待决策时分别计算\(A_{\text{clear}}\)和\(A_{\text{avoid}}\)（使用功能化Bellman-Ford或Dijkstra）。
5. **在线适应**：每个情节后更新KM曲线和阻塞概率估计。

### 算法流程（文字说明）
1. 初始化：各障碍物类别的KM生存曲线为空，阻塞概率初始猜测。
2. 每个导航情节：
   - 机器人沿当前路径移动，到达阻塞边时，感知系统提供障碍物类别。
   - 根据该类别的当前生存模型，计算候选耐心阈值集合（0, KM事件时间, \(W_{\max}\)）。
   - 对每个候选\(W\)，计算\(A_{\text{clear}}(W)\)和\(A_{\text{avoid}}(W)\)（使用时间依赖Dijkstra），进而计算\(J(W)\)，选取最小化\(J(W)\)的\(W^*\)。
   - 等待至多\(W^*\)秒：若障碍物清除则继续原路径；否则标记该边为不可用，重新规划替代路径。
   - 记录观测数据（清除时间或删失时间），在情节结束后更新对应类别的KM曲线和全局阻塞概率。
3. 重复情节，持续优化。

## 3. 实验设计

### 使用的数据集/场景
- **仿真实验**：自定义图网络（包含多条替代路径），障碍物按泊松过程生成，包括四类（人、椅子、垃圾桶、管道），每类具有对数正态分布的清除时间（人短、椅子/垃圾桶中等、管道长）。稳态边缘阻塞概率设为5%。
- **真实世界实验**：校园中庭内的移动机器人（Clearpath Jackal），使用LiDAR反射图像和ChatGPT进行障碍物分类（类标签：人、椅子、垃圾桶、管道）。分为“协调场景”（由仿真生成清单，手动放置障碍物）和“自然场景”（仅有人和椅子自然出现）。

### Benchmark对比方法
- **Always Wait**：总是等待，直到障碍物清除（永久等待）。
- **Always Reroute**：一遇到阻塞立即绕行。
- **Rule-Based**：规则启发式（人则等待，其余则绕行）。
- **Greedy CTP**：贪心加拿大旅行者问题风格，永久禁止阻塞边。
- **OSCAR w/o Memory**：无记忆的OSCAR变体（忽略之前观测到的阻塞边状态）。
- **Oracle**：使用真实清除分布、阻塞概率和类别概率，作为性能上限。
- **OSCAR（本文方法）**。

### 实验数量与充分性
- **仿真**：100个随机种子，每个种子运行1000个情节。统计平均到达时间、成功率、绕行次数、等待时间、阻塞边数。进行了数据效率实验（限制每个类别的KM样本数量）。还测试了网格图场景。
- **真实世界**：协调场景50个情节；自然场景（仅人/椅子）未特定说明情节数，但展示了在线学习曲线。使用了5次独立运行之一的结果（由于人类参与无法多次复制）。
- **充分性**：仿真实验覆盖了多种基线、消融（无记忆）、不同样本量影响、不同图结构（网格图），统计量丰富（表1）。真实世界展示了在线学习和策略行为分布。但真实场景仅一个校园环境，且障碍物类别有限，未在更复杂或开放词汇场景中验证。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量或训练时长**。仿真和真实实验均在普通计算平台（可能CPU）上运行，因为KM学习计算量小，规划使用Dijkstra（复杂度低）。文中未提及深度学习训练，障碍物分类使用预训练的ChatGPT API，不涉及本地训练资源。

## 5. 实验数量与充分性：是否客观、公平

- **数量充分性**：仿真100个随机种子保证了统计稳健性；真实场景50次情节合理展现了在线适应能力。消融实验（OSCAR w/o Memory）有效隔离了记忆模块贡献；与多种启发式基线对比，包括贪婪CTP这种极端策略。
- **公平性**：所有基线均在相同障碍物过程下评估；Oracle作为理想上限；真实实验中各方法在不同“协调”场景下比较（图8显示仅对比Always Wait？文中只展示了本文方法与Always Wait的对比，未展示其他基线在真实场景的结果）。存在潜在不公平：真实实验中未与Always Reroute、Rule-Based等对比（因为人为参与难以重复相同场景）。文中承认了这一点。
- **客观性**：仿真结果详细，数据充分；真实场景结果可信但缺乏多场景多次重复。

## 6. 论文的主要结论与发现

- 仿真中，OSCAR（学习策略）在不到20个观测样本/类别后，平均到达时间收敛到Oracle（真实分布）的1%以内，性能优于所有启发式基线（表1：OSCAR 160.7s vs Oracle 159.3s，其他基线在210-3135s）。
- 学习策略能自适应产生有意义的耐心阈值：为人等待较长时间，为椅子/垃圾桶混合策略，为管道立即绕行（图6、图8）。
- 真实世界50次导航后，到达时间持续下降，最终稳定在约200s，优于Always Wait基线；策略行为与仿真一致。
- 右删失数据的生存建模能有效处理提前绕行的情况，避免样本浪费。

## 7. 优点

- **方法创新性**：首次将生存分析系统应用于图导航中的等待-绕行决策，自然处理了右删失观测（绕行前障碍物未清除），这是一大亮点。
- **实用性**：在线学习，无需离线训练或大量预采集数据；计算效率高（KM估计简单，规划使用Dijkstra）。
- **实验结果扎实**：仿真实验统计方法严谨（100随机种子），消融实验有效；真实世界验证了在线适应能力。
- **理论完整性**：证明了时间依赖边代价的FIFO性质（附录A），保证Dijkstra可用；并证明了在KM估计下最优耐心阈值只需枚举有限时刻。
- **清晰的模块化设计**：生存模型、时间依赖代价、等待决策、全局规划各层解耦，便于扩展。

## 8. 不足与局限

- **决策模块局限**：未考虑重新规划后可能再次遇到阻塞的情况，导致绕行成本高估；另外，等待时间\(W^*\)由当前生存估计计算，形成反馈循环（censoring概率依赖决策），可能违反标准独立删失假设，尤其在早期训练中引入偏倚。文章承认了这点。
- **环境与感知假设**：假设障碍物分类可靠（真实场景中使用ChatGPT在封闭集合上表现良好，但开放场景可能失败）；未区分同一物理障碍物的反复观察（每次视为新遭遇）；阻塞概率被假设为所有边相同，忽略空间异质性。
- **实验覆盖局限**：真实世界仅一个校园中庭环境，障碍物类别少，且协调场景由仿真生成，不完全自然；未在更复杂（如拥挤走廊、室外）或更多类别场景中验证。未与其他高级方法（如强化学习、预测性导航）对比。
- **未讨论计算实时性**：虽然理论上复杂度低，但未报告规划耗时（尤其功能化Bellman-Ford实现）。
- **缺少理论收敛保证**：尽管仿真收敛，但未提供自适应决策下的学习收敛性证明。

（完）
