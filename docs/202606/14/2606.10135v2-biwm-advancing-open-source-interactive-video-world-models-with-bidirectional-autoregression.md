---
title: "BiWM: Advancing Open-Source Interactive Video World Models with Bidirectional Autoregression"
title_zh: "BiWM: 通过双向自回归推进开源交互式视频世界模型"
authors: "Shaohao Rui, Xiaofeng Mao, Zhanyu Zhang, Peijia Lin, Yansong Zhu, Yibo Zhang, Haibin Wan, Weijie Ma"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10135v2"
tags: ["query:world-model"]
score: 9.0
evidence: 交互式视频世界模型
tldr: 现有交互式视频世界模型如minWM基于因果自回归，需多阶段训练并存在误差累积，质量低于双向模型。BiWM提出首个双向自回归范式的全栈框架，仅用控制微调和几步DMD蒸馏两个阶段，从预训练骨干构建动作/相机可控世界模型。该框架支持Wan2.1-1.3B等多种模型，实现真实相机控制，集成历史压缩和可选的NVFP4 4-bit管线。通过添加GAN和前向KL目标保持场景动态，开源后可用于资源受限的高保真环境模拟。
source: arxiv
selection_source: fresh_fetch
motivation: 现有因果自回归视频世界模型训练复杂且误差累积，而双向模型质量更高但缺乏开源框架。
method: 采用双向自回归，通过控制微调注入相机控制，再用几步分布匹配蒸馏将骨干转为可控世界模型，仅两阶段训练。
result: 在多个大模型上实现真实相机控制，支持长序列推演，训练仅需数百步，质量优于因果模型。
conclusion: BiWM作为首个开源双向自回归交互式视频世界模型框架，以低成本实现高保真环境模拟。
---

## 摘要
将双向视频扩散模型过渡到自回归范式可提升视频世界模型的交互性，但现有因果流水线需要多个阶段（控制微调、自回归训练、因果初始化、少步蒸馏），且由于误差累积，其质量仍落后于双向模型。近期世界模型（如Yume-1.5和Matrix-Game-3.0）转而采用双向自回归方法，通过自纠正误差传播获得高保真度和稳定的长程推演，但开源框架（如minWM）仅支持因果模型。我们提出BiWM，这是首个面向双向自回归范式下交互式视频世界模型的全栈框架，联合优化生成质量和推理速度。基于预训练视频主干，BiWM通过微调注入相机控制，然后运行少步分布匹配蒸馏（DMD）阶段，将主干转化为动作/相机可控的世界模型：仅需两个训练阶段（而minWM需要四个），在8块H200 GPU上仅需数百步即可收敛。单一配方即可覆盖Wan2.1-1.3B、Wan2.2-5B、HunyuanVideo-1.5-8B和LTX-2.3-22B，并支持对现有双向模型的二次微调。BiWM在minWM失去控制力的场景中实现了真实的相机控制，集成了可插拔的历史压缩（FramePack风格和PackForcing风格）以支持长程推演，并提供可选的NVFP4 4位训练/推理流水线。为对抗DMD的模式寻求退化，我们添加了GAN和覆盖质量的forward-KL目标，以保留场景动态。我们将BiWM开源，以支持资源受限的研究和高保真环境模拟。

## Abstract
Transitioning bidirectional video diffusion models into an autoregressive paradigm improves the interactivity of video world models, but existing causal pipelines need many stages (control fine-tuning, autoregressive training, causal initialization, few-step distillation) and still trail bidirectional models in quality due to error accumulation. Recent world models such as Yume-1.5 and Matrix-Game-3.0 instead adopt a bidirectional autoregressive approach, gaining fidelity and stable long-horizon rollout from self-correcting error propagation, yet open-source frameworks (e.g., minWM) support only causal models. We present BiWM, the first full-stack framework for interactive video world models under the bidirectional autoregressive paradigm, jointly optimizing generation quality and inference speed. From a pretrained video backbone, BiWM injects camera control by fine-tuning, then runs a few-step Distribution Matching Distillation (DMD) stage that turns the backbone into an action/camera-controllable world model: just two training stages instead of four in minWM, converging in a few hundred steps on 8xH200 GPUs. A single recipe spans Wan2.1-1.3B, Wan2.2-5B, HunyuanVideo-1.5-8B, and LTX-2.3-22B, and also supports secondary fine-tuning of existing bidirectional models. BiWM enables real-world camera control where minWM loses controllability, integrates pluggable history compression (FramePack-style and PackForcing-style) for long rollouts, and offers an optional NVFP4 4-bit training/inference pipeline. To counter DMD's mode-seeking degradation, we add GAN and mass-covering forward-KL objectives that preserve scene dynamics. We open-source BiWM for resource-constrained research and high-fidelity environment simulation.

---

## 论文详细总结（自动生成）

# 论文详细总结：BiWM: 通过双向自回归推进开源交互式视频世界模型

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有交互式视频世界模型（如 minWM）采用因果自回归范式，存在两个主要问题：（1）训练流水线复杂，需要四个阶段（控制微调、自回归训练、因果初始化、少步蒸馏），成本高；（2）因果模型在长程推演中存在误差累积，导致生成质量低于双向模型。近期一些闭源模型（如 Yume-1.5、Matrix-Game-3.0）已转向双向自回归方法，通过自纠正传播获得更高保真度和更稳定的长程推演，但缺乏开源实现。
- **整体含义**：论文提出 BiWM，首个面向双向自回归范式交互式视频世界模型的全栈开源框架，旨在以更低训练成本（仅两阶段）、更少算力（数百步收敛）实现与双向模型相当甚至更优的生成质量，并支持真实相机控制、长程推演和资源受限场景下的高保真环境模拟。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：基于预训练视频扩散骨干，通过“控制微调 + 少步分布匹配蒸馏（DMD）”两阶段训练，将预训练模型转化为双向自回归的动作/相机可控世界模型，替代因果自回归的多阶段流水线。
- **关键技术细节**：
  - **阶段一：控制微调**。在预训练视频骨干（如 Wan2.1-1.3B、Wan2.2-5B、HunyuanVideo-1.5-8B、LTX-2.3-22B）上微调，注入相机控制能力（动作/相机参数）。
  - **阶段二：分布匹配蒸馏（DMD）**。运行少步 DMD 将骨干蒸馏为可控世界模型，仅需几百步即可收敛。该步骤将双向扩散模型转换为自回归形式（生成下一帧时以历史帧为条件）。
  - **抗退化策略**：为对抗 DMD 可能导致的模式寻求（mode-seeking）退化，添加了 GAN 损失和覆盖质量的 forward-KL 目标，保留场景动态。
  - **长程推演支持**：集成可插拔的历史压缩模块，包括 FramePack 风格和 PackForcing 风格，支持长序列推演。
  - **低精度管线**：提供可选的 NVFP4 4-bit 训练/推理流水线，降低资源需求。
  - **兼容性**：单一配方覆盖多种预训练骨干，同时支持对现有双向模型的二次微调。

## 3. 实验设计：数据集、基准、对比方法

- **数据集 / 场景**：摘要未明确说明具体数据集（可能涉及视频世界模型常用的仿真或真实场景）。论文声称在 minWM 失去控制力的场景中实现了真实相机控制，暗示使用了包含相机运动控制的视频数据。
- **Benchmark**：未提及标准基准（如 Minecraft、CARLA 等），但通过与 minWM 等现有因果模型对比来评估控制能力和生成质量。
- **对比方法**：主要对比 minWM（因果自回归框架），可能还对比了原始预训练骨干（无控制）或闭源双向模型（定性比较）。摘要未提供定量指标（如 FVD、PSNR 等），仅强调“真实相机控制”和“质量优于因果模型”。

## 4. 资源与算力

- 明确说明：训练在 **8 块 H200 GPU** 上运行，两个阶段总共仅需 **数百步** 收敛。与 minWM 需要四个阶段相比，算力和时间成本大幅降低。
- 未提供每步具体耗时或总 GPU 小时数，但指出“资源受限”场景下仍可使用（得益于低精度管线）。

## 5. 实验数量与充分性

- **实验数量**：摘要信息有限，未列出消融实验数量或具体对比实验组数。但从框架设计看，可能涵盖了不同骨干网络（四种不同规模）以及有无历史压缩、有无 GAN/forward-KL 的消融。
- **充分性评价**：由于缺乏定量结果和具体数据集/指标，**无法判定实验的充分性**。论文声称“在 minWM 失去控制力的场景中实现了真实相机控制”，但未提供与 SOTA 双向模型的定量对比。需要更多细节（如 FVD、成功帧率、控制误差等）来支撑结论。但框架的通用性和低资源特性是合理的设计贡献。

## 6. 论文的主要结论与发现

- **主要结论**：BiWM 是首个针对双向自回归交互式视频世界模型的全栈开源框架，相比因果自回归范式（minWM），训练阶段从四阶段减少到两阶段，收敛仅需数百步，生成质量更高（无累积误差），并支持真实的相机控制和长程推演。
- **发现**：双向自回归通过自纠正错误传播，能有效避免因果模型的误差累积；通过添加 GAN + forward-KL 可缓解 DMD 模式退化，保持场景动态；历史压缩和 4-bit 量化可进一步降低资源需求。

## 7. 优点

- **训练高效**：仅需两阶段（控制微调 + DMD），数步步数收敛，显著降低算力和时间成本。
- **质量优于因果模型**：双向自回归避免误差累积，生成更稳定、更长周期的高保真视频。
- **开源且通用**：单一配方覆盖多种主流视频骨干（1.3B~22B），支持二次微调，便于社区推广。
- **实用功能丰富**：集成可插拔历史压缩、NVFP4 4-bit 管线，支持低资源部署；添加抗退化目标保障场景动态。
- **真实控制能力**：在 minWM 失效的场景中仍能实现有效相机控制。

## 8. 不足与局限

- **定量评估缺失**：论文摘要未提供任何定量指标（FVD、PSNR、控制成功率、推理速度等），难以客观比较与现有方法的性能差距。实验部分需要更严格的标准基准。
- **数据集与场景不明确**：未说明训练和测试使用哪些视频数据/环境，可能限制通用性验证。
- **与其他双向模型对比不足**：仅与因果模型 minWM 对比，未与同类双向模型（如 Yume-1.5 闭源）或更先进的方法（如 DiT 变种）定量对比。
- **消融实验细节缺失**：未展示各组件（GAN、forward-KL、历史压缩、4-bit 管线）的独立影响。
- **潜在偏差风险**：开源框架可能只适用于特定预训练骨干架构（Wan、HunyuanVideo、LTX），迁移到其他架构需验证；DMD 蒸馏是否保证所有场景下无退化需更多证据。
- **应用限制**：虽然支持长程推演，但长期稳定性仍可能受限于历史压缩的精度和骨干容量；真实世界相机控制需要精确动作/相机参数输入，可能增加部署门槛。

（完）
