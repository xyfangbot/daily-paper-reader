---
title: Dynamic Execution Horizon Prediction for Chunk-based Robot Policies
title_zh: 面向基于分块的机器人策略的动态执行步长预测
authors: "Yuchi Zhao, Miroslav Bogdanovic, Arjun Sohal, Liyu Tao, Kourosh Darvish, Alán Aspuru-Guzik, Florian Shkurti, Animesh Garg"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11408v1"
tags: ["query:vla-robotics"]
score: 9.0
evidence: 针对包括VLA在内的分块机器人策略的动态地平线预测
tldr: 动作分块策略固定执行步长，在精细操作中开环控制效果差。本文提出动态执行步长预测(DEHP)，通过在线强化学习训练轻量预测分支，完全冻结原策略。实验证明DEHP大幅提升多种高精度和长时域任务的成功率，自适应预测短步长用于精细阶段、长步长用于自由运动，平衡了开环效率与闭环反应性。该方法兼容黑箱策略且无需重新训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11408v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 984, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11408v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1562, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11408v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11408v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1700, \"height\": 967, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11408v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1656, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11408v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1531, \"height\": 1153, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11408v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1710, \"height\": 627, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11408v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1493, \"height\": 1191, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11408v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1471, \"height\": 1531, \"label\": \"Table\"}]"
motivation: 固定执行步长导致开环控制，难以适应需要频繁重规划的精细操作任务，且高度依赖经验调参。
method: 在线强化学习训练轻量级执行步长预测分支，保持预训练分块策略完全冻结，实现与黑箱策略兼容的自适应步长。
result: 在多种高精度和长时域操作任务中成功率达到大幅提升，定性分析显示精细阶段预测短步长、自由运动预测长步长。
conclusion: DEHP通过动态预测执行步长，平衡了开环执行的效率和闭环控制的反应性，且无需修改原策略，兼容性强。
---

## 摘要
行动分块已成为现代机器人策略中的标准设计，从扩散/流策略到视觉-语言-行动模型，其中策略预测一系列动作并执行其中固定数量的动作，而不是一次执行一步。然而，这种范式依赖于一个关键假设：固定的执行步长。在分块执行期间，策略以开环方式运行，这对于需要频繁重新规划的精细操作任务尤其成问题。在实践中，执行步长通常通过经验调整来选择，并且高度依赖于任务。为此，我们提出了动态执行步长预测（DEHP），这是一种有效的方法，它使用在线强化学习来训练一个轻量级的执行步长预测分支，同时完全冻结预训练的分块策略。这使得该方法与黑盒分块策略兼容，并将执行步长调整的影响与底层动作生成器的变化隔离开来。在我们的评估中，DEHP大幅提高了不同高精度和长时程操作任务的成功率。我们的定性分析进一步表明，DEHP在任务的精细阶段预测更短的执行步长，在自由空间运动中预测更长的步长。这样，DEHP平衡了开环分块执行的效率与闭环单步控制的响应性。项目页面：https://dehp-chunking.github.io/

## Abstract
Action chunking has become a standard design in modern robot policies, from diffusion/flow policies to vision-language-action models, where the policy predicts a sequence of actions and executes a fixed number of them instead of acting one step at a time. However, this paradigm relies on a key assumption: a fixed execution horizon. During chunk execution, the policy operates open-loop, which is particularly problematic for fine-grained manipulation tasks that require frequent replanning. In practice, the execution horizon is typically chosen through empirical tuning and is highly task-dependent. To this end, we propose Dynamic Execution Horizon Prediction (DEHP), an effective method that trains a lightweight execution-horizon prediction branch using online reinforcement learning while keeping the pretrained chunk policy completely frozen. This makes the method compatible with black-box chunk policies and isolates the effect of adapting the execution horizon from changes to the underlying action generator. Across our evaluations, DEHP improves the success rate of different high-precision and long-horizon manipulation tasks by a large margin. Our qualitative analysis further shows that DEHP predicts shorter execution horizons during fine-grained stages of the task and longer horizons during free-space motion. In this way, DEHP balances the efficiency of open-loop chunk execution with the reactivity of closed-loop single-step control. Project page: https://dehp-chunking.github.io/

---

## 论文详细总结（自动生成）

# 论文总结：Dynamic Execution Horizon Prediction for Chunk-based Robot Policies

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现代机器人策略（如扩散策略、流策略、视觉-语言-行动模型）广泛采用**动作分块**（action chunking）设计，即一次预测一个动作序列并执行固定数量的动作，而非每一步重新规划。这种范式假定**固定的执行步长**（execution horizon），但存在根本性缺陷：
  - 固定步长导致开环执行，在需要频繁重规划的**精细操作任务**（如接触交互）中反应性差。
  - 步长选择高度依赖任务，通常通过经验调参，实际中最优步长在不同任务阶段（自由运动 vs 精细对齐）差异极大。
- **整体含义**：提出一种**动态执行步长预测**方法（DEHP），在不修改预训练分块策略的前提下，通过在线强化学习使机器人在不同任务阶段自适应调整执行步长，平衡开环执行的效率与闭环控制的反应性。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：在预训练的分块策略（Diffusion Policy）顶部添加一个轻量级的**执行步长预测头**（length head）。该头以当前观测和预测的完整动作块为输入，输出一个分类概率分布（执行步长 \( h \in \{1, \dots, H\} \)），决定从预测块中执行前 \( h \) 个动作后重规划。
- **关键技术细节**：
  1. **形式化**：将动态执行步长预测建模为**半马尔可夫决策过程（semi-MDP）**。策略联合分布分解为：\(\pi(a_{1:H}, h | s) = \pi_{\text{act}}(a_{1:H} | s) \cdot \pi_{\text{len}}(h | s, a_{1:H})\)，其中 \(\pi_{\text{act}}\) 是冻结的基策略，\(\pi_{\text{len}}\) 是待训练的长度预测头。
  2. **目标函数**：证明分块级折扣回报与原始MDP折扣回报等价（附录提供证明）。因此只需训练状态值函数 \(V_\phi(s)\)。
  3. **优化算法**：采用**PPO**（Proximal Policy Optimization）优化 \(\pi_{\text{len}}\)。由于基策略冻结，重要性比率仅依赖于长度头：\(\rho_k(\theta) = \frac{\pi_{\text{len},\theta}(h_k | s_{t_k}, a^{(k)}_{1:H})}{\pi_{\text{len},\theta_{\text{old}}}(h_k | s_{t_k}, a^{(k)}_{1:H})}\)。
  4. **GAE估计**：使用分块级GAE估计器 \(\hat{A}_k = \sum_{\ell \ge 0} (\gamma \lambda)^{t_{k+\ell} - t_k} \delta_{k+\ell}\)，其中 \(\delta_k = \bar{R}_k + \gamma^{h_k} V_\phi(s_{t_{k+1}}) - V_\phi(s_{t_k})\)，\(\bar{R}_k = \sum_{j=0}^{h_k-1} \gamma^j r_{t_k+j}\)。
  5. **实现细节**：
     - 值函数采用**分布分类**（distributional categorical critic），支撑集B=201个原子，损失用two-hot投影交叉熵。
     - 长度头初始化为均匀分布，避免偏向固定步长。
     - 先预热评论家若干轮再更新长度头。

## 3. 实验设计

- **环境/场景**：
  - **FurnitureBench**：两个长时程组装任务 `one_leg`（单腿）和 `round_table`（圆桌），含拾取、放置、插入、拧螺丝等技能。演示数据800条轨迹。
  - **IsaacLab**：两个精细操作任务——**多阶段插入**（三个不同间隙的销钉顺序插入）和**双臂针管插入**（两机械臂分别夹持针头和注射器进行空中组装）。演示数据1000条轨迹。
- **基策略**：Diffusion Policy（状态观测），预测步长分别为16或32。
- **对比方法**：
  - **固定执行步长基线**：在任务中测试多种固定步长（如 \(H=8,16,24\) 等），选最优作为对比。
  - **自身对比**：不同动作噪声水平（\(\sigma = 0 \sim 0.15\)）、不同演示数据规模（300~1000轨迹）。
- **Benchmark**：成功率（Success Rate），逐阶段成功率。

## 4. 资源与算力

- 论文**未明确说明**所使用的GPU型号、数量及具体训练时长。
- 仅提及训练配置：并行环境数1000，训练迭代次数1000~2000，PPO批次大小等。
- 需要指出：算力信息缺失，但根据实验规模（1000个并行环境、多任务训练）推测需要较强的GPU集群（如NVIDIA A100或类似）。

## 5. 实验数量与充分性

- **实验组数**：
  - 多阶段插入任务：5种噪声水平（0, 0.05, 0.07, 0.1, 0.15）× 3个种子，每个种子1000个环境。
  - FurnitureBench：两种任务 × 两种随机化等级（低/中）× 与多个固定步长对比。
  - 双臂针管插入：单一任务，与3种固定步长对比。
  - 消融：演示数据规模（300,500,700,1000）。
  - 定性分析：热力图展示不同阶段的步长分布。
- **充分性评价**：实验较充分，覆盖了不同任务、不同难度、不同噪声、不同数据量，且采用多次随机种子平均，结果客观。但固定步长基线只考虑了部分离散值（如8,16,24等），未穷举所有可能。总体公平性较好。

## 6. 主要结论与发现

- DEHP在所有任务和噪声水平下**一致超越最优固定步长基线**：
  - 多阶段插入任务：整体成功率平均提升23.03%（例如噪声0时从71.50%提至93.17%）。
  - FurnitureBench：one-leg任务从70.30%提至95.18%；round-table低随机化从29.90%提至93.80%。
  - 双臂针管插入：从10.20%提至29.00%。
- **自适应步长模式**：DEHP学会在自由运动阶段使用**长步长**（如14~16），在精细对齐和插入阶段使用**短步长**（如1~3），甚至在最高难度插孔阶段几乎每步重规划（步长接近1）。
- **鲁棒性**：在更大的动作噪声下优势更明显，说明动态步长能弥补基策略的执行误差。
- **与演示规模无关**：即使基策略用更多演示训练（1000条），DEHP仍优于固定步长，证明瓶颈在于固定步长假设本身。

## 7. 优点

1. **方法轻量**：仅添加一个小型分类头，训练时基策略完全冻结，推理仅一次前向，计算开销极低。
2. **兼容黑箱**：适用于任何预训练分块策略（扩散、流、VLA等），无需修改其架构。
3. **自适应可解释**：学到的步长模式与任务阶段直观对应，可理解性强。
4. **性能提升显著**：在多种复杂操作任务中实现大幅提升，特别对精细操作（高噪声）效果突出。
5. **理论保证**：证明分块级折扣回报与原始MDP回报等价，优化目标正确。

## 8. 不足与局限

1. **仅优化执行步长**：基策略权重完全冻结，未与动作生成联合优化。若基策略本身能力有限（如双臂针管任务仅29%成功率），进一步提升受限于基策略。
2. **实验限于状态观测**：论文明确指出实验使用状态空间，未验证图像/视觉输入的迁移，尽管方法本身不限于此。
3. **计算资源未报告**：缺少GPU型号、数量及训练时长等实用信息，不利于复现和对比资源需求。
4. **固定步长基线未穷举**：对比的固定步长仅为离散的几个值，可能遗漏更优的固定步长。
5. **定性分析有限**：仅提供了热力图和文字描述，缺乏定量指标（如不同阶段步长均值、方差）的统计分析。
6. **应用限制**：仅测试模拟环境，未在真实机器人上验证（但论文声明FurnitureBench是真实世界基准的模拟版）。

（完）
