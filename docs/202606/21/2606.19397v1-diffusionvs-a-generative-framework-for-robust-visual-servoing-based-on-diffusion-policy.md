---
title: "DiffusionVS: A Generative Framework for Robust Visual Servoing Based on Diffusion Policy"
title_zh: DiffusionVS：基于扩散策略的鲁棒视觉伺服生成框架
authors: "Hongkang Cui, Rui He, Haoyao Chen"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19397v1"
tags: ["query:vla-robotics"]
score: 9.0
evidence: 提出基于扩散策略的视觉伺服方法，用于机器人操作和导航
tldr: "回归视觉伺服因噪声敏感和分布偏移导致轨迹抖动。基于扩散策略的视觉伺服方法通过预测动作序列保持时间一致性，并利用条件去噪生成相机速度。采用在线训练范式持续扩展数据多样性，显著提升鲁棒性和泛化能力。仿真和实物实验成功率分别接近100%和93%，扩散模块可增强现有视觉伺服系统。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19397v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 870, \"height\": 319, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19397v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19397v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19397v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 893, \"height\": 517, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19397v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 465, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19397v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19397v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 823, \"height\": 194, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19397v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 906, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19397v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 899, \"height\": 129, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19397v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 902, \"height\": 159, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19397v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 904, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19397v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 903, \"height\": 242, \"label\": \"Table\"}]"
motivation: 回归视觉伺服存在噪声敏感的逐帧映射和误差累积，导致轨迹抖动，需要更鲁棒的生成式框架。
method: 基于扩散策略，输入标签角点的归一化图像坐标，通过条件去噪生成相机速度，并采用在线训练不断丰富交互数据。
result: "仿真成功率近100%，实物实验93%，且扩散模块能提升现有视觉伺服网络的性能。"
conclusion: 提出的扩散视觉伺服框架具有强鲁棒性和通用性，可广泛增强各类视觉伺服系统。
---

## 摘要
视觉伺服是机器人操作与导航中的基础技术。基于回归的视觉伺服常因噪声敏感的单步映射及分布偏移时的误差累积而出现轨迹抖动。相比之下，扩散策略通过预测动作序列保持时间一致性，并通过隐式数据增强提高鲁棒性。本文提出一种新颖的基于扩散的伺服方法。该方法基于扩散策略，以观测到的标签角点的归一化图像坐标作为输入，通过条件去噪生成相机速度。为克服在静态数据集上训练的模型的泛化限制，采用了在线训练范式，通过交互式经验收集不断扩展训练数据的多样性。该策略显著提升了模型的性能与泛化能力。全面的仿真与真实世界实验证明了该方法的有效性，在仿真中实现了近100%的成功率，在物理实验中达到93%。除了特定流程外，我们进一步验证了扩散机制的通用性。实验表明，现有视觉伺服网络在与我们基于扩散的模块集成后，性能持续提升。这些结果表明，所提策略具有广泛适用性，能够增强除本文具体架构外的各种视觉伺服系统。

## Abstract
Visual servoing is a fundamental technique in robotic manipulation and navigation. Regression-based visual servoing frequently experiences trajectory jitter as a result of noise-sensitive single-step mappings and the accumulation of errors during distribution shifts. In contrast, Diffusion Policy maintains temporal consistency by predicting action sequences and improves robustness through implicit data augmentation.   This paper presents a novel diffusion-based servoing method. Based on Diffusion Policy, the proposed approach uses normalized image coordinates of observed tag corners as input and generates camera velocity through conditional denoising. To overcome the generalization limitations of models trained on static datasets, an online training paradigm is adopted, continuously expanding the diversity of training data through interactive experience collection. This strategy substantially enhances both the performance and generalization capability of the model. Comprehensive simulations and real-world experiments demonstrate the effectiveness of the proposed method, achieving success rates of nearly 100\% in simulation and 93\% in physical experiments. Beyond the specific pipeline, we further validate the generality of the diffusion mechanism. Experiments show that existing visual servoing networks consistently achieve improved performance when integrated with our diffusion-based module. These results indicate that the proposed strategy possesses broad applicability and can enhance various visual servoing systems beyond the specific architecture presented here.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- 视觉伺服是机器人操作与导航的基础技术，传统方法分为基于位置的（PBVS）和基于图像的（IBVS）。PBVS依赖精确深度和标定，IBVS存在“相机后退”问题、局部极小值和雅可比奇异性。
- 近年基于学习的端到端回归方法（如编码-解码结构）直接将图像特征映射到相机速度，但存在严重缺陷：**噪声敏感的单步映射**导致观测扰动（尤其靠近目标时）产生时序不一致动作，引发轨迹抖动；同时**分布偏移下误差累积**，使系统偏离专家轨迹后难以恢复。
- 扩散策略（Diffusion Policy）通过预测**动作序列**保持时间一致性，在训练中注入噪声作为隐式数据增强，对分布偏移更具鲁棒性。然而，其在视觉伺服中的应用尚未探索，且依赖丰富训练数据，静态数据集泛化不足。
- 本文提出 **DiffusionVS**：一个基于扩散策略的生成式视觉伺服框架，以归一化标签角点坐标为输入，通过条件去噪生成相机速度命令；并设计**在线训练**范式持续收集交互数据，弥合仿真-真实差距。

## 二、论文提出的方法论
- **问题形式化**：给定当前图像 \(I_c\) 和目标图像 \(I_d\)，输出相机速度 \(v_c \in \mathbb{R}^6\)。使用**归一化图像平面坐标**（通过相机内参将像素坐标反投影到 \(Z=1\) 平面）作为特征点，构造误差状态向量 \(e \in \mathbb{R}^8\)（当前与期望归一化坐标的差值）。
- **数据预处理**：
  - 将4个角点的当前和期望归一化坐标拼成16维条件向量 `cond`。
  - 原始6维速度分解为**方向** \(d_t \in \mathbb{R}^6\)（单位向量）和**幅度** \(m_t \in \mathbb{R}^2\)（标量速度大小），再将幅度通过逆 soft-plus 变换 \(l = T^{-1}(m_t)\)（其中 \(T(x)=\ln(1+e^x)\)）映射到负无穷范围，平衡早期高速与后期低速样本的权重，避免网络被大速度主导。
  - 最终动作定义为8维向量 `action = [d_t^T, l^T]^T`。
- **DiffusionVS 框架**：
  - 使用标准 DDPM：前向过程对干净动作序列 \(a_0\)（长度 \(H\)，由PBVS专家策略生成）逐步加噪得到 \(a_k\)；后向过程以当前观测 `cond` 为条件，从高斯噪声 \(a_K\) 迭代去噪恢复 \(a_0\)。
  - 动作序列长度为 \(H\)，仅执行第一个动作，后续作为规划缓冲。
  - 网络结构：简单 MLP 扩散网络，将带噪动作序列与观测条件拼接，经过 Linear + Mish 激活层，融合时间嵌入（Time embedding），再通过多层 ResBlock（残差+线性+Mish），最后投影输出预测噪声 \(\epsilon_\theta\)。
- **在线训练**（算法1）：
  - 并行运行多个仿真环境，每个时间步：网络生成学生速度 \(v_\theta\) 控制相机运动，同时仿真环境计算PBVS专家速度 \(v_{gt}\)（使用真实3D坐标）。将状态-动作对存入共享回放缓冲区 \(D\)。
  - 训练时从缓冲区采样 batch，对干净动作 \(x\) 加噪声（连续时间 \(t \sim U(0,1)\)，线性插值 \(z_t = t x + (1-t)\epsilon\)），优化 MSE 损失 \(\|\pi_\theta(z_t, t) - v_{\text{tar}}\|^2\)（其中 \(v_{\text{tar}} = (x - z_t)/(1-t)\)）。
  - 在线训练的优势：模型能通过交互探索偏离专家分布的轨迹，学习纠错行为，显著提升对真实噪声和分布漂移的鲁棒性。

## 三、实验设计
- **仿真环境**：PyBullet，固定AprilTag标定板，随机生成相机初始和目标位姿。采样方式：位置在水平区域均匀采样，垂直范围限定，俯仰/翻滚加均匀噪声，偏航在指定范围。保证标签角点在图像平面有足够区分度。
- **真实机器人**：AUBO-i5 6-DOF机械臂，眼在手RGB相机。初始和目标位姿在半径为0.6~0.8m的半球工作空间内随机扰动。网络输入为从原始RGB提取的AprilTag角点归一化坐标。
- **评价指标**：
  - TE（平移误差）：最终与目标位置的欧氏距离。
  - RE（旋转误差）：轴角表示下的旋转角度。
  - SR（成功率）：仿真中 TE < 5cm & RE < 3° 视为成功；真实实验中像素误差 < 15 pixels。
- **对比方法**：
  - **回归基线**：与 DiffusionVS 相同网络结构但去掉时间嵌入模块，直接输出速度（非扩散）。
  - **GraphVS**【24】：现有SOTA端到端回归网络，将其解码器替换为扩散MLP得到 GraphDiffVS。
  - **静态训练基线**：使用相同数据量（240万帧）的离线预训练模型，训练不同epoch（500/1000/2000）。
- **实验场景配置**：三组难度（Set A/B/C），参数范围见表II（水平范围、垂直范围、角度扰动逐渐增大）。
- **消融实验**：在线 vs 静态训练；仿真 + 真实。

## 四、资源与算力
- 文中未明确说明使用的 GPU 型号、数量、训练时长。仅提到在线训练收集了240万交互帧，静态数据集同样240万帧；训练过程以“迭代次数”或“epoch”描述，但未给出具体硬件配置。
- 计算效率方面：测试时 Diffusion 模块单步耗时 7.58 ms，编码器+主干共 2.63 ms，总步耗 10.2 ms（约100 Hz）。硬件推测为单块消费级 GPU，但原文未确认。

## 五、实验数量与充分性
- **仿真实验**：每组 100 trials，对比回归/扩散、有无位姿输入、GraphVS/GraphDiffVS 等。共报告多个表格（表I、III、VI），每组重复100次取平均，统计充分。
- **真实实验**：每组 30 trials（表V、VII）。在线训练在实物上成功率达93.3%（30次中成功28次），回归基线0%（全部失败）。
- **消融实验**：对照在线训练与静态训练（相同数据量），仿真测试100 trials，实物30 trials。
- **模块通用性验证**：在三个难度集（Set A/B/C）上分别测试 GraphVS vs GraphDiffVS，每个100 trials。结果一致显示扩散模块提升成功率（Set B 从65%提到96%，Set C 从21%提到52%）。
- **充分性评价**：实验覆盖了多种条件（不同难度、有无位姿、仿真/真实、模块替换），结果统计显著，但**真实实验仅使用单一机械臂和单一AprilTag标定板**，场景多样性有限。未测试其他视觉目标（如自然纹理）或动态环境。

## 六、论文的主要结论与发现
1. 扩散策略在视觉伺服中显著优于回归方法：在相同结构和数据下，扩散模型在仿真中成功率100%（回归仅31%），真实实验中 93.3%（回归0%）。
2. 在线训练是弥合仿真-真实差距的关键：即使数据量相同（240万帧），在线训练模型在真实实验中成功率达76.7%，而静态训练模型全部失败（0%）。静态模型易陷入振荡或发散，因为缺乏对偏离分布轨迹的纠错经验。
3. 扩散模块具有通用性：作为即插即用组件，可有效提升现有视觉伺服网络（如GraphVS）的性能（成功率提升最高30%）。
4. 提出的动作分解（方向+幅度）与幅度变换策略，平衡了不同阶段的学习权重，促进网络收敛。

## 七、优点
- **生成式框架的鲁棒性**：利用扩散模型的多模态建模能力和隐式数据增强，对观测噪声和分布漂移不敏感，轨迹平滑。
- **高效在线训练**：无需人工标注，通过仿真自主探索 + 重放缓冲区，持续提升策略多样性，有效解决 sim-to-real 迁移问题。
- **模块化与通用性**：提出的扩散模块可替换现有回归方法的解码器，不依赖特定骨干网络，易于工程集成。
- **实验设计严谨**：控制了参数量、数据量、训练轮数等变量，并设置多难度场景；消融实验清晰证明了在线训练的必要性。
- **计算效率高**：单步仅10.2 ms（约100 Hz），满足实时伺服要求。

## 八、不足与局限
- **实验场景限制**：仅使用 AprilTag 标定板作为视觉目标，未验证对自然纹理、非平面物体、部分遮挡等情况的泛化能力；真实机器人仅在静态场景测试（目标固定）。
- **资源信息缺失**：未提供 GPU 型号和训练时长，可复现性受影响。
- **泛化范围有限**：训练时限制了相机高度范围（防止标签点过于集中），虽然可通过缩放预测速度来适配不同尺度，但未系统评估跨尺度泛化边界。
- **动作分解的敏感性**：方向/幅度分解可能对低速时的方向噪声敏感；虽然幅度变换缓解了不平衡，但在近目标时仍可能出现微小抖动。
- **未考虑控制频率与动态环境**：实验假设目标静止，未涉及运动目标或外力扰动下的在线适应能力。
- **偏差风险**：对比的回归基线为作者自建（与扩散网络结构尽可能一致），可能未完全对齐最新 SOTA 回归方法（尽管也对比了 GraphVS）；另外，真实实验样本量（30 trials）相对较小。

（完）
