---
title: "Clustering-Embedded Model Predictive Path Integral Control: Avoiding Averaging-Induced Failure and Enabling Efficient Cluster Selection for Dynamic Obstacles"
title_zh: 聚类嵌入模型预测路径积分控制：避免平均诱导失败并实现动态障碍物的高效聚类选择
authors: "Zidong Liu, Kaixin Chang, Xu Chen"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06499v1"
tags: ["query:topic"]
score: 8.0
evidence: MPPI控制用于轨迹优化
tldr: "标准的MPPI控制算法在杂乱环境中因重要性加权平均会混淆可行与不可行轨迹，导致机器人犹豫或碰撞。本文提出聚类嵌入的CE-MPPI框架，通过DBSCAN聚类和几何方向特征从碰撞噪声中分离可行轨迹模式，结合智能选择逻辑在静态场景最小化成本、动态场景主动避开障碍物。在2D仿真和6-DoF UR5e机械臂实验中，该方法实现了48%的时间缩减和12%的路径缩短，有效消除了平均化引发的失败。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06499v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 757, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06499v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2207, \"height\": 896, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06499v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 825, \"height\": 590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06499v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 828, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06499v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1139, \"height\": 453, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06499v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 2161, \"height\": 1706, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06499v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1066, \"height\": 1251, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06499v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06499v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1138, \"height\": 371, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06499v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 758, \"height\": 415, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06499v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1038, \"height\": 229, \"label\": \"Table\"}]"
motivation: 标准MPPI在非凸环境中因平均化更新导致可行与不可行轨迹混淆，引发犹豫或碰撞，需要架构性解决方案。
method: CE-MPPI集成DBSCAN聚类与碰撞参考点几何方向特征，隔离可行轨迹模式，并设计最小成本或反向障碍通量的智能选择逻辑。
result: "在2D JAX仿真和UR5e机械臂测试中，时间到目标减少48%，末端执行器路径缩短12%。"
conclusion: CE-MPPI架构性解决MPPI平均化失败，显著提升动态环境避障性能，适用于复杂非线性系统。
---

## 摘要
随着并行计算硬件的广泛普及，基于采样的运动规划方法（如模型预测路径积分（MPPI）控制）在非光滑任务空间中的复杂非线性系统中变得越来越强大。然而，MPPI中的采样和正向模拟流水线在杂乱环境中存在平均诱导失败问题，其中重要性加权平均会平均不兼容的轨迹，导致当障碍物正前方出现时犹豫甚至碰撞。本文提出聚类嵌入MPPI（CE-MPPI），这是一个框架，从结构上解决了标准MPPI在非凸环境中固有的平均诱导失败问题。CE-MPPI并非简单地减轻干扰，而是通过集成高保真修剪和聚类阶段重新定义了控制律。通过利用基于密度的噪声应用空间聚类（DBSCAN）以及从碰撞衍生参考点提取的新型几何方向特征，系统将可行的轨迹模式与不可行展开的噪声隔离开来。配合智能选择逻辑，该逻辑在静态场景中优化最小成本，同时在动态环境中主动避开障碍物通量。在二维JAX加速仿真中的实验表明，CE-MPPI缓解了障碍物前的犹豫，并避免了在动态场景中与移动障碍物的持续耦合。特别是，在Isaac Gym中利用CUDA并行展开在6自由度UR5e机械臂上的实际测试中，到达目标时间减少了48%，末端执行器路径缩短了12%。

## Abstract
With the widespread availability of parallel computing hardware, sampling-based motion planning methods such as Model Predictive Path Integral (MPPI) control have become increasingly powerful for complex nonlinear systems in non-smooth task spaces. However, the sampling and forward-simulation pipeline in MPPI suffers from averaging-induced failure in cluttered environments, where the importance-weighted update averages incompatible rollouts and leads to hesitation or even collision when an obstacle lies directly ahead. This paper proposes Clustering-Embedded MPPI (CE-MPPI), a framework that architecturally resolves the averaging-induced failures inherent in standard MPPI within non-convex environments. Rather than simply mitigating interference, CE-MPPI redefines the control law by integrating a high-fidelity pruning and clustering stage. By leveraging density-based spatial clustering of applications with noise (DBSCAN) alongside a novel geometric direction feature that is extracted from collision-derived reference points, the system isolates feasible trajectory modes from the noise of infeasible rollouts. This is paired with an intelligent selection logic that optimizes for minimum cost in static scenes while actively steering opposite to obstacle flux in dynamic environments. Experiments in 2-D JAX-accelerated simulations show that CE-MPPI alleviates obstacle-front hesitation and avoids persistent coupling with moving obstacles in dynamic scenes. In particular, real-world tests on a 6-DoF UR5e manipulator with CUDA-parallel rollouts in Isaac Gym achieve a 48\% reduction in time-to-goal and a 12\% shorter end-effector path.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- 标准MPPI控制算法在杂乱、非凸环境中存在“平均诱导失败”问题：重要性加权更新会平均左绕和右绕等不兼容的可行轨迹，导致控制输出指向障碍物，造成机器人犹豫甚至碰撞。
- 现有方法如CSC-MPPI虽尝试通过约束调整和聚类解决该问题，但其聚类特征（平均线速度、角速度）在左右绕行时区分度不足，且在动态障碍物场景中仅依赖瞬时成本选择簇，导致机器人被移动障碍物长时间“阻塞”。
- 背景：采样型MPPI得益于GPU并行计算广泛应用于机器人运动规划，但其加权平均机制在多模态可行解下成为性能瓶颈，迫切需要架构性解决方案。

## 二、论文提出的方法论
- 核心思想：将碰撞轨迹剪枝与DBSCAN聚类嵌入MPPI框架，根据障碍物类型（静态/动态）智能选择单一簇进行模式一致的更新，从根本上避免平均诱导失败。
- 关键技术细节：
  - **剪枝阶段**：对每个采样轨迹计算碰撞标志，移除碰撞轨迹，保留可行轨迹集 \( V_t \)。
  - **几何方向特征提取**：计算碰撞轨迹末端位置的均值作为参考点 \( \bar{p}_t^{\text{col}} \)；对每条可行轨迹，取其末端位置相对于参考点的方向向量并归一化，作为聚类特征 \( z_t^{(k)} \)。
  - **DBSCAN聚类**：对可行轨迹的方向特征进行聚类，得到多个簇 \( \{C_{t,c}\} \)，每个簇对应不同的绕行模式（如左/右），噪声点被忽略。
  - **智能选择逻辑**：
    - 静态障碍物：选择平均轨迹成本最低的簇（argmin \( \bar{S}_{t,c} \)）。
    - 动态障碍物：估计障碍物运动方向 \( \hat{v}_{\text{obs},t} \)（基于最近H步位置线性外推）；计算每个簇的平均运动方向 \( \hat{v}_{t,c} \)；选择与障碍物运动方向点积最小的簇（即最反向的簇），以主动避开障碍物通量。
  - **簇内更新**：仅使用选定簇内的轨迹，重新归一化重要性权重，更新名义控制序列。
- 公式/算法流程（文字说明）：
  1. 并行采样K条扰动控制序列，正向仿真获得轨迹和成本，记录碰撞标志。
  2. 筛选可行轨迹集，若无可碰撞轨迹则回退到标准MPPI。
  3. 计算碰撞轨迹末端均值参考点，提取可行轨迹的几何方向特征。
  4. DBSCAN聚类，得到若干簇。
  5. 根据障碍物类型选择簇：静态选最低平均成本簇，动态选运动方向最反障碍物方向的簇。
  6. 对选定簇内的轨迹重新计算权值，加权更新控制序列，执行第一步控制，滚动时域。

## 三、实验设计
- **仿真场景**：
  - 场景1（静态障碍物）：一个静态障碍物，设计用于触发平均诱导失败；机器人从起点到目标。
  - 场景2（动态障碍物+两个静态障碍物）：一个沿正x方向移动的障碍物，两个固定障碍物；测试动态场景下的持久阻塞缓解。
  - 对比方法：标准MPPI、CSC-MPPI、CE-MPPI。每个场景运行三次取平均。
- **真实世界实验**：
  - 平台：6-DoF UR5e机械臂，桌面场景，黑色盒子作为障碍物，立方体作为目标。
  - 正向仿真加速：Isaac Gym + CUDA并行，控制频率10 Hz。
  - 对比方法：标准MPPI（CSC-MPPI因计算超时无法实时运行，故未对比）；每个方法运行一次（？）报告结果。
- **评价指标**：碰撞率、到达目标时间、路径长度。
- **基准**：未使用公开数据集；自定义2D仿真环境和真实桌面场景。

## 四、资源与算力
- 仿真硬件：Intel Core i7-9750H CPU，NVIDIA GeForce GTX 1650 GPU，32 GB RAM。软件：JAX、JIT编译。
- 真实实验硬件：同上述PC；UR5e机械臂；Isaac Gym用于CUDA加速的前向仿真（50个rollouts，12步时域）。
- 未提及训练时长：MPPI为在线规划器，无需训练；每次规划周期在10 Hz控制率内完成（约100 ms），但具体计算耗时未报告。
- 未提及多GPU或分布式训练等大规模算力使用。

## 五、实验数量与充分性
- 仿真：两个场景，每个方法运行三次取平均，共约6组对比实验（MPPI在场景1、2碰撞，无数据；CSC-MPPI与CE-MPPI各两次有效对比）。实验覆盖了论文提出的两个失败模式（平均诱导失败和动态阻塞），但样本量较少（三次），且未报告方差或置信区间，统计显著性不明。
- 真实实验：仅一个桌面场景，每个方法一次运行（？），未做多次重复，结果可能存在偶然性。
- 缺乏消融实验：未单独验证剪枝、聚类特征、DBSCAN参数、选择逻辑中各组件的贡献。未与更多基线如RRT*、MPC对比。未测试不同DBSCAN参数（ε、minPts）的敏感性。
- 公平性：仿真参数一致（同一个K、N、dt等），但CSC-MPPI在真实场景中因计算超时无法运行，对比不完全。实验设计基本客观，但充分性不足（尤其真实实验只有单次）。

## 六、论文的主要结论与发现
- CE-MPPI在静态场景中消除了平均诱导失败，比CSC-MPPI更快到达目标（4.02 s vs 4.23 s）且路径更短（2.22 m vs 2.27 m）。
- 在动态场景中，CE-MPPI通过反向障碍物运动方向的簇选择，大幅减少被阻塞时间：时间降低17.4%（7.98 s vs 9.66 s），路径缩短9.1%（4.30 m vs 4.73 m）。
- 真实UR5e实验中，CE-MPPI比标准MPPI时间减少48%（93.11 s vs 179.20 s），路径缩短12%（0.73 m vs 0.83 m）。
- 结论：基于几何方向特征的聚类以及障碍物感知簇选择能系统性解决MPPI的平均诱导失败，并提高动态避障效率。

## 七、优点
- **方法创新**：首次提出使用碰撞轨迹末端均值点作为参考点提取几何方向特征，该特征天然分离左右绕行模式，比CSC-MPPI的基于速度的特征更鲁棒。
- **动态避障设计**：引入障碍物运动方向作为簇选择依据，直觉清晰且计算简单，有效避免持久耦合，具有前瞻性。
- **计算效率**：剪枝和聚类仅对可行轨迹（通常<总rollout数）进行，不增加过多开销；在真实10 Hz控制频率下可运行（CSC-MPPI因梯度调整而超时）。
- **实现简洁**：算法流程清晰（Algorithm 1），无需复杂训练或学习，易于集成到现有MPPI框架。

## 八、不足与局限
- **实验覆盖不足**：
  - 仿真仅3次重复，统计可靠性低；未提供方差、成功率等指标。
  - 真实实验仅单次运行，无法排除随机性；未在多个不同场景或不同障碍物配置下验证。
  - 缺乏消融实验：未验证剪枝的必要性、几何方向特征相比其他特征（如位置、速度）的优势、DBSCAN参数敏感性等。
  - 未与多种基线（如RRT*、基于梯度的MPC、信息论MPC）对比，说服力有限。
- **局限性**：
  - DBSCAN的ε和minPts参数需手动调参，可能影响不同场景下的聚类效果。
  - 动态障碍物运动方向估计基于简单线性外推（最近H步平均速度），对变速或非直线运动可能失效。
  - 在动态场景中，反向簇选择虽能避免阻塞，但可能非最优（如本可以从另一侧更快绕过），缺乏理论最优性保证。
  - 未讨论障碍物数量增加、高维环境下的计算瓶颈（聚类复杂度随rollout数量增长）。
  - 未分析碰撞检查模型（Icol）精度对算法的影响（如真实机器人中的误检/漏检）。
- **应用限制**：当前实验仅二维平面和6-DoF机械臂，未在更复杂如无人机、全身移动操作平台上验证；未考虑动态障碍物与机器人运动耦合带来的复杂回避策略需求。

（完）
