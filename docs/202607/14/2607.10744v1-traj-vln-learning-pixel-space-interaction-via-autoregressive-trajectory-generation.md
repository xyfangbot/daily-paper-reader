---
title: "Traj-VLN: Learning Pixel-Space Interaction via Autoregressive Trajectory Generation"
title_zh: Traj-VLN：通过自回归轨迹生成学习像素空间交互
authors: "Changfei Fu, Guangcheng Chen, Wenjun Xu, Hong Zhang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10744v1"
tags: ["query:vtt"]
score: 9.0
evidence: 视觉语言导航与轨迹生成
tldr: 视觉语言导航（VLN）任务要求智能体在连续环境中遵循语言指令导航。现有方法将视觉嵌入映射到语言空间，但视觉语言模型（VLM）缺乏深度感知，难以处理涉及3D空间交互的子任务。本文提出Traj-VLN，通过自回归方式在2D像素空间生成轨迹（一系列像素坐标），使VLM直接在图像层面学习导航交互。实验表明，像素空间轨迹监督显著优于离散动作和像素目标监督，并在有限计算资源下达到当前最优性能，为VLM在VLN中的应用提供了高效新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10744v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1238, \"height\": 980, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2532, \"height\": 872, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1277, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1277, \"height\": 700, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10744v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1236, \"height\": 866, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2557, \"height\": 759, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 2556, \"height\": 534, \"label\": \"Table\"}]"
motivation: VLN任务可分解为3D空间交互子任务，但VLM缺乏深度感知，现有方法难以有效处理这类交互。
method: 通过自回归生成像素坐标轨迹，在2D图像空间直接学习导航交互，避免引入深度或3D几何信息。
result: 像素空间轨迹监督优于离散动作和像素目标，在有限计算资源下达到SOTA水平。
conclusion: 提出像素空间轨迹生成方法，有效提升VLM在连续环境导航中的泛化能力。
---

## 摘要
得益于大规模预训练数据中蕴含的强大先验知识以及新兴的常识推理能力，大语言模型（LLMs）在许多研究领域展现出了前所未有的泛化能力。近年来，通过视觉-语言模型（VLMs）将视觉嵌入投影到语言空间，以实现仿真到现实和跨场景的泛化，已成为连续环境下的视觉-语言导航（VLN-CE）领域的主流范式。VLN要求具身智能体遵循自然语言指令，在未知环境中进行导航。我们强调，一个VLN任务可以分解为一系列子任务，每个子任务对应于一个与环境的3D空间交互过程，这类交互由诸如“走到沙发尽头然后左转”的指令描述。然而，这种涉及沿深度感知方向进入图像的3D空间交互对VLMs来说难以理解，因为它们主要是在RGB图像对话上进行训练的。我们不采用深度或3D几何信息（这些信息在VLMs预训练中很少出现），而是提出一种替代方法：通过自回归轨迹生成，直接微调VLMs在2D像素空间中学习导航交互。给定语言指令和历史观测，我们的模型依次预测一系列像素坐标，在当前观测的底部中心画出一条轨迹。尽管先前的工作已证明像素目标监督优于离散动作的学习，但我们的实验进一步验证了像素空间轨迹的监督显著提升了VLN性能。此外，我们证明，在相对有限的计算资源和训练数据下，我们的旗舰模型达到了最先进的性能水平。

## Abstract
Benefiting from the powerful priors embedded in large-scale pre-training data and the emerging commonsense reasoning ability, large language models (LLMs) have shown unprecedented generalization capabilities in many research fields. Recently, projecting visual embeddings into the language space via vision-language models (VLMs) to achieve sim-toreal and cross-scene generalization has become a prevailing paradigm in the field of Vision-and-Language Navigation in Continuous Environments (VLN-CE). VLN requires an embodied agent to navigate through unseen environments following natural linguistic instructions. We emphasize that a VLN task can be decomposed into a sequence of sub-tasks, each corresponding to a process of 3D spatial interaction with the environments described by instructions such as "walk to the end of the sofa and turn left." However, such spatial interactions involving moving into the image along the direction of depth sensing are puzzling for VLMs as they were predominantly trained on conversations with RGB images. Rather than incorporating depth or 3D geometric information-which VLMs rarely encounter during pretrainingwe propose an alternative approach: fine-tuning VLMs to learn navigation interactions directly in 2D pixel space through autoregressive trajectory generation. Given a linguistic instruction and historical observations, our model sequentially predicts a series of pixel coordinates, drawing a trajectory from the bottom center of the current observation. While prior work has proved that pixel-goal supervision outperforms learning of discrete actions, our experiments further verify that the supervision of pixel-space trajectory significantly enhances VLN performance. Moreover, we demonstrate that our flagship model achieves state-of-the-art level performance with relatively limited computational resources and training data.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉-语言导航（VLN）任务要求智能体在连续环境中遵循自然语言指令导航，指令通常包含“走到沙发尽头然后左转”这类涉及3D空间交互的子任务。然而，现有的视觉-语言模型（VLM）主要在大规模2D RGB图像对话数据上预训练，缺乏对深度和3D几何信息的理解能力，因此难以有效处理这类需要3D空间感知的交互过程。
- **研究动机**：已有方法（如JanusVLN、NaVid-4D）尝试引入深度信息或3D几何先验，但由于VLM在预训练阶段极少接触这些信息，对齐3D与2D表示效果有限。相反，论文提出一种替代思路：直接在VLM熟悉的2D像素空间中进行学习，通过自回归生成像素坐标轨迹来建模导航交互过程，从而绕过3D推理的瓶颈。
- **整体含义**：论文旨在探索VLM在VLN任务中如何利用其预训练先验，通过创新的监督形式（像素空间轨迹）来提升导航性能，同时保持与预训练数据分布的一致，避免灾难性遗忘。

## 二、论文提出的方法论

- **核心思想**：提出Traj-VLN框架，将VLN任务中的3D空间交互过程转化为2D像素空间中的轨迹生成问题。通过微调通用VLM，使其能够自回归地预测一系列像素坐标，在当前观测图像上描绘出一条从近到远的轨迹，从而隐式学习导航的交互过程。
- **关键技术细节**：
  - **任务分解与对话设计**：将VLM对话分为两个阶段。第一阶段，VLM输出转向动作（←、→）或“STOP”；若输出“↓”，则进入第二阶段，模型接收俯视图像，自回归生成像素坐标序列（格式如“[u1,v1] [u2,v2] …]”），表征一条从图像底部中心出发的轨迹。
  - **历史观测处理**：均匀采样8张历史图像，缩放至384×384；当前观测保持640×480，俯视图像也是640×480。
  - **像素坐标到3D轨迹的转换**：利用深度图将生成的像素坐标转换为3D点，结合相机内参和外参，得到在机器人坐标系中的3D轨迹，供运动执行。
  - **自回归轨迹推理**：模型逐个预测像素坐标，前一步输出作为下一步输入，形成一个链式推理过程。这相当于一种针对像素目标预测的链式思考（CoT）机制。
- **数据收集**：从R2R-CE、RxR-CE和ScaleVLN三个数据集提取约107.5K条轨迹，以4步为间隔采样，得到1.75M个样本。每个样本包含指令、历史观测、当前观测和监督信号（像素坐标序列、转向动作或“STOP”）。地面真值轨迹通过将未来机器人位姿投影到当前观测图像上获得，并过滤掉被遮挡或重复的像素点。
- **训练设置**：基于Qwen3-VL-8B，冻结视觉编码器，微调LLM和投影层，学习率2e-5，一个epoch。

## 三、实验设计

- **数据集与基准**：
  - 使用VLN-CE标准基准：R2R-CE Val-Unseen（1,839条轨迹）和RxR-CE Val-Unseen（3,669条轨迹）。
  - 评估指标：导航误差（NE）、成功率（SR）、加权路径长度（SPL）、归一化动态时间规整（nDTW）等。
- **对比方法**：
  - **VLM方法**：NaVid、NaVid-4D、JanusVLN、InternVLA-N1、Goal2Pixel、NaVILA、StreamVLN等（共7种）。
  - **传统非VLM方法**：Sim2Real、AO-Planner、NavMorph（共3种），作为性能对比基线。
- **消融实验与对比**：
  - 对比像素轨迹监督与像素目标监督，控制变量（相同骨干网络Qwen2.5VL-7B、LoRA微调、1.75M训练数据），仅改变监督信号。
  - 探讨每步执行轨迹点数量的影响（从1个到多个点）。
  - 验证Traj-CoT机制：将预测轨迹的最终像素坐标作为像素目标进行评估，与直接像素目标预测对比。

## 四、资源与算力

- **旗舰模型训练**：基于Qwen3-VL-8B，在8块Ascend 910C NPU上训练约91小时，总计728 NPU小时。
- **对比实验训练**：基于Qwen2.5VL-7B，采用LoRA微调（rank=32），在4块Nvidia 4090 GPU（48GB内存）上训练约171小时，共274,266步。
- **训练数据规模**：1.75M样本（来自约107.5K条轨迹），采样间隔为4步。
- **算力节省策略**：冻结视觉编码器，比全微调节省约62.5%的训练时间。

## 五、实验数量与充分性

- **实验数量**：
  - 三组主要实验：旗舰模型与SOTA方法对比、轨迹监督与像素目标监督对比、Traj-CoT验证。
  - 在R2R-CE和RxR-CE两个基准的Val-Unseen分割上评估，覆盖不同难度和语言风格的任务。
  - 消融分析：改变每步执行的轨迹点数量（图4），观察其对SR和OSR的影响。
- **充分性与公平性**：
  - 对比实验严格控制变量（相同骨干、数据、训练配置），仅改变监督信号，结论可靠。
  - 与多种SOTA方法对比，包括使用更多训练数据或全微调视觉编码器的方法，结果具有说服力。
  - 但实验评估未涵盖真实机器人部署场景，仅在模拟器中进行（Habitat平台）。虽然有定性真实世界结果，但缺乏定量性能指标，泛化到真实环境的鲁棒性有待进一步验证。

## 六、论文的主要结论与发现

- 像素空间轨迹监督显著优于像素目标监督：在R2R-CE Val-Unseen上SR提升4%（43.2% vs. 39.6%），在RxR-CE Val-Unseen上SR提升3.29%（41.54% vs. 36.09%）。
- 自回归轨迹生成可作为有效的链式思考（CoT）机制：将轨迹的最终像素坐标作为像素目标评估时，性能优于直接预测像素目标的方法，表明过程级监督有助于更合理的推理。
- 在有限计算资源和训练数据下，旗舰模型Traj-VLN在R2R-CE Val-Unseen上SR达58.4%，在RxR-CE Val-Unseen上SR达52.6%，达到或超越SOTA水平（对比InternVLA-N1：55.4%和49.5%）。
- 轨迹生成方式能够成功处理杂乱环境中的空间交互，而预训练的通用VLM难以做到（图1定性对比）。

## 七、优点

- **创新性**：首次提出在2D像素空间通过自回归轨迹生成来学习VLN的3D空间交互，绕过了VLM在3D感知上的固有缺陷。
- **对齐预训练数据分布**：基于像素坐标文本序列的监督形式与VLM在大量文本和图像对话上的预训练数据分布一致，减少了分布漂移，避免灾难性遗忘。
- **计算效率高**：冻结视觉编码器显著节省训练时间和资源，同时性能出色。
- **数据效率高**：使用相对较少的训练数据（1.75M样本）即达到SOTA水平，说明轨迹监督形式的信息密度更高。
- **可解释性**：轨迹生成相当于为导航决策提供了过程级的推理链条（CoT），相比直接预测像素目标或动作更透明、更可解释。

## 八、不足与局限

- **依赖深度图**：虽然训练时在2D像素空间进行，但执行时仍需深度图将像素坐标转换为3D轨迹，这在实际部署中可能带来传感器噪声和标定误差。
- **场景覆盖有限**：实验仅在Habitat模拟器的室内场景（Matterport3D）上进行，未涉及室外、动态环境或更复杂的交互场景。
- **瓶颈固定**：采用预先设定的“↓”动作作为进入第二阶段的触发条件，可能限制了模型在复杂空间中灵活调整策略的能力。
- **对比实验的骨干差异**：旗舰模型使用Qwen3-VL-8B，而消融实验使用Qwen2.5VL-7B，因此旗舰模型的性能提升部分可能来自更强的骨干网络，而不仅仅是轨迹监督的贡献。结论的通用性需要更多骨干网络验证。
- **推理频率问题**：每执行一个轨迹点就重启对话，虽然性能最优，但增加了推理次数和延迟，实时性有提升空间。
- **鲁棒性验证不足**：对深度图噪声、光照变化、遮挡等真实环境干扰的鲁棒性未进行定量分析。

（完）
