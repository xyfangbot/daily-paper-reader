---
title: "VINE: Taming Generative Control Policies for Reinforcement Learning"
title_zh: "VINE: 驯服强化学习的生成控制策略"
authors: "Rushuai Yang, Zhuo Han, Houlin Li, Hecheng Wang, Zhichao Wu, Rui Zhang, Zhaowei Zhang, Zihong Chen, Xiaohan Yan, Chiming Liu, Yi Chen, Wei Shan, Maoqing Yao"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10369v1"
tags: ["query:rl-robotics"]
score: 9.0
evidence: 用于机器人学习的强化学习与流匹配策略
tldr: 流匹配策略通过迭代生成动作，能建模复杂多模态分布，但结合值梯度强化学习时训练不稳定。现有方法错误归因于迭代生成，因此牺牲迭代生成或值梯度优化。本文提出VINE，一种面向强化学习的采样方法，它重构每个去噪步骤的插值状态，创建稳定的可微路径，使得端到端值梯度优化得以稳定进行。在OGBench离线RL基准和真实机器人操控任务中，VINE持续超越最先进方法。该工作揭示了不稳定的真正根源，为流匹配策略的强化学习训练提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1957, \"height\": 640, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2005, \"height\": 1078, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1994, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1796, \"height\": 898, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1777, \"height\": 1030, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1918, \"height\": 1879, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1919, \"height\": 1878, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2030, \"height\": 590, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1970, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1674, \"height\": 536, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 2078, \"height\": 906, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 2143, \"height\": 2346, \"label\": \"Table\"}]"
motivation: 现有流匹配策略与值梯度RL结合时不稳定，错误归因于迭代生成，实际源于行为克隆采样策略。
method: VINE在每个去噪步骤重建插值状态，形成稳定可微路径，实现端到端值梯度优化。
result: 在OGBench离线RL基准和真实机器人任务中，VINE性能持续超越最先进方法。
conclusion: 揭示了不稳定根源并提出了RL导向采样方法，为流匹配策略的强化学习训练提供了可行方案。
---

## 摘要
流匹配策略已成为机器人学习的有效策略参数化方法。它们迭代地从噪声生成动作，从而能够对复杂多模态动作分布进行高表达力建模。然而，先前的工作发现，将这些策略与值梯度强化学习（RL）结合扩展往往导致训练不稳定。现有方法将这种不稳定性归因于迭代生成，因此通过牺牲迭代生成、高表达力或值梯度优化来避免端到端值梯度优化。与先前的观点相反，我们表明不稳定性并非源于迭代生成本身，而是源于最初为行为克隆设计的朴素采样策略，该策略在值梯度RL下变得脆弱。基于这一见解，我们提出了VINE，一种面向RL的采样方法，它能够为流匹配策略实现稳定的端到端值梯度优化。VINE不是遵循单一的流轨迹，而是在每个去噪步骤中重建一个新的插值状态，为值梯度传播创建一条稳定的可微路径，同时与原始的流匹配去噪过程保持兼容。因此，VINE保留了流匹配的表达力和迭代生成，而不牺牲端到端值梯度优化。尽管通过所有十个去噪步骤执行端到端反向传播，VINE仍实现了稳定的策略改进，并在OGBench离线强化学习基准和真实世界机器人操作任务上持续优于最先进的RL方法。视频可在我们的网站上获取：https://agibottech.github.io/vine。

## Abstract
Flow-matching policies have emerged as an effective policy parameterization for robot learning. They iteratively generate actions from noise, enabling highly expressive modeling of complex and multimodal action distributions. However, prior works observed that scaling these policies with value-gradient reinforcement learning (RL) often leads to training instability. Existing methods attribute this instability to iterative generation and therefore avoid end-to-end value-gradient optimization by sacrificing iterative generation, high expressiveness, or value-gradient optimization. Contrary to prior belief, we show the instability does not stem from iterative generation itself, but from the vanilla sampling strategy originally designed for behavior cloning, which becomes brittle under value-gradient RL. Motivated by this insight, we propose VINE, an RL-oriented sampling method that enables stable end-to-end value-gradient optimization for flow-matching policies. Instead of following a single flow trajectory, VINE reconstructs a new interpolation state at every denoising step, creating a stable differentiable path for value-gradient propagation while remaining compatible with the original flow-matching denoising process. As a result, VINE preserves the expressiveness and iterative generation of flow-matching without sacrificing end-to-end value-gradient optimization. Despite performing end-to-end backpropagation through all ten denoising steps, VINE achieves stable policy improvement and consistently outperforms state-of-the-art RL methods on the OGBench offline RL benchmark and real-world robotic manipulation task. Videos are available on our website: https://agibottech.github.io/vine.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：流匹配（flow-matching）和扩散模型等生成式控制策略在机器人学习行为克隆中表现出色，能够建模复杂多模态动作分布。然而，当尝试通过值梯度强化学习（RL）进一步优化这些策略时，出现了严重的训练不稳定问题。
- **背景**：现有工作将不稳定归因于迭代生成过程，因此采取了三种妥协策略：(1) 冻结生成策略参数，仅使用外部引导信号；(2) 将多步生成蒸馏为单步策略；(3) 放弃批评者（critic）的动作梯度，仅作为权重信号。这些方法虽提升了稳定性，但牺牲了表达力、迭代生成或值梯度优化，导致性能次优。
- **核心发现**：本文通过分析指出，不稳定性并非来自迭代生成本身，而是来自最初为行为克隆设计的朴素采样策略（如欧拉ODE采样器），该策略在值梯度RL下变得脆弱。行为克隆只需重建固定动作分布，而RL需要策略在批评者梯度驱动下平滑地在不同高价值模式间移动，原有采样方式无法支持这种动态模式迁移。

## 二、论文提出的方法论
- **核心思想**：VINE（Value-gradient Iterative Noise Exploration）是一种面向强化学习的采样方法，旨在为端到端值梯度反向传播（BPTT）提供稳定的可微路径，同时保留流匹配的表达力和迭代生成。
- **关键技术细节**：
  - 在每个去噪步骤，VINE不再沿着单一流轨迹前进，而是重建一个插值状态：$\hat{x}_k = t_k \hat{a}_k + (1-t_k) z_k$，其中$\hat{a}_k$是当前动作估计，$z_k$是独立采样的高斯噪声，$t_k$是单调递增的时间点。
  - 然后使用预训练的流匹配速度场$v_\theta$进行端点预测：$\hat{a}_{k+1} = \hat{x}_k + (1-t_k)v_\theta(\hat{x}_k, t_k; s)$。
  - 初始$\hat{a}_0 \sim \mathcal{N}(0,I)$，经过K步递归得到最终动作$a=\hat{a}_K$。
- **与预训练流策略兼容**：理论证明，最优速度场$v^*$使得$\hat{x}_k + (1-t_k)v^*(\hat{x}_k, t_k; s) = \mathbb{E}[a \mid \hat{x}_k, t_k, s]$，即该操作是贝叶斯最优的动作估计，不依赖于$\hat{x}_k$的生成方式，因此VINE可直接用于预训练的流匹配策略，无需修改训练目标。
- **算法流程**（Actor-Critic框架）：
  - 使用VINE的GENERATE（s）函数产行动作，收集或采样数据。
  - 训练批评者$Q_\phi$：最小化贝尔曼误差。
  - 更新速度网络$v_\theta$：通过BPTT最小化$-\mathbb{E}[Q_\phi(s, \hat{a}_K)] + \alpha \|\hat{a}_K - a\|^2$（行为克隆正则项）。
- **优势**：VINE在每一步注入新的局部噪声，实现结构化探索；随着$t_k$增加，噪声权重$(1-t_k)$递减，由粗探索向精调逐步过渡。这种设计避免了欧拉采样器单条轨迹的孤立模式，提供了更广的状态覆盖，有利于价值梯度驱动的模式迁移。

## 三、实验设计
- **离线RL实验**：使用OGBench基准，包含8个领域共40个任务：antmaze-large, antmaze-giant, humanoidmaze-medium, humanoidmaze-large, scene-sparse, puzzle-3x3-sparse, cube-double, cube-triple。
  - **对比方法**：分为四类：
    - 高斯策略：ReBRAC（基线）。
    - Around the Chain（不通过完整采样链更新梯度）：FQL, FAWAC, QAM, DAC, QSM, CGQL变体。
    - Outside the Chain（冻结生成策略，外部优化）：DSRL, FEdit, FBRAC, BAM。
    - Through the Chain（直接BPTT）：本文VINE和其他少数方法。
  - 评估指标：成功率，每个任务12个种子，报告均值和95%置信区间。
- **真实世界在线RL实验**：插头插座插入任务（socket insertion），涉及接触力精确控制。对比方法：SAC-Flow, Hil-SERL, DSRL, RLT, EXPO。评估指标：20次试验的成功率、微调时间、需要人为干预的轨迹比例。
- **消融实验**：
  - 采样方式消融：VINE vs 标准欧拉采样器，在同一流匹配策略下比较成功率和训练梯度稳定性。
  - 迭代步数消融：K=1,4,6,8,10，评估不同生成步数对最终性能的影响。

## 四、资源与算力
- 论文未明确给出使用的GPU型号、数量或训练总时长。
- 仅提及训练离线VINE使用1M梯度步（state-based OGBench任务），每20k步评估50个episode。对于真实机器人实验，报告了微调时间（VINE为20分钟，其他方法为20-50分钟），但未提及具体算力资源配置。

## 五、实验数量与充分性
- **实验数量**：相当充分。
  - 离线RL：8个领域×5个任务=40个任务，每个任务12个种子，共480次独立实验。
  - 真实世界：1个任务，20次试验，5个基线方法对比。
  - 消融实验：采样消融（5个任务）、迭代步数消融（1个任务AntMaze）。
- **充分性**：覆盖了长时导航（antmaze/humanoidmaze）、稀疏奖励操作（scene/puzzle/cube）等挑战性场景；对比方法涵盖了当前主流范式（高斯、扩散、流策略的各种变体）；消融实验验证了核心设计（采样方式、迭代步数）的有效性。实验设计较为客观公平，但真实世界任务仅涉及单个任务，泛化性证据有限。

## 六、论文的主要结论与发现
1. **不稳定性根源**：流匹配策略与值梯度RL结合的不稳定性主要源于采样策略（单噪声轨迹），而非迭代生成本身。
2. **VINE有效性**：提出的VINE采样方法通过在每个去噪步骤重建插值状态、注入新鲜噪声，创建了稳定的可微路径，使端到端BPTT变得稳定。
3. **性能优势**：
   - 在OGBench 40个任务上，VINE以60%的总体平均成功率（95% CI：[55,65]）超越所有对比方法（第二名QAM为55%，其余均低于50%），尤其在antmaze-giant和humanoidmaze-large等长时任务中提升显著。
   - 在真实世界插头插入任务中，VINE达到20/20成功，且训练时间最短（20分钟），人为干预率最低（19.2%），显著优于其他基线（成功率12/20至19/20，干预率29.7%-74.6%）。
4. **迭代生成重要性**：消融实验表明，增加去噪步数（从1到10）能逐步提升动作质量，后期精调步骤对解决长时任务至关重要。

## 七、优点
1. **理论洞察新颖**：指出不稳定源于采样策略，而非迭代生成，为设计RL导向的采样方法提供了坚实动机。
2. **方法简洁高效**：VINE仅通过修改采样器实现，无需改变预训练策略的结构或训练目标，可直接复用现有流匹配模型。
3. **兼容性证明**：从概率角度证明了VINE的端点预测是贝叶斯最优的，保证了与原始流匹配训练语义的兼容。
4. **实验全面性**：同时在离线基准（40任务）和真实机器人（1任务）上验证，消融实验设计合理，对比基线覆盖主流方法。
5. **实际应用价值**：真实世界实验中，VINE不仅成功率最高，还显著降低了训练时间和人类干预，具有实际部署潜力。

## 八、不足与局限
1. **真实世界实验覆盖不足**：仅测试了单个插头插入任务，缺乏多样化的真实操作任务（如抓取、组装、移动等），泛化性有待验证。
2. **未讨论更复杂的策略架构**：论文仅使用MLP作为动作头，未涉及基于Transformer的大型基础模型（如π0等）上的表现。作者在结论中提及“扩展至大型基础模型”是未来方向。
3. **算力资源未详细报告**：未提供GPU型号、数量、训练时长等关键消信息，不利于复现和比较计算成本。
4. **对超参数敏感性分析不足**：除了行为克隆系数α在不同任务上设置不同外（附录表4），未系统分析VINE对步数K、噪声分布等超参数的敏感性。
5. **仅评估Actor-Critic框架**：未尝试将VINE应用于其他RL算法（如基于策略梯度的方法），适用范围有待拓展。
6. **消融实验任务量少**：迭代步数消融仅在一个AntMaze任务上展示，未在更多任务上验证通用性。

（完）
