---
title: "S-squared-VLA: Decoupling Semantic and Spatial Streams in Vision-Language-Action Models for Autonomous Driving"
title_zh: "S-squared-VLA: 自动驾驶视觉-语言-动作模型中语义与空间流的解耦"
authors: "Jianguo Yu, Rukang Wang, Duanfeng Chu, Chen Wang, Renju Feng, Liping Lu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13926v1"
tags: ["query:vla-robotics"]
score: 9.0
evidence: 解耦空间与语义流的VLA自动驾驶模型
tldr: 现有VLM在自动驾驶中难以生成精确控制动作，VLA架构因语义-空间耦合导致空间表征坍缩。提出S-squared-VLA，解耦语义和空间流：语义流层级桥接提取多尺度特征，空间流绕过语言瓶颈保留未压缩空间特征，双流规划适配器融合。在NAVSIM上PDMS达87.1、无碰撞率98.4，超越所有基线。该方法缓解了空间表征坍缩，实现了VLA模型在闭环基准上的新SOTA。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1255, \"height\": 1412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2593, \"height\": 1206, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 1037, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 784, \"height\": 697, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2136, \"height\": 746, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 2252, \"height\": 312, \"label\": \"Table\"}]"
motivation: 标准VLA架构因语义-空间耦合导致空间表征坍缩，限制了精确轨迹规划。
method: 解耦语义与空间流，语义流层级桥接提取VLM特征，空间流直接保留视觉编码器未压缩特征，双流规划适配器融合。
result: 在NAVSIM上PDMS达87.1，无碰撞率98.4，超越SFT设置下所有VLA基线。
conclusion: 通过解耦语义和空间流，有效缓解空间表征坍缩，提升自动驾驶闭环性能。
---

## 摘要
视觉-语言模型（VLM）在自动驾驶的高层推理中展现出巨大潜力，但其在生成精确的低层控制动作方面存在根本性困难。这一局限性源于离散语言标记与连续轨迹规划之间的固有 mismatch 所导致的语义-物理鸿沟。尽管视觉-语言-动作（VLA）架构试图通过将感知与控制统一为单一策略来弥合这一鸿沟，但这种纠缠却带来了新的瓶颈。标准 VLA 会经历严重的空间表示坍塌，从而不可逆地降低安全、边界感知导航所必需的细粒度空间与几何先验。为解决这一问题，我们提出了 S-squared-VLA，该模型明确解耦了视觉-语言-动作模型中的语义流与空间流。语义流利用分层桥接从 VLM 中提取多尺度特征，以实现鲁棒的意图推理；同时，独立的空间流绕过自回归语言瓶颈，直接保留来自视觉编码器的未压缩空间特征。通过整合辅助感知监督，该流显式地赋予模型丰富的空间与几何先验。最后，双流规划适配器通过级联注意力机制将高层语义意图与精确空间约束进行融合。在 NAVSIM 闭环基准上的评估显示，S-squared-VLA 的预测驾驶员模型得分（PDMS）达到 87.1，在纯监督微调（SFT）设置下创下了 VLA 模型的新纪录。通过缓解传统 VLM 的空间表示坍塌，我们的框架显著优于基线，并在所有评估方法中取得了最高的无碰撞率（NC）——98.4。

## Abstract
Vision-Language Models (VLMs) have demonstrated remarkable potential for high-level reasoning in autonomous driving, yet they fundamentally struggle to generate precise, low-level control actions. This limitation is rooted in a semantic-physical gap caused by the inherent mismatch between discrete language tokens and continuous trajectory planning. While Vision-Language-Action (VLA) architectures attempt to bridge this gap by unifying perception and control into a single policy, this entanglement creates a new bottleneck. Standard VLAs experience a severe spatial representation collapse, which irreversibly degrades the fine-grained spatial and geometric priors essential for safe, boundary-aware navigation. To address this limitation, we propose the S-squared-VLA, which explicitly decouples the semantic and spatial streams in Vision-Language-Action models. The semantic stream leverages hierarchical bridging to extract multi-scale VLM features for robust intent reasoning. In parallel, an independent spatial stream bypasses the autoregressive language bottleneck, directly preserving uncompressed spatial features from the visual encoder. By integrating auxiliary perception supervision, this stream explicitly equips the model with rich spatial and geometric priors. Finally, a dual-stream planning adapter fuses high-level semantic intent with precise spatial constraints via cascaded attention mechanisms. Evaluations on the NAVSIM closed-loop benchmark show that S-squared-VLA achieves a Predictive Driver Model Score (PDMS) of 87.1, establishing a new state-of-the-art for VLA models under a purely supervised fine-tuning (SFT) setting. By mitigating the spatial representation collapse of traditional VLMs, our framework significantly outperforms baselines, achieving the highest No Collision (NC) rate of 98.4 among all evaluated methods.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：视觉‑语言模型（VLM）在自动驾驶的高层推理（如意图理解）中表现优异，但其输出本质上是离散的语言 token（如“左转”“减速”），无法直接生成连续的低层控制信号（轨迹），存在“语义‑物理鸿沟”。  
- **现有VLA的瓶颈**：为弥合这一鸿沟，视觉‑语言‑动作（VLA）模型将感知、推理与控制统一为单一策略。然而，标准VLA采用单流设计，直接将高层语义表示映射到低层控制，导致**语义与空间的纠缠**，并引发“空间表示坍缩”——深层网络压缩不可逆地丢失了细粒度的空间与几何先验，使模型缺乏精确的边界感知能力，产生不安全或几何不一致的轨迹。  
- **核心问题**：如何在不牺牲语义推理能力的前提下，保留并利用好视觉编码器中早期的未压缩空间特征，实现安全、边界感知的轨迹规划。

## 二、论文提出的方法论
- **核心思想**：明确解耦语义流与空间流，让语义流专注高层意图推理，空间流直接保留未压缩的空间几何信息，再通过双流规划适配器融合。  
- **关键技术细节**：  
  1. **多尺度语义流**：  
     - 采用 InternVL3‑2B（InternViT + Qwen2.5）作为视觉‑语言骨干。  
     - 注入 64 个可学习动作查询（`Q_act`），引导网络聚集与执行相关的特征。  
     - 从选定的稀疏层集 \(L = \{3,8,13,18,23,24\}\) 提取多尺度隐藏状态（语义特征 + 动作中心特征），兼顾效率与表示丰富性。  
     - 将车辆历史状态通过 MLP 编码为状态嵌入，与动作特征拼接形成统一状态记忆。  
  2. **任务驱动的空间流**：  
     - 输入前视图，采用动态分辨率策略切分为 9 块，每块引入 64 个可学习视觉查询，与类别 token 和 patch token 一起进入 ViT。  
     - 编码后的视觉查询重组为空间密集特征，加入块级位置编码并通过 Transformer 编码器。  
     - **辅助感知分支**：  
       - **地图头**：将 1D 序列重塑为 2D 网格，经上采样和卷积生成局部 BEV 语义地图（X: [0,32]m, Y: [−32,32]m），使用加权交叉熵损失 \(L_{map}\)。  
       - **Agent头**：采用 DETR 范式，30 个可学习查询经 Transformer 解码器和 MLP 输出障碍物边界框和类别，使用 L1 回归和 BCE 分类损失 \(L_{agent}\)。  
     - 通过这种多任务监督，空间流被严格正则化以编码度量布局和空间边界。  
  3. **双流规划适配器**：  
     - 初始化 8 个可学习规划 token（对应未来 8 个轨迹点）。  
     - **第一阶段（语义与状态对齐）**：规划 token 与多尺度语义特征和状态记忆进行并行交叉注意力和自注意力，经门控融合得到中间表示。  
     - **第二阶段（视觉空间细化）**：中间表示与空间流输出的密集几何特征进行交叉注意力，注入显式空间约束。  
     - 经过 N 个级联块后，用轻量 MLP 解码出轨迹。  
     - 损失函数：L1 轨迹回归 + 平滑性约束（惩罚加速度和 jerk），总损失为 \(\mathcal{L}_{total} = \lambda_{plan}\mathcal{L}_{plan} + \lambda_{agent}\mathcal{L}_{agent} + \lambda_{map}\mathcal{L}_{map}\)。

## 三、实验设计
- **数据集与基准**：使用 **NAVSIM 闭环基准**，强调智能体与动态环境的交互。评估指标为 **PDMS（Predictive Driver Model Score）**，由五个子指标组成：  
  - No Collision（NC，无碰撞）  
  - Drivable Area Compliance（DAC，可行驶区域合规）  
  - Ego Progress（EP，自车进展）  
  - Time to Collision（TTC，碰撞时间）  
  - Comfort（Comf，舒适性）  
- **对比方法**：  
  - **E2E 模型**：TransFuser、UniAD、PARA‑Drive、DRAMA、ARTEMIS、DiffusionDrive（部分使用 LiDAR）。  
  - **VLM/VLA 模型**：InternVL3‑2B、ImagiDrive、AutoVLA、ReCogDrive。  
  - 所有对比均采用纯监督微调（SFT）设置，排除 RL 后训练以保证公平。
- **S²‑VLA 配置**：仅使用单前视摄像头，无 LiDAR。

## 四、资源与算力
- 文中明确说明：使用 **4 块 NVIDIA A100 GPU**，优化器为 **AdamW**。  
- 训练过程分三阶段：  
  1. 第一阶段：在 ReCogDrive VQA 数据集上对预训练 VLM 进行 SFT 3 个 epoch。  
  2. 第二阶段：冻结 VLM 骨干，使用 LoRA 训练意图提取和辅助感知模块 4 个 epoch。  
  3. 第三阶段：冻结感知和意图组件，仅训练视觉细化模块 4 个 epoch。  
- **未明确说明**：具体的总训练时长（小时数）和单卡批大小（仅给出全局 batch size 16）。

## 五、实验数量与充分性
- **定量实验**：核心结果表（Table II）列出了 12 种方法在 PDMS 及 5 个子指标上的完整对比，包含 E2E、VLM/VLA 两个组别。  
- **消融实验**：Table III 从 BaseVLM 开始逐步引入语义特征、空间流、辅助感知，构成 4 组消融（共 5 行，包括最终完整模型），清晰展示了各组件的贡献增量。  
- **定性分析**：图 4 给出了与 TransFuser、InternVL3‑2B、ReCogDrive 的轨迹可视化对比，展示了 S²‑VLA 在高曲率弯道、路口等场景下的贴合度优势。  
- **充分性评价**：  
  - **客观公平**：所有基线均排除 RL 后训练，使用纯 SFT 设置；对比的方法覆盖了近期主流 VLM/VLA 和 E2E 模型。  
  - **略显不足**：消融实验仅在一个基准（NAVSIM）上进行，未跨数据集验证泛化性；也未报告推理速度或参数量等实用性指标。

## 六、论文的主要结论与发现
- S²‑VLA 在 **NAVSIM 闭环基准**上取得了 **PDMS 87.1**，在纯 SFT 设置下创下 VLA 模型新纪录，且 **NC（无碰撞率）达 98.4**，为所有评估方法中最高。  
- 相比使用 LiDAR 的 ARTEMIS（87.0）和 DRAMA（86.9），S²‑VLA 仅用单目相机即达到或超越其性能，证明了解耦空间流对单目深度模糊的有效缓解。  
- 消融实验证实：语义流带来 +1.5 PDMS，空间流进一步 +0.6，辅助感知带来 +0.9，各组件协同作用显著。  
- 定性结果说明：传统 VLM 因空间离散 token 导致轨迹偏离，而 S²‑VLA 能严格贴合道路边界，验证了“保留未压缩空间特征”的核心理念。

## 七、优点
- **架构创新**：首次在 VLA 中明确提出并实现语义流与空间流的显式解耦，避免了单流设计的空间坍缩问题。  
- **双流规划适配器设计精巧**：先用语义+状态对齐粗粒意图，再用空间特征强制几何约束，符合“先确定意图、再验证可行性”的自然规划顺序。  
- **辅助感知任务强正则**：BEV 语义地图和障碍物检测为空间流提供了显式的几何与语义先验，显著提升了边界感知能力。  
- **性能卓越**：在纯 SFT 条件下超过所有 VLM/VLA 基线，甚至超越部分融合 LiDAR 的 E2E 模型，验证了方法的有效性。  
- **实验设计公平**：严格控制训练范式（SFT only），排除 RL 干扰，对比充分。

## 八、不足与局限
- **计算开销**：双流结构（特别是密集空间特征提取和级联注意力）带来非平凡的计算成本，文中提到未来需探索更稀疏的特征机制。  
- **训练范式局限**：当前仅在离线 SFT 下训练，未与闭环强化学习结合（作者也将其列为未来工作）。  
- **单一基准依赖**：所有实验仅在 NAVSIM 上进行，未在 nuScenes、CARLA 等其他流行 benchmark 上验证，泛化性有待进一步检验。  
- **细节缺失**：文中未提供模型参数量、推理延迟等工程指标，影响实际部署可行性评估。  
- **消融覆盖有限**：缺少对动作查询数量、稀疏采样层数、门控机制变体等超参数的敏感度分析。

（完）
