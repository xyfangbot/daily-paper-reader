---
title: Depth-Regularized JEPA World Models Learn More Transferable Representations from Real Outdoor Robot Data
title_zh: 深度正则化JEPA世界模型从真实户外机器人数据中学习更可迁移的表征
authors: Usman M. Khan
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16314v1"
tags: ["query:world-model"]
score: 9.0
evidence: 基于深度正则化的JEPA世界模型，用于真实户外机器人数据
tldr: "针对真实户外环境中视觉复杂导致世界模型学习困难的问题，本文在JEPA架构中引入深度作为几何先验，结合各向同性潜在正则化器SIGReg并增加训练时过参数化。在真实农业机器人视频上训练18M参数模型，冻结表征的视觉里程计误差降低33%，域内外异常检测分数分离度显著提升，多步rollout保真度随步长增长优势更明显。结果表明，轻量级几何先验能增强紧凑JEPA世界模型的泛化性与可迁移性，且不增加推理开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2254, \"height\": 1585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2373, \"height\": 943, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 2370, \"height\": 1949, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2375, \"height\": 986, \"label\": \"Table\"}]"
motivation: 现有JEPA世界模型难以处理真实户外环境的视觉复杂性，需更鲁棒的潜在动力学表示。
method: 在JEPA训练中融入深度监督与各向同性潜在正则化器SIGReg，并添加训练时过参数化。
result: "冻结表征的视觉里程计误差降低33%，域内外异常检测分数分离度提升，多步rollout保真度在域偏移下持续改善。"
conclusion: 轻量级训练时深度几何先验使紧凑JEPA世界模型在真实户外数据上更实用、更可迁移，且无需推理开销。
---

## 摘要
世界模型，尤其是基于JEPA架构的模型，已被证明能够学习多种环境的鲁棒动力学。然而，从视觉复杂的真实世界数据中学习仍然是一个挑战，特别是在不可预测的户外环境中。我们引入深度作为训练期间的几何先验，以直接从机器人视频数据中学习更鲁棒的潜在动力学，并处理视觉复杂性。这将深度监督与各向同性诱导的潜在正则化器（SIGReg）相结合，最大化任务无关的潜在多样性，同时约束该多样性的组织方式，综合目标旨在获得与场景几何一致的最高熵表征。为了在不增加推理时间的情况下满足这种更高的复杂性，我们还添加了仅训练过程中的过参数化。在一个真实农业机器人的视频上训练了一个1800万参数的模型，我们通过冻结表征的视觉里程计探针、基于预测器的意外检测以及多步潜在 rollout 保真度进行评估。与基线LeWM相比，我们的方法将视觉里程计探针误差降低了33%，显著提高了领域内和领域外TartanGround基准上的意外得分分离度，并改进了领域偏移下的多步 rollout 保真度，且增益随着 rollout 视野的增加而增加。值得注意的是，我们在与3D几何不直接相关的物理理解（如光照和阴影）上也观察到了意外得分分离度的提升。这些结果表明，轻量级的训练时间几何先验使得紧凑的JEPA世界模型在真实户外数据上更具实用性、更易迁移，且拥有强大的底层表征，同时不增加推理开销。我们的工作表明，深度作为物理基础的先验可以增强世界模型在各种任务上的泛化能力。

## Abstract
World models, especially based on JEPA architectures, have been shown to learn robust dynamics of various environments. However, learning from visually complex real-world data remains a challenge, especially in unpredictable outdoor environments. We introduce depth as a geometric prior during training in learning more robust latent dynamics directly from robot video data and handling visual complexity. This combines depth supervision with an isotropy-inducing latent regularizer (SIGReg), maximizing task-agnostic latent diversity while constraining how that diversity is organized, with the combined objective targeting the highest-entropy representation consistent with scene geometry. To satisfy this greater complexity without increasing inference time, we also add training-only overparameterization. Training an 18M-parameter model on video from a real agricultural robot, we evaluate with frozen-representation visual odometry probes, predictor-based surprise detection, and multi-step latent rollout fidelity. Compared to the baseline LeWM, our method lowers visual odometry probe error by 33%, substantially increases surprise-score separation both in-domain and on the out-of-domain TartanGround benchmark, and improves multi-step rollout fidelity under domain shift, with gains that grow with rollout horizon. Notably, we also see improvements in surprise-score separation on physics understanding that is not directly tied to 3D geometry, such as lighting and shadows. These results show that a lightweight training-time geometric prior makes a compact JEPA world model more useful and more transferable on real outdoor data with strong underlying representations, without adding inference overhead. Our work suggests that depth as a physically grounded prior can enhance world model generalization on a variety of tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有JEPA世界模型在简单模拟环境中表现出色，但在视觉复杂的真实户外环境（如农业机器人场景）中难以学习鲁棒的潜在动力学。直接像素预测容易建模无关的视觉变化（光照、阴影、纹理等），而JEPA风格的潜在预测虽能规避部分问题，但面临表征坍缩和泛化性不足的挑战。
- **研究动机**：希望在不显著增加推理开销的前提下，通过训练时的几何先验（深度信息）增强JEPA世界模型对真实户外数据的表征质量、物理理解能力和域外泛化能力。
- **整体含义**：本文证明，在紧凑的端到端JEPA世界模型（LeWM）上，引入仅训练阶段的深度对齐损失和过参数化，能使模型学到更符合场景几何的、更具可迁移性的潜在表征，且推理时无需深度输入或额外计算。

## 二、论文提出的方法论

- **基础架构**：基于LeWorldModel（LeWM）——一种紧凑JEPA世界模型，使用ViT-Tiny编码器、6层预测器，嵌入维度192，历史帧数3，预测未来1步。训练时使用SIGReg正则化器（鼓励各向同性高斯分布）防止表征坍缩。
- **核心创新——深度正则化**：
  - 在训练过程中，将RGB帧和配对的深度图（灰度图复制为3通道）通过相同的编码器-投影器网络处理，深度分支使用stop-gradient，RGB分支通过余弦相似度损失对齐到深度嵌入：
    \[
    L_{\text{depth}} = 1 - \cos(f_\theta(x_{\text{RGB}}), \text{sg}[f_\theta(x_{\text{depth}})])
    \]
  - 总损失 = \( L_{\text{pred}} + 0.09 L_{\text{sigreg}} + 0.1 L_{\text{depth}} \)。SIGReg最大化潜在多样性，深度项约束多样性的组织方向，两者结合寻求与场景几何一致的最高熵表征。
- **训练时过参数化**：
  - 在预测器的每个MLP上投影和下投影层添加rank-16的低秩RepLinear分支，增加模型容量以匹配更复杂的优化目标，训练后这些分支代数合并到基权重中，推理时架构与基线一致，无额外开销。
- **推理流程**：仅使用RGB历史+动作路径，深度分支和目标帧分支被丢弃，预测器权重已合并。

## 三、实验设计

- **数据集**：
  - **训练/验证集**：Aigen Element平台收集的真实农业机器人数据，约31K帧（85个episode）用于训练，3,587帧（9个episode）用于验证。深度图截断至1米。
  - **域外（OOD）基准**：TartanGround[14]——地面机器人基准数据集，选取15,608帧（6个episode），涵盖多种环境，同时改变机器人平台、环境和模拟-真实差距。
- **评估任务与指标**：
  - **视觉里程计（VO）线性探针**：用冻结的潜在表征回归自我运动，度量MSE。
  - **基于预测器的意外检测**：构造含有真实/合成扰动（瞬移、时序反转、亮度突变、颜色交换）的短片段，度量模型预测误差（意外得分）的分离度（perturbed - plausible），以及分类准确率。
  - **多步潜在rollout保真度**：给定真实动作序列，自回归展开预测潜在表征，计算目标帧相似度和轨迹相似度（余弦相似度），在h=10和h=20下评估。
- **对比方法**：
  - 基线：标准LeWM（仅SIGReg，无深度正则化）。
  - 冻结的DINOv2编码器作为非LeWM参考（仅用于VO探针和部分rollout）。
  - 两个模型变体：vanilla LeWM vs. 深度正则化LeWM（本文方法）。

## 四、资源与算力

- 论文未明确说明使用的GPU型号、数量、训练时长等具体算力信息。
- 仅提及模型为18M参数（1800万参数）的紧凑模型，训练了100个epoch，优化器和批次配置与基线相同。
- 由于是单平台、小数据集（~31K frames）训练，算力需求相对较低，但具体细节缺失。

## 五、实验数量与充分性

- **实验数量**：主要实验包括：
  - VO线性探针（Aigen-val上的MSE）。
  - 意外检测：两个模型在Aigen-val和TartanGround上的准确率、分离度（按扰动类型细分：瞬移、反转、亮度、颜色）。
  - 多步rollout保真度：在Aigen-val和TartanGround上分别测试h=10和h=20的goal/traj相似度。
  - 跨域潜在几何分析：PCA子空间重叠、主角度、2D可视化、kNN/线性分类器分离度。
  - 未单独进行过参数化或深度正则化的消融实验（作者注明留作未来工作）。
- **充分性评价**：
  - **正面**：覆盖了表征质量、物理理解、域外泛化三个核心维度，包含多类型扰动和不同步长的rollout评估，对比了基线LeWM和DINOv2参考。
  - **不足**：
    1. 缺少对过参数化单独贡献的消融（文中承认）。
    2. 仅有一个对比基线（vanilla LeWM），未与更大规模的JEPA模型（如V-JEPA）、其他几何先验方法（如使用自监督深度）对比。
    3. 数据集规模较小（~31K帧），单平台，结论普遍性需更多数据验证。
    4. 实验均为离线指标，无闭环机器人规划或控制实验。

## 六、论文的主要结论与发现

- **VO探针**：深度正则化模型MSE从0.0022降至0.0015（降低33%），优于vanilla LeWM和DINOv2（0.0029），证明深度正则化提升了几何相关的运动表征质量。
- **意外检测**：
  - 域内准确率均为78.7%，但分离度从0.067提升至0.120。
  - 域外（TartanGround）准确率从87.5%升至89.2%，分离度从0.035升至0.099（近3倍提升）。
  - 最大增益出现在瞬移扰动（几何相关），但亮度、颜色等非几何扰动也有显著提升（如亮度分离度从0.114→0.247，域外0.032→0.124），说明深度正则化帮助形成更一致的物理预期。
- **多步rollout**：
  - 域内：深度模型在h=10时goal相似度略低于基线（0.890 vs 0.903），但traj相似度接近。
  - 域外：深度模型在h=10时更好（0.745 vs 0.716），h=20时优势扩大（0.537 vs 0.458），增益随步长增长，表明深度正则化提高域外泛化能力。
- **潜在几何分析**：
  - 两个域在全空间中均可完美分离，但深度正则化模型降低了域间分离度（centroid距离从9.96σ降至8.96σ，silhouette从0.170降至0.137），且中秩子空间（k≈5-50）的PCA子空间重叠更高，说明潜在几何更具跨域对齐性。
- **主要结论**：训练时的轻量级深度几何先验能提升紧凑JEPA世界模型的表征可迁移性、物理理解能力和域外泛化，且推理零开销。

## 七、优点

- **方法简洁有效**：仅增加一处深度对齐损失和可合并的过参数化分支，训练后无推理成本，工程友好。
- **理论动机清晰**：将SIGReg（最大化熵）与深度（几何约束）结合，视为“与场景几何一致的最高熵表征”，逻辑自洽。
- **评估全面**：从表征质量（VO探针）、预测器锐度（意外分离度）、动力学保真度（多步rollout）、潜在几何分析（PCA/分离度）多个角度验证，结果一致性强。
- **意外发现**：深度正则化不仅改善几何任务，还提升非几何物理理解（如光照突变），提示几何先验能结构化整个潜在空间。
- **域外泛化增益随步长增长**：说明深度正则化使动力学模型更鲁棒，避免复合误差快速放大。

## 八、不足与局限

- **实验规模有限**：仅使用单机器人平台、约31K帧小数据集，未验证在大规模数据或多种机器人上的有效性。
- **缺少消融/对比**：
  - 未单独消融过参数化贡献。
  - 未与V-JEPA、DINO-WM等更大或更先进的JEPA基线对比。
  - 缺少与非几何辅助任务（如光度一致性）的对比以区分深度特异效果。
- **评估离线**：所有结果均为离线潜在指标，无闭环机器人控制或规划实验，实际部署效果未知。
- **深度数据获取假设**：训练时需要配准的深度图，这可能在部分真实场景中难以获取（虽然可在自车采集时同步得到）。
- **深度截断至1米**：仅适用于近距离场景，对远距离几何可能缺乏监督。
- **规划评估不充分**：初步基于潜在相似性的规划实验因为各模型潜空间动态范围不同而结论不明确，未成有效评估。

（完）
