Title: Optimal Safety Control using High-Order Control Barrier Functions

URL Source: https://arxiv.org/pdf/2607.17032v1

Published Time: Tue, 21 Jul 2026 00:53:23 GMT

Number of Pages: 8

Markdown Content:
# Optimal Safety Control using High-Order Control Barrier Functions 

Neng Li, Zuodong Pan, Jiaxing Wang, Weiguo Xia, and Wei Ren 

Abstract —This paper investigates the optimal safety control problem of nonlinear control systems by proposing novel high-order control barrier functions (HOCBFs). Different from ze-roing HOCBFs, two novel HOCBFs are derived and the safety controllers are designed in an explicit way. Next, we implement vector Lyapunov function approach to propose a novel high-order control Lyapunov function (HOCLF) for the stabilization control problem. The relations between the proposed and existing HOCBFs are discussed. Afterwards, the compatibility of the proposed HOCLF and HOCBF is addressed to guarantee the stabilization and safety control objectives simultaneously, and thus the optimal controller is established. Finally, a numerical example from the navigation problem of quadrotors is presented to illustrate the efficacy of the derived results. 

Index Terms —High-order control barrier function, high-order control Lyapunov function, optimal safety control. 

I. I NTRODUCTION 

To describe and guarantee different system performances, e.g., stability, safety and robustness, numerous certificate func-tions have been proposed and extensively applied in the past few decades [1]. Among these certificate functions, the most well-known may be Lyapunov functions, whose strict decrease along the state trajectory is able to show the convergence of the system state to the equilibrium [2]. Hence, Lyapunov functions have been applied extensively [3] and further extended as control Lyapunov functions (CLFs) for the control design [4]. Lyapunov functions enable us to avoid to compute the exact solutions of dynamical systems [3], and thus facilitate both stability analysis and controller design. In a similar way, other certificate functions are constructed, e.g., control barrier functions (CBFs) for system safety [5], [6], artificial potential functions for safe convergence [7], navigation functions for motion coordination [8], and contract metrics for trajectory tracking [1]. These certificate functions are useful both the-oretically and practically such that nonlinear and complex systems can be addressed and the computational complexity can be reduced. For instance, with CBFs, the computation of the reachable sets of dynamical systems is avoided and safety controllers are derived explicitly [9], [10]. With increasing integrated demands, different system perfor-mances are required to be satisfied simultaneously, and thus  

> This work was supported by the National Natural Science Foundation of China under Grants 62573085, the Fundamental Research Funds for the Central Universities under Grant DUT22RT(3)090, and the LiaoNing Revitalization Talents Program (XLYC2403048). N. Li, Z. Pan J. Wang, W. Xia and W. Ren are with the Key Labo-ratory of Intelligent Control and Optimization for Industrial Equipment of Ministry of Education, Dalian University of Technology, Dalian 116024, China. Email:

{744845870, zuodongpan, wanna}@mail.dlut.edu.cn, {wgxiaseu, wei.ren}@dlut.edu.cn) .

different certificate functions are combined either implicitly or explicitly. A fundamental integration is for the stabilization and safety, both of which are the basic performances of dynam-ical systems. Hence, both CLFs and CBFs have been combined effectively in numerous applications, including automotive control problems [5], [11]. In particular, both zeroing and reciprocal CBFs are proposed [5] such that the combinations can be done from different perspectives. However, many existing results are devoted to the classic first-order cases, while are not available for the so-called high-order cases. In terms of practical applications, dynamical systems are usually governed by physical laws, including Newton’s law, Theorem of linear and angular momentum, and Kirchhoff’s laws of current and voltage [12]. Hence, the real-world system models are of second and higher orders. For instance, the dynamics of quadrotors are based on the Euler-Lagrange equa-tion [13] and are of second order. For the high-order cases, the existing CLFs/CBFs are not available, since the Lie derivatives of CLFs/CBFs related to control inputs are required to be non-zero. These requirements are to guarantee the existence of the controllers and are violated in the high-order cases [14]. For instance, the control force of quadrotors is imposed on the angular velocity, while the desired performances are on the position space. In this way, if the CLF is defined on the position space, then the Lie derivative is zero such that the controller cannot be designed. As a result, numerous attention has been paid to the high-order cases [15]–[18], and aims to propose a systematic framework which is highly relevant for real-world applications. However, the existing results are based on zeroing CBFs, which are from the function describing the safe set. Note that not all the description functions can be taken directly as CBFs, and for the high-order cases the constructed safe set is much smaller than the original one, which inevitably results in the conservatism. Inspired by the above discussions, in this paper we propose novel HOCBFs to address the optimal safety control problem of nonlinear control systems. Different from zeroing HOCBFs [15], [16], we first propose a reciprocal HOCBFs, which offers more flexibility in terms of the construction and applications, and design an explicit safety controller. Next, based on vector CLFs [19], a novel high-order CLF (HOCLF) is developed to address the stabilization control problem. Following the similar mechanism, a novel HOCBF is proposed such that the safe set does not need to be reconstructed, thereby relaxing the conservatism from zeroing HOCBFs. Note that with the proposed HOCBF and HOCLF, both the stabilizing and safety controllers can be derived explicitly, and the relations between arXiv:2607.17032v1 [eess.SY] 19 Jul 2026 the proposed and existing functions are discussed. Finally, the compatibility of the proposed HOCBF and HOCLF is investigated to satisfy the stabilization and safety objectives simultaneously. In particular, an HOCBF-stabilizable set is proposed to mediate the conflict between the two objectives, which extends the result in [20] to the high-order cases, and further the optimal safety controller is established, which is illustrated via a numerical example. The remainder of this paper is organized as follows. Prelimi-naries are presented in Section II. High-order control functions are proposed in Section III. The optimal controller is designed in Section IV. Numerical results are presented in Section V, followed by the conclusion and future works in Section VI. II. P ROBLEM FORMULATION 

Let R := ( −∞ , +∞), R+ := [0 , +∞) and N := {1, 2, . . . }.For x ∈ Rn, xi is the i-th element of x; |x| is the Euclidean norm of x. For x, y ∈ Rn, (x, y ) := ( x⊤, y ⊤)⊤, and x ⪰ y

(or x ⪯ y) if xi ≥ yi (or xi ≤ yi) for all i = 1 , . . . , n . A matrix A ∈ Rn×n is Hurwitz if all its eigenvalues lie in the open left half of the complex plane. Given a set S ⊂ Rn, ∂S is the boundary of S; Int (S) is the interior of S; ˆS is the closure of S. Given δ > 0 and x ∈ Rn, the open ball centered at x

with radius δ is defined as B(x, δ ) := {x ∈ Rn : |x − x| < δ };

B(δ) := B(0 , δ ). A continuous function α : R+ → R+ is of class K, if it is strictly increasing and α(0) = 0 ; it is of class 

K∞, if it is of class K and unbounded. A continuous function 

α : R → R is of extended class Ke, if it is strictly increasing and α(0) = 0 . A continuous function β : R+ × R+ → R+ is of class KL , if β(s, t ) ∈ K for each fixed t ≥ 0 and β(s, t )

decreases to zero as t → ∞ for each fixed s ≥ 0. C(Rn, Rp)

denotes the class of continuous functions mapping Rn to Rp.For r ∈ N, Cr (Rn, Rp) denotes the class of continuously r-th differentiable functions mapping Rn to Rp.Consider the following nonlinear control system 

˙x = f (x) + g(x)u, (1) where x ∈ Rn is the state initialized at x0 ∈ X0 ⊂ Rn,and u ∈ U ⊂ Rm is the control input. Both f : Rn → Rn

and g : Rn → Rn×m are assumed to be locally Lipschitz continuous, which ensures the existence of the unique solution to (1); see [2] for more details. Let f (0) = 0 and g(0) = 0 .Hence, x(t) ≡ 0 with t ≥ 0 is a trivial solution to (1). 

Definition 1 ( [2]): Given a control input u ∈ Rm, the system (1) is globally asymptotically stable (GAS) , if there exists β ∈ KL such that for all x0 ∈ Rn,

|x(t)| ≤ β(|x0|, t ), ∀t ≥ 0. (2) In addition, if (2) holds for x0 ∈ X0 ⊂ Rn, then the system (1) is semiglobally asymptotically stable (SGAS). 

Definition 2 ( [5]): A set S ⊂ Rn is forward invariant (FI) 

for the system (1), if x(t) ∈ S for any trajectory x(t) starting from x0 ∈ S. If S is FI, then the system (1) is safe with respect to S, and S is called the safe set .A set S ⊂ Rn is associated with a function h ∈ C(Rn, R),and thus the set S ⊂ Rn can be described explicitly as 

S := {x ∈ Rn : h(x) ≥ 0} . (3) Let ∂S := {x ∈ Rn : h(x) = 0 } and Int (S) := {x ∈ Rn :

h(x) > 0}. Assume that Int (S)̸ = ∅ and \Int (S) = S.For the system (1), we aims to design optimal controllers to ensure the systems stabilization and safety. To this end, both CLF V ∈ C(Rn, R+) and CBF B ∈ C(Rn, R) are proposed and combined together via optimization problems; see, e.g., [5], [14]. In this way, the optimal controllers can be designed. However, the Lie derivatives Lg V (x) := ∂V (x) 

> ∂x

g(x)

and Lg B(x) := ∂B (x) 

> ∂x

g(x) are required to be non-zero such that the optimization problems can be solved, thereby resulting in additional constraints on the applicability of the CLF/CBF and the availability of dynamical systems. Hence, in this paper novel high-order CBFs and CLF are proposed to guarantee the stabilization and safety objectives. III. H IGH -O RDER CONTROL LYAPUNOV AND BARRIER 

FUNCTIONS 

In this section, novel high-order control Lyapunov and barrier functions are proposed and further implemented to design the safety and stabilizing controllers in an explicit way. 

A. High-Order Control Lyapunov Function 

Motivated by vector CLFs in [19], a novel HOCLF is proposed for the stabilization control in the high-order cases. 

Definition 3: Consider the system (1). A function V ∈

Cp(Rn, R+) with p ≥ 2 is called a high-order control Lyapunov function (HOCLF) , if for all x ∈ Rn,(i) Lg V (x) = . . . = Lg Lp−2 

> f

V (x) = 0 , Lg Lp−1 

> f

V (x)̸ = 0 ,and there exist α1, α 2 ∈ K ∞ such that 

α1(|x|) ≤ V (x) ≤ α2(|x|); (4) (ii) there exists Λ = ( λ1, . . . , λ p) ∈ Rp×p such that 

inf u∈U {Lf V(x) + Lg V(x)u} ⪯ ΛV(x), (5) where V(x) := (V (x), L f V (x), . . . , L p−1 

> f

V (x)) ,

Lf V(x) := ( Lf V (x), L 2 

> f

V (x), . . . , L pf V (x)) ∈ Rp and 

Lg V(x) := (0 , . . . , 0, L g Lp−1 

> f

V (x)) ∈ Rp×m;(iii) the matrix Λ ∈ Rp×p is Hurwitz, and there exist orthogonal matrices Ω := ( ω1, . . . , ω p) ∈ Rp×p, Π := (π1, . . . , π r ) ∈ Rp×p and a Jordan matrix ¯Λ ∈ Rp×p

such that Λ = Ω ⊤ ¯ΛΠ and Ppi=1 ω⊤ 

> 1

πiVi(x0) > 0.Definition 3 is different from the one in [21] based on the same mechanism as in [16] for HOCBFs. In Definition 3, all the derivatives of V are coupled via a vector inequality (5), while the lasso-like functions are involved in [21]. The matrix 

Λ ∈ Rp×p in (5) needs to satisfy item (iii), whose necessity is presented in the proof of Theorem 1. A special case is 

Λ = 



λ⊤

> 1

...

λ⊤

> r

 =



0

...

0

λp1 λp2 · · · λpp 

I

, (6) which is based on the nature of V(x) and only imposes the constraint on the derivative of Lp−1 

> f

V (x). Based on the proposed HOCLF, the following result is derived to design the stabilizing controller for the high-order cases. 

Theorem 1: If the system (1) admits an HOCLF V ∈ Cp(Rn,

R+), then the closed-loop system is GAS under the controller 

u(x) := 

(0, if x = 0 ,κ(a(x), b(x)) , otherwise , (7) where a(x) := Lpf V (x)−λ⊤ 

> p

V(x), b(x) := Lg Lp−1 

> f

V (x), and 

κ(a(x), b(x)) := a(x) + pa2(x) + |b(x)|4

−| b(x)|2 b⊤(x). (8) 

Proof: From the controller (7), if x = 0 , then u(x) = 0 

and thus Lpf V (x) + Lg Lp−1 

> f

V (x)u − λ⊤ 

> p

V(x) = 0 ; otherwise, 

Lpf V (x) + Lg Lp−1 

> f

V (x)u − λ⊤ 

> p

V(x)= a(x) − d(x) a(x)b⊤(x)

|b(x)|2 − b(x)

pa2(x) + |b(x)|4

|b(x)|2 b⊤(x)= −pa2(x) + |b(x)|4 ≤ 0.

In addition, Lg V (x) = . . . = Lg Lp−2 

> f

V (x) = 0 and 

Lg Lp−1 

> f

V (x)̸ = 0. Hence, for all x ∈ Rn, Lf V(x) + 

Lg V(x)u ⪯ ΛV(x), that is, ˙V(x) ⪯ ΛV(x).We define the following comparison system: 

˙ξ(t) = Λ ξ(t), ξ(0) = V(x0) ∈ Rp.

From item (iii) of Definition 3, the first component of ξ(t)

satisfies ξ1(t) ≤ eλmax ( ¯Λ) t Ppi=1 ω⊤ 

> 1

πiξi(0) and λmax (¯Λ) <

0, where λmax (¯Λ) is the maximal eigenvalue of ¯Λ. That is, 

ξ(t) decreases to zero as t → ∞ asymptotically. From the comparison principle in [22, Lem. 1], V (x) ≤ ξ1(t) for all 

t > 0, and thus the closed-loop system is GAS. From the proof of Theorem 1 we can the necessity of item (iii) of Definition 3. Note that V (x0) > 0 is true, while whether Lf V (x0), . . . , L p−1 

> f

V (x0) are positive is unknown. Hence, item (iii) of Definition 3 is needed for the convergence of V (x) and constrains the choice of the initial state. On the other, the controller (7) is continuous in the region away from the origin, and can be continuous at the origin by introducing the high-order small control property (HOSCP): in a small region around the origin, there exists a bounded control input such that (5) is satisfied. With the HOSCP, we can follow [23] to show the continuity of the controller (7) at the origin. 

B. Reciprocal High-Order Control Barrier Function 

Regarding the safety control, the existing HOCBFs are recalled first, and then a reciprocal HOCBF is proposed. 

Definition 4 ( [15]): Consider the system (1) and a set D ⊂

Rn. A function h ∈ Cr (Rn, R) has least relative degree r ∈ N

in D for the system (1), if Lg Lkf h(x) ≡ 0 for all x ∈ D and 

k = 0 , . . . , r − 2, where Lkf h(x) := ∂k h(x) 

> ∂x k

f (x).If the function h in (3) has least relative degree r ∈ N, then 

h can be defined as the high-order control barrier function (HOCBF) via the following mechanism [15], [16]. First, for all i = 1 , . . . , r , we define the functions: 

ψ0(x) := h(x), ψi(x) := 

 ddt + γi



ψi−1(x), (9) where γi ∈ K e. Next, the following sets are defined: 

Si := {x ∈ Rn : ψi−1(x) ≥ 0}, i = 1 , . . . , r, (10) 

S := ∩ri=1 Si. (11) In the high-order cases, S is taken as the safe set . From (3)-(11), we can see that S ⊆ Si and S ⊆ S. That is, a smaller safe set is addressed for the high-order cases. Finally, from [16], 

h is an HOCBF, if there exists D ⊂ Rn such that S ⊂ D and 

ψr (x) ≥ 0 for all x ∈ D. If the HOCBF h does exist, then the set S is guaranteed to be FI. From the above mechanism, the HOCBF is based on the function describing the safe set, and thus h is called the zeroing HOCBF ; see also [16]. Note that h may be simple and cannot be differentiable appropriately; see [5] for examples. Another way to introduce the HOCBF is based on the function h

implicitly, and the resulting HOCBF is called the reciprocal HOCBF . Following this direction, we define the following functions. For all i = 1 , . . . , r ,

ϕ0(x) := B(x), 1

ϕi(x) := ηi

 1

ϕi−1(x)



− dϕ i−1(x)

dt , (12) where B ∈ C(Rn, R) is a function related to h, and ηi ∈ K e.From (12), a novel HOCBF is proposed below. 

Definition 5: Consider the system (1) and the set S in (11). A function B ∈ Cr (Int (S), R) is called a high-order control barrier function (HOCBF-I) , if for all x ∈ Int (S),(i) Lg Lr−1 

> f

B(x)̸ = 0 and Lg Lkf B(x) = 0 , k = 0 , . . . , r −2;(ii) there exist αi1, α i2 ∈ K e such that for i = 0 , . . . , r − 1,

αi1(ψi(x)) ≤ 1

ϕi(x) ≤ αi2(ψi(x)) , (13) 

inf 

> u∈U

{Lf ϕr−1(x) + Lg ϕr−1(x)u} ≤ ηr

 1

ϕr−1(x)



, (14) where ηr is given in (12). Definition 5 extends the reciprocal CBF in [5] to the high-order cases. Item (i) is from h(x) of least relative order r. In particular, if B(x) is defined via h(x) (see, e.g., [5]), then let 

B(x) := F(h(x)) with F ∈ Cr (R, R), and thus Lg B(x) =  

> ∂F(h)
> ∂h

Lg h(x) = 0 . Based on the chain rule for derivatives and the property of h(x), we can derive Lg B(x) = . . . =

Lg Lr−2 

> f

B(x) = 0 and Lg Lr−1 

> f

B(x)̸ = 0 . In item (ii), (13) shows the relations between ϕi(x) and ψi(x). If i = 0 , then (13) is the same as the one in [5] to show the relation between 

B(x) and h(x). (14) is for the r-th order case and imposes the constraint on the control input. From the definition of ϕi(x),we can see that (14) equals to ˙ϕr−1(x) ≤ ηr (1 /ϕ r−1(x)) ,and thus ϕr (x) ≥ 0. The next theorem presents the safety controller design from the HOCBF-I. Theorem 2: Consider the system (1) admitting an HOCBF-I 

B ∈ Cr (Int (S), R) with the set S in (11). The system (1) is safe with respect to S under the following controller 

u(x) := 

(0, if x ≡ 0,κ(c(x), d(x)) , otherwise , (15) where c(x) := Lf ϕr−1(x) − ηr (1 /ϕ r−1(x)) , d(x) := 

Lg ϕr−1(x) and κ is defined in (8). 

Proof: First, let Hr−1(x) := 1 /ϕ r−1(x), and we have 

˙Hr−1(x) = −(Lf ϕr−1(x) + Lg ϕr−1(x)u)/ϕ 2

> r−1

(x).

From (14), we have 

˙Hr−1(x) ≥ − ηr (1 /ϕ r−1(x)) /ϕ 2

> r−1

(x)= −H2

> r−1

(x)ηr (Hr−1(x)) =: −¯ηr (Hr−1(x)) ,

where ¯ηr is a continuous function. In addition, ¯ηr (0) = 0 

and ¯ηr (Hr−1(x)) > 0 if and only if Hr−1(x) > 0. Since 

x0 ∈ Int (S), Hr−1(x0) > 0 holds from (13) of Definition 5. From the comparison principle [2, Sec. 3.4] and [2, Lem. 4.4], there exists β ∈ KL such that Hr−1(x) ≥ β(Hr−1(x0), t ) for all t ≥ 0. From (13), we have 

ψr−1(x) ≥ α−1(r−1)2 (β(α(r−1)1 (ψr−1(x0)) , t )) .

Hence, ψr−1(x) > 0. That is, Int (Sr )̸ = ∅ and thus Sr is FI. Next, since Hr−1(x) > 0 for all x ∈ Int (S), from the definitions of Hr−1(x) and ϕr−1(x), we have 

˙ϕr−2(x) < η r−1(1 /ϕ r−2(x)) .

Let Hr−2(x) := 1 /ϕ r−2(x), and thus 

˙Hr−2(x) = − ˙ϕr−2(x)/ϕ 2

> r−2

(x)

> −H2

> r−2

(x)ηr−1(Hr−2(x)) .

Following the same mechanism as in the case of r − 1, we can show that Int (Sr−1)̸ = ∅ and Sr−1 is FI. Finally, the above mechanism can be implemented itera-tively, and thus for all i = 1 , . . . , r , Int (Si)̸ = ∅ and Si is FI. Hence, the set Int (S) is FI under the controller (15). Theorem 2 shows how to design the safety controller from the HOCBF-I in Definition 5, and thus extends the existing results in [15], [16] from the zeroing type to the reciprocal type. Hence, the HOCBF is allowed to be different from h

in (3). On the other hand, similar to [15], [16], here the FI property is for the set S in (11), which is a subset of S in (3). How to guarantee the FI property of the set S in the high-order cases is addressed in the following. 

C. HOCBF with respect to the Set (3) To ensure the FI property of the set S in (3), we following Section III-A to propose another novel HOCBF as follows. 

Definition 6: Consider the system (1) and the set S in (3). A function B ∈ Cr (Int (S), R) is called a high-order control barrier function (HOCBF-II) , if for all x ∈ Int (S), item (i) of Definition 5 holds and (i) there exist α1, α 2 ∈ K e and Θ = ( θ1, . . . , θ r ) ∈ Rr×r

such that 

α1(h(x)) ≤ 1

B(x) ≤ α2(h(x)) , (16) 

inf u∈U {Lf B(x) + Lg B(x)u} ⪯ Θ⊤B(x), (17) where B(x) := ( B(x), L f B(x), . . . , L r−1 

> f

B(x)) ∈ Rr ;(ii) there exist orthogonal matrices Ω := ( ω1, . . . , ω p) ∈

Rp×p, Π := (π1, . . . , π r ) ∈ Rp×p and a Jordan matrix ¯Θ ∈ Rr×r such that Θ = Ω⊤ ¯ΘΠ and Pri=1 ω⊤ 

> 1

πiBi(x0) > 0.Different from Definition 5 for the set ¯S in (11), Definition 6 is for the set S in (3), which is larger than the one in (11). 

Theorem 3: Consider the system (1) admitting an HOCBF-II B ∈ Cr (Int (S), R) with the set S in (3). The system (1) is safe with respect to S under the following controller 

u(x) := 

(0, if x = 0 ,κ(¯ c(x), ¯d(x)) , otherwise , (18) where ¯c(x) := Lrf B(x) − θ⊤ 

> r

B(x), ¯d(x) := Lg Lr−1 

> f

B(x), and 

κ is defined in (8). 

Proof: Following the similar fashion as in the proof of Theorem 1, ˙B(x) ⪯ ΘB(x) holds for all x ∈ Int (S). From item (ii) of Definition 6, 

B(x(t)) ≤ eλmax ( ¯Θ) t Pri=1 ω⊤ 

> 1

πiBi(x0) =: β(B(x0), t ).Since eλmax ( ¯Θ) t > 0 for any λmax ( ¯Θ) ∈ R, β(B(x0), t ) >

0 holds for all t > 0. From (16), h(x(t)) >α−12 (1 /β (B(x0), t )) > 0 holds for all t > 0. As a result, the system (1) is safe with respect to the set S.From Theorem 3, S in (3) can be a safe set for the system (1) and thus a larger safe set can be ensured in the high-order cases. Regarding the two proposed HOCBFs, some further discussions are presented. First, the differences of the HOCBF-I and HOCBF-II are discussed below. The HOCBF-I is based on the lasso-like functions in (12) and thus a small safe set in (11) is ensured. The HOCBF-II is based on the techniques in Section III-A and the safe set in (3) can be guaranteed via an additional condition on the initial state (i.e., item (ii) of Definition 6). As a result, both the HOCBF-I and HOCBF-II can be available, and which is to be chosen depends on the considered system. Second, similar to Definition 3, the assumption on the matrix Θ ∈ Rr×r is made to ensure the positiveness of B(x) along the time line. Just like (6), a special case of the matrix Θ is 

Θ = 



θ⊤

> 1

...

θ⊤

> r

 =



0

...

0

θr1 θr2 · · · θrr 

I

. (19) In this special case, Θ is required to be Hurwitz, which will be discussed later. Finally, besides the comparison principles in the proofs of Theorems 1-3, an alternative technique is based on the properties of λp ∈ Rp or θr ∈ Rr ; see [17]. For the two special cases as in (6) and (19), the following result shows the equivalence relation between the two methods. Proposition 1: Consider the matrix Λ in (6). The following two statements are equivalent. 1) The polynomial yp +λp1yp−1 +. . . +λpp = 0 has negative roots only. 2) The matrix Λ is Hurwitz. 

Proof: From the first statement, let the roots of the polynomial be −y1, . . . , −yp < 0. We define the linear system 

˙ξ = Λ ξ, 

and the notations 

z1 = ξ1, zi = ˙ξi−1 + yi−1ξi−1, i = 2 , . . . , p. 

The relation between z and ξ is given below 

z = U ξ, ˙z = Kz, 

where 

U =



1 0 0 . . . 0 0

y1 1 0 . . . 0 0

y1y2 y1 + y2 1 . . . 0 0

... ... ... . . . ... ...

Qp−1 

> i=1

yi . . . . . . . . . Pp−1 

> i=1

yi 1



,K =



−y1 1 0 . . . 0 00 −y2 1 . . . 0 0

... ... ... . . . ... ...

0 0 0 . . . −yp−1 10 0 0 . . . 0 −yp



.

Note that U is invertible and K = U ΛU −1. Hence, the eigenvalues of Λ and the roots of the polynomial are the same, which implies that Λ is Hurwitz. In a similar way, 

Λ = U −1KU and the second statement can be guaranteed to be satisfied from the first statement. The same result for the matrix Θ in (19) can be derived in the similar mechanism. From Proposition 1, we can see the transformation of checking the roots of the polynomial into checking the Hurwitz property of a matrix. In the proof of Proposition 1, the decomposition of the matrix Λ is derived and thus ¯Λ = K, Ω = U and Π = U −1. However, how to implement such a decomposition is flexible and can be set manually such that the constraint on V(x0) can be relaxed. IV. O PTIMAL CONTROL DESIGN 

Once the HOCBF and HOCLF are proposed, a direct way is to combine both of them to address the safety and stabilization control problems simultaneously. In this respect, we can implement the stabilizing controller in Theorem 1 to formulate the following optimization problem. 

min 0.5|u − us|2

s.t. c(x) + d(x)u ≤ 0, (20) where us is the stabilizing controller (7). In this way, the condition (14) is embedded in (20) such that the safety is always guaranteed. If the HOCBF-II is applied, then it is 

¯c(x)+¯ d(x)u ≤ 0 that is embedded in (20), and in this case the following mechanism can be implemented in a similar way. Next we only need to address how to modify the stabilizing controller in an optimal manner. Here we stress that the controller (7) may not be optimal, and us can be selected as certain optimal stabilizing controller a priori . From (20), the optimal controller is designed as 

u(x) := us(x) + ¯ u∗(x), (21) where ¯u∗(x) ∈ Rm is the term to be designed. For this purpose, the following definition is introduced as an extension of [20, Def. 2] to the high-order cases. 

Definition 7: Consider the system (1). Let κ > 0 and 

M (x) ∈ Rm×m be a locally Lipschitz continuous and positive-definite matrix. A set B(κ) is said to be HOCBF-stabilizable , if there exists D ⊂ B(κ) ∩ S such that 

b(x)M −1(x)d⊤(x) ≥ 0, ∀x ∈ (B(κ) ∩ S) \ D, (22) 

˜c(x) ≤ 0, ∀x ∈ D, (23) where ˜c(x) := c(x) + d(x)us(x), b(x) is defined below (7), and c(x), d(x) are defined below (15). 

Lemma 1: Consider the system (1). For any locally Lipschitz continuous and positive-definite matrix M (x) ∈ Rm×m, there exists κ > 0 such that the set B(κ) is HOCBF-stabilizable. 

Proof: Since the origin is included in S, we consider a region around the origin in S. That is, there exists δ > 0

such that B(δ) ⊂ S. Hence, ϕr−1(x) > 0 for all x ∈ B(δ).From the Lipschitz continuity of the functions f, g in (1) and the continuity of us(x), we have |f (x) + g(x)us(x)| ≤ L|x|,where x ∈ B(δ) and L > 0 is the Lipschitz constant. We can find a sufficiently small ϵ > 0 such that for all 

x ∈ B(ϵ), η r (1 /ϕ r−1(x)) > ϵL |∂ϕ r−1(x)/∂x |. Let κ := min {ϵ, δ }, and thus B(κ) ⊆ B(δ) ⊂ S. For all x ∈

B(κ), |f (x) + g(x)us(x)| < L κ and |∂ϕ r−1(x)/∂x || f (x) + 

g(x)us(x)| ≤ ϵL |∂ϕ r−1(x)/∂x | < η r (1 /ϕ r−1(x)) . From the norm inequality, ˜c(x) ≤ | ∂ϕ r−1(x)/∂x || f (x) + g(x)us(x)| − 

ηr (1 /ϕ r−1(x) < 0. Hence, there exists D ⊂ B(κ) ∩ S such that ˜c(x) ≤ 0 for all x ∈ D. That is, the set B(κ) is HOCBF-stabilizable, and the proof is completed. From Lemma 1, the HOCBF-stabilizable set does exist. With the HOSCBF-stabilizable set, we can establish the opti-mal solution to (20) and derive the controller (21). 

Theorem 4: Consider the system (1), the set S ∈ Rn in (11), and the problem (20). The controller (21) with 

¯u∗(x) := 



0, if ˜a(x) ≤ 0

− ˜c(x)

|d(x)|2 d⊤(x), otherwise (24) is an optimal solution to (20). Under the controller (21) with (24), the set S is FI, and the closed-loop system is SGAS with respect to the HOCBF-stabilizable set. 

Proof: First, we prove that the controller (21) with (24) is the optimal solution to (20). From [24, Ch. 5], the problem (20) is convex with respect to the control input, and the solution 40-2 2204062 4 -2 0 2 4 6 80510 15 20 0 2 4 6 80510 0 2 4 6 8-300 -200 -100 0100 200 300 (a) (b) (c)  

> Fig. 1. Illustration of the safe navigation of the quadrotor in the three cases. (a) The position trajectories of the quadrotor. (b) The evolution of the HOCLFs and HOCBFs. (c) The control inputs for the quadrotor.

to (20) exists if and only if the Karush-Kuhn-Tucker (KKT) conditions are satisfied. The Lagrangian for (20) is defined as 

L(x, u, ϱ ) := 0 .5|u − us|2 + ϱ(a(x) + b(x)u),

where ϱ ≥ 0 is a Lagrange multiplier. The KKT conditions are given below. 

∂L(x, u, ϱ )

∂u := u − us + ϱd⊤(x) = 0 , (25) 

ϱ(c(x) + d(x)u) = 0 . (26) From (25)-(26), if c(x) + d(x)u = 0 , then no constraint is imposed to the choice of ϱ ≥ 0. In this case, ϱ is chosen to be 0 such that the cost function in (20) is minimized, thereby resulting in the optimal solution u∗(x) = us(x).If c(x) + d(x)u̸ = 0, then ϱ ≡ 0 from (26) and thus 

u∗(x) = us(x) from (25). In this case, we need to consider two scenarios. The first scenarios is that ˜c(x) ≤ 0, which implies that the safety constraint holds. Hence, u∗(x) = us(x)

minimizes the cost function. The second scenario is that 

˜c(x) > 0, and the safety constraint does not hold. Furthermore, 

us is not the solution to (20) and a novel controller is needed. In this scenario, from (25)-(26), we have ϱ = ˜ c(x)|d(x)|−2

and thus u∗(x) = us(x) − ˜c(x)|d(x)|−2d⊤(x). Hence, the controller (21) with (24) is the optimal solution. Next, from (24), if ˜c(x) ≤ 0, then ¯u∗(x) = 0 and 

Lf ϕr−1(x) + Lg ϕr−1(x)us(x) ≤ ηr (1 /ϕ r−1(x)) . (27) If ˜c(x) > 0, then u(x) = us + ¯ u∗(x), and further 

Lf ϕr−1(x) + Lg ϕr−1(x)( us(x) + ¯ u∗(x)) = Lf ϕr−1(x) + Lg ϕr−1(x)us(x) − ˜c(x)= ηr (1 /ϕ r−1(x)) . (28) From (27)-(28) and Theorem 2, we conclude that under the controller (21) with (24), the set S is FI. Finally, let M (x) := ˜ c(x)|d(x)|−2I, and from Lemma 1 there exists an HOCBF-stabilizable set B(κ) with κ > 0.Since u(x) = us(x) + ¯ u∗(x), we have 

Lpf V (x) + Lg Lp−1 

> f

V (x)u(x) + λ⊤V(x)

≤ − pa2(x) + |b(x)|4 + Lg Lp−1 

> f

V (x)¯ u∗(x)

≤ Lg Lp−1 

> f

V (x)¯ u∗(x).

For all x ∈ B(κ), ¯u∗(x) = 0 if ˜c(x) ≤ 0. Otherwise, from (22) we have Lg Lp−1 

> f

V (x)¯ u∗(x) = −b(x)M (x)d⊤(x) ≤ 0.Hence, for all x ∈ B(κ), Lpf V (x) + Lg Lp−1 

> f

V (x)u(x) + 

λ⊤V(x) ≤ 0, which implies from the proof of Theorem 1 that the closed-loop system is SGAS with respect to B(κ).Theorem 4 presents the optimal controller for both stabi-lization and safety objectives. The proposed controller extends the classic case in [20] to the high-order cases, and does no involve the choice of the gain margin in [14]. In addition, if the HOCBF-II is applied, then a similar controller can be derived to guarantee the FI property of the set (3). V. N UMERICAL RESULTS 

In this section, a numerical example from the safe naviga-tion of quadrotors is presented to illustrate the derived results. All computation is executed via MATLAB R2023a on a laptop with AMD Ryzen 9 5900HX (3.30GHz) and 16GB RAM. Consider the quadrotor modelled in the inertial frame [13]: 

˙p = v, M ˙v = M g e3 − Re3F, 

˙R = R ˆω, J ˙ω = −ω × Jω + τ , (29) where p ∈ R3 is the position and v ∈ R3 is the velocity. M > 

0 is the mass of quadrotor, g is the acceleration of gravity, 

e3 = [0 , 0, 1] ⊤, and R ∈ SO (3) is the rotation matrix from the body-fixed frame to the inertial frame, where SO (3) := 

{R ∈ R3×3 : R⊤R = I, det (R) = 1 } and det (R) is the determinant of R. F ∈ R is the thrust, which is taken as the control input. In the body-fixed frame, ω ∈ R3 is the angular velocity, ˆω ∈ SO (3) is the skew symmetric matrix derived from ω, J ∈ R3×3 is the inertia matrix, “ ×” is the cross product, and τ ∈ R3 is the moment; see [13] for more details. The quadrotor aims to execute a safe navigation mission in an environment; see Fig. 1(a). The unsafe set is defined as 

O := {p ∈ R3 : ( p−po)2 ≤ r2 

> o

}, which is a spherical obstacle. 

po ∈ R3 and ro > 0 are respectively the center and radius of the obstacle. To maintain an appropriate safety margin, let the safe set be S := {p ∈ R3 : h(p) ≥ 0} with 

h(p) := ( p − po)2 − r2, (30) where r := ro + δ with a safety margin δ > 0. Since h(p)

is only dependent on the position p, the attitude controller in [13] is applied directly such that only the position controller needs to be designed via the optimization problem (20). In order to complete the safe navigation mission, the first goal is to reach a desired position pd ∈ R3. To this end, we define the following HOCLF candidate: 

V (p) := ( p − pd)2. (31) Hence, the position controller is to guarantee the convergence of V (p). From Definition 3, V is an HOCLF if the conditions (4)-(5) are satisfied. The second goal is to guarantee the quadrotor to be navigated safely. From (30), we introduce the following HOCBF candidate: 

B(p) := 1 /h (p), (32) which is an HOCBF-I if the conditions (13)-(14) hold and is an HOCBF-II if the conditions (16)-(17) hold. Here we consider the following three cases. Case 1 is to combine the proposed HOCLF and HOCBF-I. Case 2 is to combine the HOCLF and HOCBF-II, while Case 3 is to combine the HOCLF and the HOCBF in [15]. Let M = 4 .34 ,

g = 9 .8, J = diag (0 .082 , 0.0845 , 0.1377) , po := [1 , 1, 3] ⊤,

pd := [3 , 3, 2] ⊤, ro = 2 .35 and δ = 0 .15 . For the HOCLF (31), let λ = [−10 , −11] ⊤. For the HOCBF-I (32), let 

η0(s) := 2 s and η1(s) := 10 s for all s > 0. For the HOCBF-II, let [Θ 21 , Θ22 ] = [10 , −10] . For the HOCBF in [15], let 

γ1(s) := 10 s for all s > 0.Based on (20)-(21) and (24), all the simulation results are presented in Fig. 1. From Fig. 1(a), the obstacle avoidance is achieved in the three cases. From Fig. 1(b), B(p) > 0 in Cases 1 and 2, and h(p) > 0 in Case 3, all of which correspond to the obstacle avoidance in Fig. 1(a). The HOCLF V (p) in the three cases converges to 0 asymptotically, which implies the satisfaction of the navigation mission. However, for Cases 1 and 3 it only takes 4s to achieve the navigation mission, while it takes around 6.5s for Case 2. The control inputs of the three cases are presented in Fig. 1(c), where we can see the bounds of the control input in Case 2 are minimal. To compare the control efforts in the three cases, we intro-duce the evaluation function Ui := R 80 |ui(t)|dt , i ∈ { 1, 2, 3}

and derive U1 = 400 .92 , U 2 = 383 .18 , U 3 = 399 .28 , which in turn shows the advantages of the HOCBF-II. If the control input is constrained, then the explicit controller (24) cannot be derived, and the optimization problem (20) needs to be solved in real time. Let F ∈ [−300 , 300] , and the computation times for the three cases are t1 = 6 .11 ms, t2 = 6 .87 ms, and t3 = 6 .26 ms. In this way, we can see the metrics of the HOCBF-I in terms of the computational complexity. VI. C ONCLUSIONS 

In this paper, we considered the safety and stabilization control problems of nonlinear systems via high-order control barrier and Lyapunov functions. A novel reciprocal high-order control barrier function was proposed for the safety control, while novel high-order control Lyapunov function was proposed for the stabilization control. The compatibility of the safety and stabilization was investigated such that these two objectives could be achieved in a unified manner. Future work will focus on high-order control functions for more general cases like distributed, stochastic and time-delay cases. REFERENCES [1] C. Dawson, S. Gao, and C. Fan, “Safe control with learned certificates: A survey of neural lyapunov, barrier, and contraction methods for robotics and control,” IEEE Trans. Robotics , vol. 39, no. 3, pp. 1749–1767, 2023. [2] H. K. Khalil, Nonlinear Systems , 3rd ed. Prentice Hall Upper Saddle River, 2002. [3] W. E. Dixon, A. Behal, D. M. Dawson, and S. P. Nagarkatti, Nonlinear Control of Engineering Systems: a Lyapunov-based Approach . Springer Science & Business Media, 2003. [4] A. Zvi, “Stabilization with relaxed controls,” Nonlinear Analysis: The-ory, Methods & Applications , vol. 7, no. 11, pp. 1163–1173, 1983. [5] A. D. Ames, X. Xu, J. W. Grizzle, and P. Tabuada, “Control barrier function based quadratic programs for safety critical systems,” IEEE Trans. Autom. Control , vol. 62, no. 8, pp. 3861–3876, 2016. [6] W. Ren, R. M. Jungers, and D. V. Dimarogonas, “Razumikhin and Krasovskii approaches for safe stabilization,” Automatica , vol. 146, p. 110563, 2022. [7] S. Paternain and A. Ribeiro, “Stochastic artificial potentials for online safe navigation,” IEEE Trans. Autom. Control , vol. 65, no. 5, pp. 1985– 2000, 2019. [8] H. G. Tanner and A. Kumar, “Formation stabilization of multiple agents using decentralized navigation functions,” in Robotics: Science and systems , vol. 1. Boston, 2005, pp. 49–56. [9] S. Prajna and A. Jadbabaie, “Safety verification of hybrid systems using barrier certificates,” in International Workshop on Hybrid Systems: Computation and Control . Springer, 2004, pp. 477–492. [10] P. Wieland and F. Allgöwer, “Constructive safety using control barrier functions,” IFAC Proceedings , vol. 40, no. 12, pp. 462–467, 2007. [11] D. Panagou, D. M. Stipanovi´ c, and P. G. Voulgaris, “Distributed coor-dination control for multi-robot networks using Lyapunov-like barrier functions,” IEEE Trans. Autom. Control , vol. 61, no. 3, pp. 617–632, 2015. [12] B. Siciliano, O. Khatib, and T. Kröger, Springer Handbook of Robotics .Springer, 2008, vol. 200. [13] T. Lee, M. Leok, and N. H. McClamroch, “Geometric tracking control of a quadrotor uav on se (3),” 49th IEEE conference on decision and control , pp. 5420–5425, 2010. [14] M. Jankovic, “Robust control barrier functions for constrained stabiliza-tion of nonlinear systems,” Automatica , vol. 96, pp. 359–367, 2018. [15] X. Tan, W. S. Cortez, and D. V. Dimarogonas, “High-order barrier functions: Robustness, safety, and performance-critical control,” IEEE Trans. Autom. Control , vol. 67, no. 6, pp. 3021–3028, 2021. [16] W. Xiao and C. Belta, “High-order control barrier functions,” IEEE Trans. Autom. Control , vol. 67, no. 7, pp. 3655–3662, 2021. [17] X. Xu, “Constrained control of input–output linearizable systems using control sharing barrier functions,” Automatica , vol. 87, pp. 195–201, 2018. [18] P. Ong, M. H. Cohen, T. G. Molnar, and A. D. Ames, “Rectified control barrier functions for high-order safety constraints,” IEEE Control Syst. Lett. , 2024. [19] W. Ren, J. Li, J. Xiong, and X.-M. Sun, “Vector control Lyapunov and barrier functions for safe stabilization of interconnected systems,” SIAM J. Control Optim. , vol. 61, no. 5, pp. 3209–3233, 2023. [20] W. S. Cortez and D. V. Dimarogonas, “On compatibility and region of attraction for safe, stabilizing control laws,” IEEE Trans. Autom. Control ,vol. 67, no. 9, pp. 4924–4931, 2022. [21] A. Frauenfelder, A. Wiltz, and D. V. Dimarogonas, “Decentralized ve-hicle coordination and lane switching without switching of controllers,” 

IFAC-PapersOnLine , vol. 56, no. 2, pp. 3334–3339, 2023. [22] W. Ren and J. Xiong, “Vector-Lyapunov-function-based input-to-state stability of stochastic impulsive switched time-delay systems,” IEEE Trans. Autom. Control , vol. 64, no. 2, pp. 654–669, 2018. [23] E. D. Sontag, “A ‘universal’ construction of Artstein’s theorem on nonlinear stabilization,” Syst. Control Lett. , vol. 13, no. 2, pp. 117–123, 1989. [24] S. P. Boyd and L. Vandenberghe, Convex Optimization . Cambridge University Press, 2004.