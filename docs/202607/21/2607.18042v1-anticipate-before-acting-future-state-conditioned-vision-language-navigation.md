---
title: "Anticipate Before Acting: Future-State-Conditioned Vision-Language Navigation"
title_zh: 行动前先预测：基于未来状态条件的视觉语言导航
authors: "Lingfeng Zhang, Zhanguang Zhang, Liheng Ma, Tongtong Cao, Yingxue Zhang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18042v1"
tags: ["query:vtt"]
score: 9.0
evidence: 未来状态条件化的视觉语言导航，用于指令跟随
tldr: 端到端视觉语言导航中，标准行为克隆仅监督下一步动作，未能利用未来视觉信息。本文提出未来状态条件化导航（FSC-VLN），在训练时通过未来查询令牌对齐未来视觉嵌入，使策略隐式学习预测未来，推理时不需未来图像。在R2R数据集上，FSC-VLN在两种训练数据设置下均提升了成功率、目标到达率与路径长度加权分数，长时程任务增益尤其显著。该方法验证了未来状态条件化可作为有效的训练监督，提升导航性能而无推理开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18042v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1961, \"height\": 945, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18042v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1883, \"height\": 567, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1995, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1738, \"height\": 497, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1884, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1593, \"height\": 306, \"label\": \"Table\"}]"
motivation: 现有VLN行为克隆仅监督下一步动作，缺乏对未来视觉结果的预测能力。
method: 提出FSC-VLN，通过未来查询令牌和对齐未来视觉嵌入，在训练阶段引入未来信息。
result: 在R2R val-unseen上，FSC-VLN提升SR/OSR/SPL，长时程任务增益更大。
conclusion: 未来状态条件化是一种有效的训练策略，可在不额外推理开销下提升VLN性能。
---

## 摘要
端到端视觉语言导航（VLN）结合因果视觉语言模型，能够直接将指令和以自我为中心的观察映射到动作，但标准的行为克隆仅监督下一个动作，并未明确训练策略状态以预测未来的视觉结果。我们首先提出一个诊断性问题：如果在训练和测试时，策略获得了专家轨迹的未来图像作为特权输入，那么这一额外的视觉证据是否有助于选择当前动作？（在实际部署中，测试时无法获得这些专家轨迹的未来图像，因此我们仅将此设置用作特权输入诊断。）答案是肯定的；这一合理性检验表明，未来观察可以提供丰富且可操作的线索。然后，我们提出一个可部署的问题：在不使用未来图像进行推理的情况下，我们能否通过仅将压缩的未来视觉潜在状态作为训练监督来获益？我们提出了未来状态条件视觉语言导航（FSC-VLN），它添加了一个未来查询标记，并通过仅在训练阶段使用的目标分支（训练后移除）将其隐藏状态与冻结的视觉嵌入对齐，对齐距离为Δ步。在R2R验证集-未知场景上，FSC-VLN在两种训练数据设置下相较于基于StreamVLN风格的基线提高了成功率/目标成功率/路径长度加权成功率，尤其在长程任务中提升更大；消融实验进一步支持了双查询设计（分离未来查询和动作查询）。

## Abstract
End-to-end vision-language navigation (VLN) with causal vision-language models can map instructions and egocentric observations directly to actions, but standard behavior cloning supervises only the next action and does not explicitly train the policy state to be predictive of future visual outcomes. We first ask a diagnostic question: if the policy is given an expert-trajectory future image as privileged input at training and testing time, is that additional visual evidence useful for choosing the current action? (These expert-trajectory future images are unavailable at test time in real deployment, so we use this setting only as a privileged-input diagnostic.) The answer is yes; this sanity check shows that future observations can provide rich, actionable cues.   We then ask a deployable question: without accessing future images at inference, can we still benefit from future information by using a compressed future visual latent only as training supervision? We propose Future-State-Conditioned VLN (FSC-VLN), which adds a future-query token and aligns its hidden state to a frozen visual embedding $Δ$ steps ahead via a training-only target branch that is removed after training. On R2R val-unseen, FSC-VLN improves SR/OSR/SPL over a StreamVLN-style baseline under two training-data regimes, with larger gains on long-horizon episodes; ablations further support the dual-query design (separating future and action queries).

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：标准端到端视觉语言导航（VLN）采用行为克隆，仅监督下一步动作，未显式训练策略状态预测未来视觉结果。这可能导致长期导航中微小错误累积，难以纠正。
- **核心问题**：如何在不增加推理开销的前提下，利用未来视觉信息改善导航策略？具体分为两个子问题：①如果直接在训练和测试时给策略提供专家轨迹的未来图像（特权输入），是否有帮助？②能否在不使用未来图像进行推理时，仅通过训练阶段的未来视觉潜在监督来提升在线策略性能？
- **整体含义**：验证未来信息对导航决策具有价值，并提出一种轻量级可部署方法（FSC-VLN），在训练时注入未来状态预测目标，推理时无需额外计算。

## 二、论文提出的方法论
- **核心思想**：通过引入一个未来查询令牌（future query）与一个动作查询令牌（action query），在训练阶段将未来查询隐藏状态对齐到未来观察的压缩视觉嵌入（来自冻结视觉编码器），从而隐式训练策略学习预测未来状态；推理时移除目标分支，仅保留两个查询令牌，保持原有推理流程。
- **关键技术和流程**：
  - **视觉编码**：使用冻结的SigLIP和MLP投影器，将当前和历史观察映射为视觉令牌；未来目标观察也通过相同编码器提取特征，但仅用于训练。
  - **因果序列结构**：在每个观测轮次中，在线上下文$C_t$后依次插入可学习的未来查询$q_F$和动作查询$q_A$，然后接着动作令牌序列$y_t$。因果自注意力确保$q_F$能聚合上下文，$q_A$能关注$q_F$，动作令牌依据$q_A$及之前令牌解码。
  - **未来状态对齐损失**：将未来查询的隐藏状态$h_F^t$通过一个投影层$g$映射到低维空间，与未来目标嵌入$z_{t+Δ}^*$（经平均池化和stop-gradient处理）计算L1距离$L_{\text{future}}$。
  - **总损失**：$L_{\text{total}} = L_{\text{LM}} + \lambda_F L_{\text{future}}$，其中$L_{\text{LM}}$是动作令牌的交叉熵损失。
  - **推理阶段**：移除目标分支，无需未来图像或额外模型调用；仅需一次因果预填充处理所有令牌，然后自回归解码动作令牌。
- **参数设置**：未来查询$q_F$和动作查询$q_A$为可学习向量；对齐投影为线性层+层归一化（文中未详细说明但可推断）；$\lambda_F=0.1$，未来步数$\Delta=32$。

## 三、实验设计
- **数据集与场景**：使用标准室内VLN基准R2R（Room-to-Room）和RxR（Room-across-Room）。主要评估在R2R验证集-未知场景（val-unseen，1839个episode）上进行。
- **Benchmark与对比方法**：以StreamVLN为基线方法（端到端VLM导航策略），对比两种训练数据设置：①仅使用R2R+RxR；②使用R2R+RxR+EnvDrop+ScaleVLN（扩大数据）。无其他外部方法对比，主要进行自身基线对比。
- **评估指标**：成功率（SR）、目标成功率（OSR）、路径长度加权成功率（SPL）。
- **轨迹分层**：根据地标距离将episode分为短程（≤10m，1293个）和长程（>10m，546个），分析不同长度下的效果。
- **消融实验**：去除动作查询令牌（仅保留未来查询），对比性能变化。

## 四、资源与算力
- **文中明确信息**：训练使用4块GPU，每块GPU批大小1，梯度累积16步（全局批大小64）。优化器为AdamW，学习率2×10⁻⁵，训练1个epoch。使用bfloat16精度、梯度检查点、DeepSpeed ZeRO-2。未指定GPU型号、总训练时长及浮点运算量。
- **缺失信息**：未提及具体GPU型号（如A100、H100等）、单次训练持续时间、总GPU小时数。因此无法精确量化算力成本。

## 五、实验数量与充分性
- **主要实验组数**：
  - 特权输入诊断实验（1组，表1）：证明未来图像确实有效。
  - 全局性能对比（2组，表2）：两种训练数据设置下FSC-VLN vs 基线。
  - 分层分析（2组，表3）：两种设置下短程/长程对比。
  - 消融实验（2组，表4-5）：完整双查询 vs 单未来查询，含全面指标和分层结果。
- **实验充分性评价**：
  - 优点：覆盖了不同数据规模（小/大），进行了分层分析揭示长程增益，消融实验验证分离动作查询的必要性；实验公平（共享相同配置）。
  - 不足：仅在一个基准（R2R）上报告完整结果，未见在RxR验证集或其他数据集（如VLN-CE、RoomAcrossRoom）上的评估；方法仅与单一代基线（StreamVLN）对比，未见与其他最新VLN方法（NaVid、Uni-NaVid等）的直接比较；仅在单一大模型骨干（Qwen2-7B）上验证，泛化性有限；未进行超参数敏感性（如Δ、λF）的广泛探索；没有涉及多目标未来（多个Δ）或不确定性建模实验。

## 六、论文的主要结论与发现
- **特权输入诊断**：直接将专家未来图像作为输入，SR从29.96%跃升至71.77%，证明未来视觉信息对导航高度有效。
- **FSC-VLN方法有效**：在不使用未来图像推理情况下，FSC-VLN在两种训练数据设置下均提升SR、OSR、SPL（例如在R2R+RxR下分别+1.47、+3.27、+1.11个百分点；扩充数据下+0.98、+1.04、+0.85个百分点）。
- **长程收益显著**：增益主要集中在长程episode（>10m）：在R2R+RxR下长程SR提升3.85个百分点，SPL提升3.70；而在短程上变化极小。表明未来状态监督在需要长期连贯决策的场景中更有帮助。
- **分离动作查询必要**：去除动作查询令牌会导致SR和SPL下降（尤其长程），Oracle SR可能略有上升，说明专用未来查询和动作查询能更好分离预测与行为生成，提高路径效率。

## 七、优点
- **方法轻量高效**：仅在训练时增加未来目标分支，推理仅多两个令牌，几乎不影响推理速度与计算开销。
- **不改变骨干架构**：保持原始VLM骨干和视觉编码器冻结，仅引入可学习查询，易于集成到现有端到端VLN框架。
- **诊断与部署分离**：通过特权输入实验建立未来信息有用性的理性依据，再设计可部署方法，逻辑清晰。
- **长程问题针对性**：实验通过分层分析突出对长程轨迹的改善，恰中行为克隆的短板。
- **消融实验设计合理**：检验了双查询设计的必要性，结果支持设计决策。

## 八、不足与局限
- **实验范围有限**：仅使用单一基线和单一大模型（Qwen2-7B），未在更多骨干（如LLaVA、VILA）上验证；仅在R2R上报告完整结果，未在RxR测试集或其他环境（如VLN-CE真实物理机器人）验证。
- **未来目标来自专家轨迹**：与智能体实际on-policy未来可能存在差异，无法保证对错误恢复的帮助。
- **固定未来步长Δ**：单一Δ=32可能不适用于所有场景和轨迹长度，缺乏适应性。
- **缺乏不确定性建模**：未来预测本质是多模态的，但方法仅用一个向量对齐，可能丢失多模态信息。
- **泛化性未验证**：未在未见过的环境数据集、不同导航任务（如物体导航、视觉语言重排）上测试。
- **计算细节缺失**：未提供完整训练时间、GPU模型、内存占用等资源度量，不利于可重复性。

（完）
