---
title: "Anticipate Before Acting: Future-State-Conditioned Vision-Language Navigation"
title_zh: 先预测，再行动：基于未来状态条件的视觉语言导航
authors: "Lingfeng Zhang, Zhanguang Zhang, Liheng Ma, Tongtong Cao, Yingxue Zhang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18042v2"
tags: ["query:vtt"]
score: 9.0
evidence: 未来状态条件化的视觉语言导航
tldr: 端到端视觉语言导航的标准行为克隆仅监督下一步动作，缺乏对未来视觉信息的预测，限制长程决策。本文提出FSC-VLN，通过在训练时将未来查询表示与未来视觉嵌入对齐，将未来状态信息蒸馏进策略状态，推理时仅需当前观测。在R2R基准上，该方法显著提升导航成功率，尤其在长程情节中，且仅增加两个前缀令牌，几乎无额外开销。这项工作首次在因果VLN模型中引入未来状态条件，有效增强长程决策能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18042v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1978, \"height\": 953, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18042v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1884, \"height\": 567, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1995, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1738, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1784, \"height\": 339, \"label\": \"Table\"}]"
motivation: 标准行为克隆忽略未来视觉信息，限制长程导航决策；专家未来图像虽提供丰富线索，但部署时不可用。
method: 训练时用未来查询令牌对齐未来视觉嵌入，蒸馏未来信息进策略状态；推理仅需当前观测，仅增两个前缀令牌。
result: 在R2R val-unseen上，FSC-VLN在两种数据设置下提升SR/OSR/SPL，长程情节增益更大。
conclusion: 通过未来状态条件蒸馏，因果VLN模型长程决策能力显著提升，且额外开销极小。
---

## 摘要
端到端的视觉语言导航（VLN）利用因果视觉语言模型将指令和第一人称观察直接映射到动作，但标准行为克隆仅监督下一个动作，并未明确鼓励策略状态能够预测未来的视觉结果，从而限制了长程决策。一种特权输入诊断表明，访问专家轨迹的未来图像可以显著改善导航，这表明未来观测包含丰富的、可操作的线索，尽管此类输入在部署时不可用。受此信号启发，我们提出基于未来状态条件的VLN（FSC-VLN），一种可部署的模型，它通过一个未来查询令牌增强因果策略，并使用仅训练时存在的未来状态监督，将未来观测中的信息提炼到策略状态中。具体地，在训练过程中，我们将未来查询表示与冻结的视觉嵌入对齐到未来Δ步，而推理仅需过去和当前观测。该设计保留了基线推理模式，仅增加两个可学习的前缀令牌，即最小开销。在R2R验证未见过场景上，FSC-VLN在两种训练数据规模下相较于StreamVLN风格的基线在SR/OSR/SPL指标上均有提升，且在长程任务中增益更大；消融实验进一步支持了将未来查询和动作查询分离的双查询设计。

## Abstract
End-to-end vision-language navigation (VLN) with causal vision-language models maps instructions and egocentric observations directly to actions, but standard behavior cloning supervises only the next action and does not explicitly encourage the policy state to be predictive of future visual outcomes, limiting long-horizon decision making. A privileged-input diagnostic shows that access to an expert-trajectory future image can substantially improve navigation, indicating that future observations contain rich, actionable cues, though such inputs are unavailable at deployment. Motivated by this signal, we propose Future-State-Conditioned VLN (FSC-VLN), a deployable model that augments a causal policy with a future-query token and uses training-only future-state supervision to distill information from future observations into the policy state. Concretely, during training we align the future-query representation to a frozen visual embedding $Δ$ steps ahead, while inference requires only past and current observations. This design preserves the baseline inference pattern and adds only two learned prefix tokens, implying minimal overhead. On R2R val-unseen, FSC-VLN improves SR/OSR/SPL over a StreamVLN-style baseline under two training-data regimes, with larger gains on long-horizon episodes; ablations further support the dual-query design that separates future and action queries.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：端到端视觉语言导航（VLN）中，标准行为克隆（Behavior Cloning）仅监督下一步动作，未明确鼓励策略状态预测未来视觉结果，导致长程决策时误差累积、性能受限。
- **研究动机**：通过特权输入诊断发现，若在训练和测试时直接提供专家轨迹的未来图像（30步后），导航性能大幅提升（SR从29.96%升至71.77%），表明未来观测包含丰富的可操作线索，但部署时此类输入不可用。因此希望将未来信息蒸馏进策略状态，同时保持推理时无需未来图像。
- **整体目标**：提出一种可部署的、轻量级方法，利用训练时的未来状态监督改善在线策略的长程导航能力。

## 二、论文提出的方法论
- **核心思想**：在因果VLM策略中引入一个未来查询令牌（future query）和一个动作查询令牌（action query），训练时将未来查询的隐藏表示与冻结视觉编码器提取的未来观测（Δ步后）的池化特征对齐，使策略状态具备预测性；推理时仅用当前和历史观测，不增加额外开销。
- **关键技术细节**：
    - **模型架构**：采用StreamVLN基线，冻结的SigLIP视觉编码器 + Qwen2-7B因果Transformer。在线上下文（指令、历史观测、当前观测）后依次插入可学习未来查询令牌qF和动作查询令牌qA，然后生成动作令牌序列。所有令牌通过单一因果自注意力处理。
    - **未来目标构建**：训练时，使用同一冻结视觉编码器处理专家轨迹中Δ步后的观测ot+Δ，经平均池化得到目标表示z*_t+Δ，并施加stop-gradient。
    - **对齐损失**：将qF的隐藏态hF_t通过投影层g映射到低维空间，与目标投影表示计算ℓ1损失：L_future = ||g(hF_t) - g(z*_t+Δ)||₁。总损失为语言模型交叉熵 L_LM 加上 λ_F * L_future，λ_F=0.1。
    - **推理流程**：丢弃未来目标分支，在线上下文和两个查询令牌联合进行单次因果prefill，未来查询不参与自回归解码，动作令牌自回归生成。仅增加两个可学习前缀令牌，推理成本几乎不变。

## 三、实验设计
- **数据集**：R2R（Room-to-Room）和RxR（Room-across-Room）室内VLN基准，在R2R val-unseen分割上评估。训练数据有两种设置：1) R2R+RxR；2) R2R+RxR+EnvDrop+ScaleVLN（扩展数据）。
- **评测指标**：Success Rate (SR)、Oracle Success Rate (OSR)、Success weighted by Path Length (SPL)。
- **对比方法**：以StreamVLN为基线（相同骨干、优化器等配置）。还进行了特权诊断实验（基线+专家未来图像输入）。
- **分层分析**：按起始点到目标的测地距离将1800+个episode分为短程（≤10m，1293条）和长程（>10m，546条），分别报告指标。
- **消融实验**：移除动作查询令牌（仅保留未来查询，同时承担动作条件），对比完整FSC-VLN。

## 四、资源与算力
- **训练硬件**：4张GPU（未明确具体型号），每设备batch size=1，梯度累积16步（全局batch size=64）。
- **优化设置**：AdamW优化器，学习率2×10⁻⁵，权重衰减0，预热比0.075，余弦学习率调度，梯度裁剪1.0。
- **精度与加速**：bfloat16精度，使用梯度检查点（gradient checkpointing）和DeepSpeed ZeRO-2。
- **训练周期**：1个epoch（在构建的轨迹片段上）。
- **未明确信息**：GPU型号（如A100或V100）、训练总时长未提及。

## 五、实验数量与充分性
- **实验数量**：包括1个特权诊断实验（表1）、2个主要结果表格（表2和表3，分别对应两种数据设置及分层分析）、1个消融实验（表4）。
- **充分性**：实验设计较为全面，覆盖了不同训练数据规模、短/长程分层分析、以及独立的消融验证。控制变量严格（保持基线所有共享组件不变），对比公平。
- **客观性**：结果报告了绝对变化百分点，区别对待了整体与分层表现，不遗漏负面结果（短程改进甚微）。消融实验揭示了双查询设计的必要性。
- **潜在不足**：仅在一个骨干网络（Qwen2-7B + SigLIP）上进行，缺乏跨骨干或跨环境（如室外场景）的验证。未与其他未来预测方法（如VLA-JEPA等）进行直接比较。

## 六、论文的主要结论与发现
- **主要结论**：FSC-VLN通过训练时未来查询对齐蒸馏未来视觉信息，有效提升了端到端VLN策略的性能，尤其对长程导航情节改善显著（长程SR提升约2.9~3.9个百分点）。分离未来查询和动作查询的双查询设计是必要的，删除动作查询导致性能下降。
- **具体发现**：
    - 特权诊断实验证实未来图像包含丰富导航线索。
    - 在两种训练数据设置下，FSC-VLN均提升SR/OSR/SPL（整体提升约1~3个百分点）。
    - 短程情节改进有限（+0.15~0.46点），长程情节增益集中，支持假设：未来状态监督在需长时间保持决策一致时更有价值。
    - 消融表明，单一未来查询同时承担预测和动作条件时，可能因表示冲突而损害效率（SPL下降），动作查询有助于更好利用预测信息。

## 七、优点
- **轻量高效**：推理时仅增加2个前缀令牌，不改变Transformer结构，不计额外前向或解码步骤，几乎不增加成本。
- **可部署性强**：无需未来图像、未来图像生成器或第二策略前向，完全保持基线推理模式。
- **目标明确**：通过特权诊断验证未来信息有用性，使方法设计有据可依。
- **有效提升长程性能**：针对长导航序列误差累积问题提出针对性解决方案，并在分层分析中清晰展示增益。
- **消融设计清晰**：通过移除动作查询验证了分离预测与动作条件的必要性，提升解释性。

## 八、不足与局限
- **未来目标偏差**：训练时使用的未来观测来自专家轨迹，与智能体实际on-policy未来的可能状态存在差异，可能导致分布不匹配。
- **固定视野Δ**：使用固定Δ=32步，可能不适合所有场景和轨迹长度（如极短或极长情节）。未探索自适应或多尺度未来目标。
- **骨干依赖性**：仅在Qwen2-7B单一骨干上验证，未在其他VLM（如LLaVA、InternVL）或VLN基线上验证，泛化性未知。
- **环境局限**：仅在室内VLN基准（R2R/RxR）上评估，未涉及室外或移动操作场景。
- **与先进方法比较不充分**：未与其他引入未来预测的模型（如VLA-JEPA、FastWAM等）进行直接对比，无法确认其相对优劣。
- **计算资源细节缺失**：GPU型号、训练时长等未公开，影响可复现性。

（完）
