---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：一种用于世界与动作建模的统一扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:vla-robotics"]
score: 9.0
evidence: 用于机器人操控的世界与动作统一扩散Transformer
tldr: 许多最新机器人策略为提升控制能力，常依赖于大型预训练的视觉语言模型作为动作骨干。本文提出WorldDiT，一种统一扩散变换器架构，通过将动作生成与视觉世界建模耦合，在训练中利用单个扩散变换器同时生成连续的机器人动作块并预测未来相机帧的归一化RGB图像块。在四个LIBERO模拟套件上，WorldDiT在总模型参数和平均成功率方面均位于报告方法的帕累托前沿，以不到10亿参数达到强竞争力。该架构为未来机器人策略的规模化研究提供了强有力的基线，展示出无需大型VLM骨干即可实现优异控制的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2316, \"height\": 1067, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2240, \"height\": 1638, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 2333, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 2330, \"height\": 916, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2399, \"height\": 1614, \"label\": \"Table\"}]"
motivation: 现有机器人策略过度依赖大型预训练VLM作为动作骨干，本文旨在探索无需大VLM的统一扩散架构同时实现动作生成与世界建模。
method: WorldDiT采用扩散变换器架构，训练时同时生成连续动作块和预测未来帧的归一化RGB patch，实现动作与视觉的联合建模。
result: 在四个LIBERO模拟套件上，WorldDiT以次十亿参数达到帕累托最优的平均成功率与参数效率。
conclusion: 本文提供了一个无需大型VLM的强效次十亿参数基线，为未来机器人策略规模化研究奠定基础。
---

## 摘要
近年来许多机器人策略通过使用大型预训练视觉语言模型（VLM）作为动作骨干来追求更强的控制。我们提出了WorldDiT，一种统一的扩散变换器架构，它将动作生成与视觉世界建模相结合，并在没有大型预训练VLM动作骨干的情况下实现了强大性能。在训练过程中，单个扩散变换器生成连续的动作块，并从未来相机帧中预测归一化的RGB块目标。在四个LIBERO仿真套件中，WorldDiT位于报告所有四个套件的方法的总模型参数和平均成功率的帕累托前沿上。这些结果为未来的扩展研究提供了一个强大的十亿参数以下基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- 当前许多机器人策略依赖于大型预训练视觉语言模型（VLM）作为动作骨干，这种模式虽然带来了强大的感知和语言理解能力，但也使架构和规模对性能的贡献难以分离。
- 当策略模型包含数十亿参数时，其强大控制能力的来源（动作设计、预训练骨干或其组合）不明确。
- 本文旨在探索：是否可以在不使用大型预训练VLM作为动作骨干的情况下，通过一个统一的扩散变换器架构，将连续动作生成与辅助的未来视觉预测目标结合起来，从而保持强大的控制性能。
- 这一研究旨在为未来的模型规模化研究提供一个清晰的、低于十亿参数的强基线。

## 二、论文提出的方法论
- **核心思想**：WorldDiT提出一种统一的扩散变换器架构，在单个共享骨干网络中同时完成动作生成和未来视觉预测（归一化RGB块预测）。该共享骨干基于DiT（Diffusion Transformer）。
- **技术细节**：
  - **编码阶段**：使用冻结的视觉编码器（MAE）、语言编码器（CLIP文本编码器）和一个可训练的机器人状态编码器，处理过去三帧的观测（主相机和腕部相机图像）、机器人状态和语言指令。
  - **训练时**：共享的DiT骨干网络接收被高斯噪声破坏的动作token（7步动作块）和归一化RGB块token（来自未来第7帧的主相机和腕部相机图像，各64个patch），并预测它们的流动（flow）速度。
  - **损失函数**：采用Flow Matching目标，总损失是动作速度损失与RGB块速度损失的加权和（权重分别为0.1和0.001）。
  - **推理时**：仅利用动作路径。从高斯噪声初始化动作块，通过20步欧拉积分对动作速度场进行数值积分，得到预测的动作块。然后执行前三步动作，更新观测窗口，重新规划。
  - **辅助目标的作用**：未来RGB块预测仅在训练时提供辅助监督，推理时完全不参与。

## 三、实验设计
- **数据集与场景**：在LIBERO模拟基准的四个下游套件上进行评估：`libero_spatial`、`libero_object`、`libero_goal`和`libero_10`（报告为LIBERO Long），并使用大规模多任务数据集`libero_90`进行预训练。
- **基准配置**：使用固定长度窗口（10个时间步），包含多视角RGB观测、机器人状态、语言指令和动作序列。共有3个观测上下文步骤和7个目标动作步骤。未来RGB块目标来自于时间偏移7步后的帧。
- **对比方法**：报告了24种方法的性能，分为两类：使用大型预训练VLM作为动作骨干（如π0.5, OpenVLA等）和不使用的（如WorldDiT, Diffusion Policy, Octo等）。

## 四、资源与算力
- 使用了8块RTX Pro 6000 GPU（单节点八卡）。
- 训练配置：首先在`libero_90`上预训练30个epoch，单GPU批次大小为40，梯度累积为2。然后在每个下游套件上独立微调，使用有效批次大小为512（单GPU批次32，梯度累积2）。
- 训练使用了bf16混合精度。
- 论文未明确报告微调的总训练时长（如小时数），但指明了预训练为30个epoch，微调至收敛。

## 五、实验数量与充分性
- **主要实验**：在四个LIBERO套件中各评估了500个模拟episode（共2000 episodes）。其中一个WorldDiT配置，针对每个套件单独微调后测试。
- **消融实验**：论文**没有**进行消融实验（如研究不同损失权重、预测步数、观测窗口长度或是否使用RGB预测分支的影响），这使得对方法各个组件的单独贡献理解有限。
- **公平性和充分性**：
  - 报告的成功率结合了300个episode的阶段性检查点选择，因此并非完全独立于测试集，存在轻微的数据泄漏风险，作者也承认了这一点。
  - 对比方法的数据来源不同（有些来自原始论文，有些需要估算），且由于模型权重和代码未完全公开，无法在统一协议下复现所有对比方法，因此对比的严格性有限。
  - 实验集中在LIBERO仿真套件，未触及真实世界场景。

## 六、论文的主要结论与发现
- WorldDiT在四个LIBERO套件上实现了平均94.9%的成功率（Spatial: 98.0%, Object: 97.0%, Goal: 92.8%, Long: 91.8%）。
- 在报告的所有四个套件的方法中，WorldDiT（总参数3.99亿，可训练参数1.35亿）位于总模型参数与平均成功率的帕累托前沿上。
- 所有平均成功率高于WorldDiT的方法都使用了更多的总参数，而所有参数少于或等于WorldDiT的方法报告的成功率更低。
- 结论：在不依赖大型预训练VLM作为动作骨干的情况下，统一的世界-动作建模扩散架构可以在次十亿参数级别上取得极具竞争力的基准性能。这为未来的规模化研究提供了一个可靠的基线。

## 七、优点
- **架构的简洁性与统一性**：在统一架构中融合了动作生成和世界建模（可视化预测），实现参数共享，无需单独的、庞大的VLM骨干。
- **高效的辅助训练策略**：未来RGB块预测目标仅用于训练时的监督，推理时零开销，这在不影响推理效率的情况下提升了表征学习。
- **参数量与性能的高效平衡**：以不到4亿总参数（1.35亿可训练）达到了接近SOTA的性能，展示了高效的参数利用。
- **稳健的基线贡献**：为机器人策略领域的模型规模化研究提供了一个明确的、可复现的次十亿参数基线，有助于分离规模与架构的贡献。

## 八、不足与局限
- **评估覆盖范围有限**：仅在LIBERO模拟环境中进行了评估，未在真实的机器人硬件或更复杂、更多样化的仿真环境中验证。其现实世界泛化能力未知。
- **缺乏消融实验**：没有对关键设计选择（如RGB预测目标的有效性、不同损失权重、未来预测步长、观测窗口长度等）进行系统的消融研究，导致对方法中各个组件贡献的理解不完整。
- **数据泄漏风险**：报告的成功率中包含了用于阶段性检查点选择的episode，因此不完全独立，存在高估的偏差风险。
- **对比方法的严格性不足**：由于无法在统一的评估协议下复现所有对比方法，并依赖原始论文报告的数据，比较的公平性打折扣。
- **动作与观察的时序维度有限**：仅使用三步观测和七步动作块，可能不足以建模需要更长时域依赖的任务（长期规划优势不明显）。
- **无针对失败案例的分析**：没有对失败情况（尤其是在Long套件中比例较高）进行错误模式分类和根本原因分析，限制了方法的进一步改进方向。

（完）
