---
title: Bridging Reinforcement Learning and Optimal Control via Feasible Action Mapping
title_zh: 通过可行动作映射桥接强化学习与最优控制
authors: "Stefan Richter, Alberto Giammarino, Guillem Torrente, Sam Blakeman, Peter Dürr"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23930v1"
tags: ["query:rl-robotics"]
score: 9.0
evidence: 结合强化学习和最优控制处理约束动态系统
tldr: 控制约束动态系统需兼顾任务复杂性与约束可行性。提出FAOC框架，通过优化映射将强化学习动作转为最优控制问题的可行参数，严格满足系统约束。在机器人乒乓球实验中，FAOC在样本效率和闭环性能上优于现有基线。该方法结合了强化学习的灵活性与最优控制的可预测安全性，无需专家设计动作空间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23930v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2255, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2250, \"height\": 546, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1821, \"height\": 1226, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 2245, \"height\": 703, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1582, \"height\": 2520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 2219, \"height\": 855, \"label\": \"Figure\"}]"
motivation: 结合强化学习灵活性与最优控制约束满足，解决约束动态系统中递归可行性与安全性的矛盾。
method: 提出FAOC，用优化映射将强化学习抽象动作转化为最优控制问题的可行参数集，保证约束严格满足。
result: 在机器人乒乓球任务中，FAOC在样本效率和闭环性能上均超越现有基线。
conclusion: FAOC有效融合强化学习与最优控制，无需专家设计动作空间，为受约束系统提供高效安全控制方案。
---

## 摘要
运行受约束的动态系统需要控制器在强制执行递归可行性和安全约束的同时高效解决复杂任务。为应对这些相互竞争的要求，我们提出了一种新颖的控制框架——面向最优控制的可行动作（FAOC），该框架整合了强化学习（RL）和最优控制（OC）。关键贡献在于一种计算高效的、基于优化的映射算法，该算法将RL智能体的动作从静态抽象集合转换为最优控制问题（OCP）的状态相关可行参数集合，从而严格保证动态系统约束的满足。因此，FAOC有效结合了OC的可预测安全性与RL的灵活性。与先前工作相比，RL智能体的抽象动作空间无需专家或启发式设计，且OCP的公式化不会因RL无法保证可行性而妥协。我们将该方法应用于机器人乒乓球实时运动规划，该场景集中体现了上述挑战。通过仿真实验，我们证明FAOC在样本效率和闭环性能方面均优于最先进的基线方法。

## Abstract
Operating constrained dynamical systems requires controllers to efficiently solve complex tasks while enforcing recursive feasibility and safety constraints. To address these competing requirements, we present Feasible Action for Optimal Control (FAOC), a novel control framework integrating Reinforcement Learning (RL) and Optimal Control (OC). The key contribution is a computationally efficient, optimization-based mapping algorithm that transforms the RL agent's action from a static abstract set into a state-dependent feasible parameter set of the Optimal Control Problem (OCP), guaranteeing strict satisfaction of the dynamical system's constraints. Thus, FAOC effectively combines the predictable safety of OC with the flexibility of RL. In contrast to prior work, the abstract action space of the RL agent does not require expert or heuristic design, and the OCP formulation is not compromised by the inability of RL to guarantee feasibility. We apply our approach to real-time motion planning for robot table tennis, which encapsulates these challenges. Via simulated experiments, we show that FAOC outperforms state-of-the-art baselines in both sample efficiency and closed-loop performance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：受约束动态系统的控制需要同时满足“高效解决复杂任务”和“强制执行递归可行性与安全约束”两个相互竞争的要求。纯粹基于强化学习（RL）的方法无法严格保证约束满足；纯粹基于最优控制（OC，如模型预测控制MPC）的方法在处理非凸、长时域、稀疏奖励问题时计算困难且对初始值敏感。
- **研究动机**：利用RL的灵活性和OC的安全性，将二者有机融合，避免传统结合方式中动作空间需要专家设计或OCP因RL动作不可行而妥协的问题。
- **整体含义**：提出Feasible Action for Optimal Control（FAOC）框架，通过在RL智能体和参数化OCP之间引入一个计算高效的优化映射算法，将RL的抽象动作转化为OCP的状态相关可行参数集合，从而保证系统约束严格满足，同时允许RL在静态、简单的抽象动作空间中进行探索。

## 二、论文提出的方法论
- **核心思想**：建立一个从静态、紧凑、凸的抽象动作集合 \bar{A} 到状态相关的可行参数集合 P(x) 的双射映射 M，使RL智能体采样的每个抽象动作都能对应一个保证OCP可行的参数，且所有可行参数均可由某个抽象动作生成。
- **关键技术细节**：
    1. **拓扑特征化**：给出保证参数集 P(x) 紧致、实心、凸的充分条件（Lemma 1），并针对线性MPC带终端约束的情形给出具体验证（Corollary 1）。
    2. **径向映射算法（Algorithm 1）**：通过计算从基集合内点 x_c 出发的射线与边界的缩放因子 α，再从目标集内点 y_c 沿变换后的方向 d' 计算 β，得到映射点 y = y_c + (β/α) d'。该映射是双射的（Proposition 1）。
    3. **方向变换缓解点聚集**：
        - **面积匹配变换（二维）**：基于归一化面积函数的逆函数，将极角映射使得均匀分布的点在目标集中保持面积比例均匀（Proposition 2, 3）。
        - **线性变换（任意维度）**：利用仿射相关的椭球代理集合（如最大体积内接椭球MVIE）构造变换矩阵 Φ = Q_Y Q_X^{-1}（Proposition 4），近似纠正几何畸变。
    4. **隐式定义集合的实时实现**：
        - 内点计算：通过顶点式内切球（Proposition 6）或提升式内切球（Proposition 7）在W空间中获得内点，再投影到Y。
        - 变换矩阵计算：离线构造包含初始状态和输入的可达集椭球代理，通过奇异值分解获得与状态无关的常数形状矩阵（Proposition 8, Example 3）。
        - 边界缩放因子计算：通过求解一个线性约束凸优化问题（式(8)）在W空间中找到对应的边界点。
- **算法流程**（文字描述）：① RL策略根据状态输出高斯分布参数（均值、协方差），采样或取均值后经非线性函数b得到抽象动作 \bar{a}；② 通过映射算法M将 \bar{a} 映射到状态相关可行参数 p ∈ P(x)；③ 将p作为终端约束参数代入OCP求解最优控制序列；④ 应用控制序列并观测下一状态。

## 三、实验设计
- **应用场景**：机器人乒乓球，8自由度移动机械臂（Sony AI的物理机器人）。仿真环境中训练与评估，同时提及真机已用于对阵职业选手并获胜（引用[1]）。
- **对比方法**：
    - **1Dpos / 1Dvel / 1Dacc**：使用Kiemel等人方法的单维动作空间（只能指定位置、速度或加速度其中之一）。
    - **2Dsoft**：二维动作空间（位置+速度），但使用软终端代价代替硬约束，动作空间基于静态范围（全运动学范围），可能包含不可行动作。
    - **FAOC（本文）**：二维抽象动作空间 + 可行映射 + 硬终端等式约束。
- **强化学习算法**：Soft Actor-Critic (SAC) + BRO网络架构（基线实验）；SimBaV2（分布批评网络 + 更大网络）进行扩展实验。
- **评估指标**：归一化奖励、Fraction Balls Returned（返回率）、Generated Topspin/Backspin/Velocity、Goal Distance。每个指标基于1000次评估回合，每个方法使用5个不同随机种子训练。
- **实验组别**：
    1. **基线实验**：SAC + BRONets，五个方法对比。
    2. **算法与架构影响**：使用SimBaV2（分布批评+更大网络），考察FAOC优势是否对底层RL算法鲁棒。
    3. **低RL频率实验**：将RL决策频率从31.25 Hz降至7.8125 Hz（轨迹段长从32 ms增至128 ms），检验FAOC在更低控制频率下的可控性优势。

## 四、资源与算力
- 论文**未明确披露**使用的GPU型号、数量、训练时长或总计算量。仅提及仿真环境下的训练使用SAC或SimBaV2，且每个方法训练5个种子，每个实验至多1.2亿环境步（图6横轴）。关于训练的具体硬件配置、每轮训练时间等均未给出。

## 五、实验数量与充分性
- **实验组数**：三个主要实验组（基线、SimBaV2、低频率），每个组内比较5种方法×5个种子，共约75个独立训练运行。每个评估点基于1000回合，统计量充沛。
- **统计分析**：使用Mann-Whitney U检验进行两两比较，标注p值，明确指出FAOC显著优于其他方法的具体数量（如基线中8/15个显著比较，低频率中13/15）。
- **消融与隔离**：通过比较1D与2D、软约束与硬映射，以及不同架构/频率，有效隔离了核心贡献（二维映射 + 可行性保证）的影响。
- **充分性与公平性**：实验设计覆盖了不同RL算法、不同架构规模、不同决策频率，对比基线均为相关领域代表方法。真机验证虽不在本文实验数据中（引用[1]），但提供了实际应用证据。总体实验充分、客观、公平。

## 六、论文的主要结论与发现
- FAOC在样本效率和最终性能上一致优于所有对比基线，且在统计上显著。
- 2D动作控制（FAOC和2Dsoft）优于1D动作控制，说明二维方式点（位置+速度）提供更好可控性。
- 在更强的RL算法（SimBaV2）下，所有方法均有提升，但FAOC仍保持优势，证明映射方法的有效性不依赖于特定RL实现。
- 在低RL频率（7.8125 Hz）下，FAOC的性能衰减远小于其他方法，说明其状态相关动作空间设计带来了更好的轨迹段可控性。
- 真机应用验证：FAOC使机器人首次在官方ITTF比赛中与职业选手对决并获胜（引用[1]）。

## 七、优点
- **严格的约束保证**：通过可逆映射将RL动作限制在OCP的可行参数集内，消除不可行动作探索，无需软惩罚或松弛变量。
- **无需专家设计动作空间**：抽象动作空间仅需为静态、紧致、凸集（如超盒），RL可在简单空间内学习。
- **计算高效、实时可用**：线性变换矩阵可离线计算，在线仅需线性求解和简单线搜索，二次规划由DAQP实时求解。
- **可逆性**：支持双向映射（从抽象动作到可行参数，以及逆映射），便于数据增强、利用专家演示等。
- **理论扎实**：给出参数集凸性、紧致性、实心性的充分条件，以及映射连续性和双射性证明。
- **处理几何畸变**：提出面积匹配（2D）和线性变换（任意维）方向变换，缓解点聚集，提升学习效率。

## 八、不足与局限
- **假设凸性**：核心映射要求基集和目标集均为凸集。论文通过实验表明在机器人乒乓球这一非凸任务中（例如避障、非线性运动学由RL解决，OCP内部凸）仍有效，但理论推广到一般非凸可行集时需额外处理。
- **隐式集合计算依赖SDP**：为获得内切椭球等代理，需要离线求解半定规划，对于极高维决策变量或实时变化场景可能成为瓶颈（但论文通过离线计算+状态无关设计规避）。
- **实验仅在仿真环境详细报告**：虽然提到真机获胜，但本文所有量化比较均基于仿真。实际应用中存在模型误差、延迟等不确定因素，FAOC的鲁棒性尚未充分评估。
- **未报告计算资源消耗**：缺乏对GPU/CPU训练时间、内存等资源的具体描述，使得复现和对比难度增加。
- **动作维度限制**：实验中动作维度为2（位置+速度），虽然满足乒乓球需求，但推广到更高维动作场景（如直接学习多关节转角）可能需要更复杂的映射设计。
- **仅使用线性MPC**：OCP采用线性动力学（三积分器），对于强非线性系统（如四足机器人、无人机）需扩展至非线性MPC，映射的凸性假设可能不再成立。

（完）
