---
title: "τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision"
title_zh: τ：从未来视觉监督中学习触觉增强的视觉-语言-动作模型
authors: "Ning Cheng, Jinan Xu, Wanlin Li, Yangzhi Chen, Jing Gao, Yiqun Wang, Kelan Peng, Wenjuan Han"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24485v1"
tags: ["query:vla-robotics"]
score: 9.0
evidence: 触觉增强的视觉-语言-动作模型用于机器人操作
tldr: 触觉增强的视觉-语言-动作模型在有限演示数据下难以学习高维时空触觉表示。提出τ框架，利用联合嵌入预测架构从未来视觉监督学习动作条件化触觉表示，与视觉语言特征融合生成动作。在四个接触丰富操作任务上，τ优于现有方法并泛化到未见物体。引入TacAura同步数据集，提升了有限数据下的操作性能和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24485v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2519, \"height\": 1386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2554, \"height\": 566, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 2523, \"height\": 1694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1210, \"height\": 803, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1152, \"height\": 658, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24485v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2521, \"height\": 556, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24485v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2505, \"height\": 404, \"label\": \"Table\"}]"
motivation: 现有触觉增强VLA模型在有限数据下难以学习有效的高维时空触觉表示，且部署开销大。
method: 提出τ，采用JEPA从未来视觉监督学习动作条件化触觉表示，融合视觉语言特征生成动作，监督仅在训练时使用。
result: 在四个接触丰富任务上，τ优于现有方法，且泛化到未见物体和场景。
conclusion: 通过未来视觉监督的触觉表示学习，τ在有限数据下提升操作性能，并引入TacAura数据集。
---

## 摘要
学习信息丰富的触觉表示并将其有效适应预训练的视觉-语言-动作（VLA）模型在数据和建模层面均具有挑战性。在数据层面，有限的任务特定演示限制了表示质量，而大规模预训练则带来了高昂的成本。在建模层面，现有方法要么关注瞬时接触状态，要么使用6维力/力矩序列对时间交互动态进行建模，使得高维触觉信号尚未得到充分探索。为了解决这些挑战，我们提出了τ，一种触觉增强的VLA框架，它受到联合嵌入预测架构（JEPA）的启发，从未来的视觉监督中学习动作条件下的时空触觉表示，并将其与视觉-语言特征融合，以在有限数据下生成动作。这种监督在潜在空间中运作，且仅在训练期间使用，不增加部署开销。我们还引入了TacAura，这是一个包含四个代表性接触丰富操作任务中同步视觉、本体感觉和基于视觉的触觉信号的数据集。实验表明，τ优于现有模型，并能泛化到未见过的物体和场景，提供改进的操作性能和鲁棒性。

## Abstract
Learning the informative tactile representation while effectively adapting it to pretrained Vision-Language-Action (VLA) models remains challenging at both the data and modeling levels. At the data level, limited task-specific demonstrations constrain representation quality, whereas large-scale pretraining incurs substantial costs. At the modeling level, existing methods either focus on instantaneous contact states or model temporal interaction dynamics using 6D wrench sequences, leaving high-dimensional tactile signals underexplored. To address these challenges, we present τ, a touch-augmented VLA framework that learns an action-conditioned spatiotemporal tactile representation from future visual supervision inspired by the Joint-Embedding Predictive Architecture (JEPA), and fuses it with vision-language features for action generation under limited data. This supervision operates in latent space and is used only during training, adding no deployment overhead. We also introduce TacAura, a dataset of synchronized vision, proprioception, and vision-based tactile signals across four representative contact-rich manipulation tasks. Experiments show that τ outperforms existing models and generalizes to unseen objects and scenes, delivering improved manipulation performance and robustness

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在接触丰富的机器人操作任务中，现有的视觉-语言-动作（VLA）模型仅依赖视觉模态，难以捕捉物理交互动态（如接触状态、力分布、变形模式），导致操作性能受限。尽管触觉传感提供了补充信息，但如何在有限数据下有效学习高维时空触觉表示，并将其融入预训练VLA模型，同时不增加推理开销，仍然是一个开放挑战。
- **研究动机**：现有方法要么仅利用瞬时接触状态，要么用6D力/力矩序列建模时间动态，忽视了高维视觉触觉信号中丰富的空间接触模式及其时间演化。此外，大规模多模态预训练成本高昂，任务特定演示数据稀缺。因此，需要一种在有限数据和计算预算下，能学习动力学感知触觉表示并高效融合进VLA模型的方法。
- **整体含义**：本文提出一个触觉增强的VLA框架 **τ**，通过联合嵌入预测架构（JEPA）从未来视觉监督中学习动作条件化的时空触觉表示，在保持预训练VLA能力的同时，提升接触丰富任务的性能和泛化能力。

## 二、论文提出的方法论
- **核心思想**：引入一个触觉编码与适配模块，将视觉触觉信号映射到预训练VLA的语义空间；同时，利用JEPA风格的跨模态自监督预测分支，以当前触觉表示和后续动作为条件，预测未来视觉特征的变化，从而学习动力学感知的触觉表示。该分支仅在训练时使用，不增加推理复杂度。
- **关键技术细节**：
  1. **触觉编码与适配模块**：使用触觉编码器 $E_{tou}$ 提取左右指触觉特征 $z_t^{touch}$，再通过可学习触觉适配器 $A_{tou}$ 将其投影到VLA模型的潜在空间，得到LLM对齐的触觉标记 $Z_t^{touch}$。这些标记与视觉标记 $Z_t^{vision}$、文本标记 $Z_t^{language}$ 拼接成统一表示 $Z_t$，供大语言模型和动作专家预测动作块 $\hat{a}_{t:t+H}$。
  2. **JEPA式预测自监督分支**：将适配后的触觉标记 $Z_t^{touch}$ 与动作编码器 $E_{act}$ 编码的动作标记 $Z_{t:t+H}^{action}$ 拼接，送入轻量级MLP预测器 $P(\cdot)$，预测未来视觉特征的变化 $\Delta \hat{z}_{t+\Delta_k}^{vision}$（相对于当前视觉特征）。目标变化 $\Delta z_{t+\Delta_k}^{vision}$ 由VLA视觉编码器从未来观测中提取。损失函数为加权余弦对齐损失 $L_{SSL}$，其中权重 $w_{t+\Delta_k}$ 根据触觉变化幅度计算并裁剪，以突出显著接触过渡。
  3. **训练目标**：联合优化模仿学习损失 $L_{IL}$（条件流匹配）和自监督损失 $L_{SSL}$，总损失 $L_{total} = L_{IL} + \lambda L_{SSL}$。模仿学习通过去噪向量场匹配专家动作轨迹；自监督损失鼓励触觉表示编码未来交互动态。
- **算法流程**：在每个时间步 $t$，输入多视图RGB图像、语言指令、本体状态和左右触觉信号 → 分别编码为视觉/文本/触觉标记 → 融合后经LLM和动作专家预测动作块 → 训练阶段额外通过预测器从当前触觉和动作序列预测未来视觉特征变化，计算加权余弦损失；推理阶段移除预测分支。

## 三、实验设计
- **使用的数据集/场景**：作者自建 **TacAura** 数据集，包含四个接触丰富操作任务：插头插入、USB插入、印章按压、白板擦拭。每项任务收集100条高质量人类演示（共400条轨迹），同步记录多视角RGB-D图像、左右指视觉触觉图像、机器人本体状态和动作，并以10Hz均匀重采样，附有自然语言指令。
- **Benchmark**：在真实机器人（Franka Research 3臂+Franka Hand末端，配备DM-Tac WS触觉传感器）上评估，每个模型在每项任务上运行20次随机试验，使用分阶段成功率（抓取/对齐/插入或拾取/接触/完成）作为评价指标。
- **对比方法**：
  - 通用VLA模型：π0、π0.5
  - 针对接触丰富操作的方法：ForceVLA†、ForceFlow†、T-Rex†（后三者原本使用力信号，此处适配使用τ的触觉编码器提取特征以公平比较）。共5个基线方法，加上τ的三个变体（使用腕部视角、前视角、双视角作为自监督信号）。

## 四、资源与算力
- **训练资源**：使用2块NVIDIA A800 80GB GPU，训练30k步（每2k步保存检查点），batch size为32，动作窗口长度为32，余弦学习率调度（10k步预热，峰值学习率5e-5）。
- **推理资源**：单块NVIDIA RTX 4090 GPU（实时运行）。
- **数据资源**：400条高质量演示轨迹（自采），未使用大规模预训练数据集。

## 五、实验数量与充分性
- **实验数量**：共进行以下实验：
  - **主实验**：4项任务 × 6种模型（π0, π0.5, ForceVLA†, ForceFlow†, τ的三个变体） × 20次试验 = 480次试验。报告每个阶段的成功率。
  - **消融实验**：在完整τ（腕部监督变体）上移除三个组件（动作序列条件、自监督学习、触觉模块），共4种设置 × 4任务 × 20次 = 320次试验。
  - **泛化实验**：仅在USB插入和白板擦拭两项任务上进行零样本测试，包括物体泛化（2个未见物体）和场景泛化（2个干扰物集），每种设置20次试验，共计若干次（但未明确列举总数）。
- **充分性与公平性**：
  - 充分性：实验覆盖了多个任务、多种对比方法、三种消融、两种泛化类型，且使用分阶段成功率提供了细粒度分析，较为全面。
  - 公平性：对使用力信号的基线方法（ForceVLA、ForceFlow）进行了适配，使其使用与τ相同的触觉编码器输出，保证了输入模态等价；训练数据完全相同。但注意τ的变体使用了不同的监督视觉来源，这属于框架设计比较，并非严格公平（因为监督信号本身就是方法的一部分）。此外，对比方法中未包括最新的世界模型类方法（作者明确排除，理由是计算开销大），但这一排除可能影响与同样高效方法的比较。

## 六、论文的主要结论与发现
- **主实验结论**：
  - τ的三个变体在所有任务上平均完整成功率均显著优于基线（最高τ-腕部监督71.25% vs 最强基线ForceVLA† 30.00%），提升幅度达41.25个百分点。
  - 基线模型在最终完成阶段失败严重（如插头插入中对齐率85%但插入率为0），而τ能保持较高完成率（插头插入65%，USB插入50%，印章按压90%，白板擦拭95%）。
  - 监督视角的选择具有任务依赖性：前视角监督更适合对齐插入任务，腕部监督更适合局部接触任务；双视角监督并未带来提升，表明需要更有效的融合机制。
- **消融实验结论**：
  - 触觉编码与适配模块是最关键的组件（移除后平均成功率下降42.50个百分点），其次是自监督学习（下降20.00个百分点），再次是动作序列条件（下降12.50个百分点）。但组件间可能有交互，不能简单加和。
- **泛化实验结论**：
  - 白板擦拭任务展现出极强的零样本泛化（未见物体和干扰物下保持90-95%成功率），而USB插入任务泛化能力较弱（物体泛化平均30%，场景泛化平均22.5%），主要由于对几何和视觉干扰更敏感。
- **整体发现**：通过未来视觉监督学习动作条件化的触觉表示，可以在有限数据下显著提升接触丰富操作的成功率和鲁棒性，且不增加推理开销。

## 七、优点
1. **方法创新性**：首次将JEPA式跨模态预测引入触觉增强的VLA策略学习，以未来视觉特征变化作为监督信号，使触觉表示捕获交互动态，而非仅静态接触。
2. **训练-推理解耦**：自监督分支仅在训练时使用，推理时完全移除，不增加任何部署成本，实用性强。
3. **数据高效**：每项任务仅使用100条演示，却显著超越基线（基线需大规模预训练），证明了在有限数据下的有效性。
4. **实验设计严谨**：分阶段成功率提供了细粒度分析；消融实验和泛化实验全面；对基线方法的适配（触觉特征统一）增强了公平性。
5. **开放贡献**：公开TacAura数据集和基础设施，促进社区研究。

## 八、不足与局限
1. **实验覆盖范围有限**：仅包含4个任务，且均为桌面级接触丰富操作，缺乏对长时域、多阶段或动态环境（如移动操作）的测试。泛化实验仅涉及两个任务。
2. **对比方法不够全面**：未与同样是高效微调的世界模型类方法（如Dream-Tac等）比较，即使作者明确排除，但仍有缺失。此外，未与基于轻量策略（如ACT+触觉）比较，无法体现VLA框架的额外优势。
3. **数据偏差风险**：所有数据来自单一机器人平台、单一触觉传感器（DM-Tac WS），且由单一人群演示，可能存在硬件和示教风格偏差，影响泛化结论的普适性。
4. **双视角监督性能不佳**：τ的双视角变体反而不如单视角，作者仅推测需要更好的融合机制，但未深入分析原因或提出解决方案。
5. **未报告统计显著性**：虽然报告了20次试验的成功率，但未给出置信区间或统计检验（如p值），无法评估结果差异的可靠性。
6. **计算资源未充分说明**：训练时长（30k步，但未提及实际时间小时数）和推理速度未明确，影响可复现性评估。此外，未比较方法间的训练开销差异（τ需额外训练预测器，但成本可能很小）。
7. **局限性：任务特异性**：框架依赖VLA主干π0.5，而π0.5本身需要大规模预训练。若τ应用于无预训练VLA骨干的场景，性能可能受影响。同时，触觉传感器DM-Tac WS的获取门槛可能限制方法推广。

（完）
