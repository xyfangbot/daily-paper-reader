---
title: Bridging Reinforcement Learning and Optimal Control via Feasible Action Mapping
title_zh: 通过可行动作映射桥接强化学习与最优控制
authors: "Stefan Richter, Alberto Giammarino, Guillem Torrente, Sam Blakeman, Peter Dürr"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23930v1"
tags: ["query:rl-robotics"]
score: 9.0
evidence: 强化学习与最优控制结合用于约束系统
tldr: 操作受约束动力系统需兼顾任务效率与安全性。本文提出FAOC框架，通过优化映射算法将RL抽象动作转化为OCP可行参数，严格满足系统约束。在机器人乒乓球实时运动规划中，FAOC在样本效率和闭环性能上超越现有基线。该工作融合RL灵活性与OC安全可预测性，且无需专家设计动作空间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23930v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 2255, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2250, \"height\": 546, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1821, \"height\": 1226, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 2245, \"height\": 703, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1582, \"height\": 2520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 2219, \"height\": 855, \"label\": \"Figure\"}]"
motivation: 现有方法难以同时保证安全约束和任务灵活性，RL动作空间设计依赖经验且OC可行性难保障。
method: 提出FAOC框架，核心是计算高效的优化映射算法，将RL抽象动作转化为状态依赖的OCP可行参数集。
result: 在机器人乒乓球模拟实验中，FAOC在样本效率和闭环性能上均优于最先进基线。
conclusion: FAOC有效结合RL与OC优势，实现严格约束满足与高效探索，为复杂约束控制提供新范式。
---

## 摘要
操作受约束的动力系统需要控制器在强制执行递归可行性和安全约束的同时，高效解决复杂任务。为应对这些相互竞争的需求，我们提出了可行动作最优控制（FAOC），一种集成强化学习（RL）和最优控制（OC）的新型控制框架。核心贡献在于一种计算高效的、基于优化的映射算法，该算法将RL智能体的动作从静态抽象集合转换为最优控制问题（OCP）的状态依赖可行参数集，从而严格保证动力系统约束的满足。因此，FAOC有效结合了OC的可预测安全性与RL的灵活性。与先前工作不同，RL智能体的抽象动作空间无需专家或启发式设计，并且OCP的表述不会因RL无法保证可行性而受损。我们将该方法应用于机器人乒乓球实时运动规划，该场景 encapsulates 这些挑战。通过仿真实验，我们证明FAOC在样本效率和闭环性能方面均优于最先进的基线方法。

## Abstract
Operating constrained dynamical systems requires controllers to efficiently solve complex tasks while enforcing recursive feasibility and safety constraints. To address these competing requirements, we present Feasible Action for Optimal Control (FAOC), a novel control framework integrating Reinforcement Learning (RL) and Optimal Control (OC). The key contribution is a computationally efficient, optimization-based mapping algorithm that transforms the RL agent's action from a static abstract set into a state-dependent feasible parameter set of the Optimal Control Problem (OCP), guaranteeing strict satisfaction of the dynamical system's constraints. Thus, FAOC effectively combines the predictable safety of OC with the flexibility of RL. In contrast to prior work, the abstract action space of the RL agent does not require expert or heuristic design, and the OCP formulation is not compromised by the inability of RL to guarantee feasibility. We apply our approach to real-time motion planning for robot table tennis, which encapsulates these challenges. Via simulated experiments, we show that FAOC outperforms state-of-the-art baselines in both sample efficiency and closed-loop performance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- 受约束动力系统的控制需要在高效解决复杂任务的同时保证递归可行性和安全性，现有方法难以兼顾两者。
- 纯强化学习（RL）控制器缺乏严格的约束满足保证，限制其在安全关键场景的应用。
- 最优控制（OC）/模型预测控制（MPC）虽能显式处理约束，但面对非凸、长时域、稀疏奖励任务时计算复杂且效果有限。
- 已有RL-OC结合方法存在动作空间需专家/启发式设计、且RL动作可能导致OCP不可行的问题，学习效率与最终性能受损。
- 本文提出Feasible Action for Optimal Control（FAOC）框架，通过计算高效的优化映射算法将RL抽象动作鲁棒地转化为OCP的状态依赖可行参数集，融合OC的可预测安全性与RL的灵活性。

## 二、论文提出的方法论
- **核心思想**：构建一个可逆映射`M`，使RL智能体在静态、形状简单的抽象动作集`\bar{A}`（如超立方体`[-1,1]^n`）上采样，映射得到的状态依赖参数`p ∈ P(x)`（OCP的可行参数集）严格满足系统约束，同时确保所有可行参数均可达。
- **关键技术细节**：
  1. **拓扑条件**：证明当OCP满足一定凸性、连续性与无衰退方向条件时，状态依赖参数集`P(x)`是紧致、实心、凸的（Lemma 1），并以线性MPC为例（Corollary 1）。
  2. **通用映射算法（Algorithm 1）**：基于径向缩放，从基集内点`xc`出发，对方向`d`计算边界缩放因子`α`和`β`，通过连续可逆、正齐次的方向变换`ϕ`将方向映射至目标集，保证映射双射（Proposition 1）。
  3. **缓解几何畸变**：
     - **二维面积匹配变换**：定义归一化面积函数CDF，通过`υ = a_Y^{-1}(a_X(ξ))`进行角度匹配（Proposition 2, 3），完全消除点累积。
     - **高维线性变换**：基于仿射代理椭球集（例如最大体积内切椭球），通过`Φ = Q_{eY} Q_{eX}^{-1}`构造线性变换`ϕ(d)=Φd`近似几何畸变（Proposition 4），可扩展至高维。
  4. **隐式定义集合的处理**：当`P(x)`仅通过OCP约束隐式定义（如`Y=EW+e`）时，提出三种在线计算策略：
     - **内点计算**：通过Chebyshev中心或凸优化求解最大内接球（Proposition 6, 7），无需显式几何表示。
     - **线性变换矩阵计算**：从高阶空间代理椭球经SVD投影得到状态无关的`Q_{eY}`（Proposition 8），可离线计算。
     - **边界缩放因子计算**：直接通过凸优化（8）求解，无需投影到显式边界。
- 算法流程（图1）：RL输出高斯参数 → 采样/取均值 → 非线性映射得抽象动作`\bar{a}` → 可逆映射得可行参数`p` → 求解OCP得到控制轨迹 → 开环执行并更新状态。

## 三、实验设计
- **应用场景**：8自由度移动机械臂机器人乒乓球（任务包括落点控制、上旋/下旋、扣杀）。
- **数据集**：训练时使用合成轨迹与真实人类发球混合样本，每个episode返回一个来球。
- **Benchmark与对比方法**：
  - 1Dpos, 1Dvel, 1Dacc（基于Kiemel & Kröger方法，仅指定一维目标）。
  - 2Dsoft：二维位置-速度目标，但使用软终端代价代替硬约束，动作空间为静态全运动学边界。
  - FAOC（本文方法，使用线性变换）。
- **评价指标**：返回率（Fraction Balls Returned）、生成上旋（Generated Topspin）、生成速度（Generated Velocity）、生成下旋（Generated Backspin）、目标距离（Goal Distance）。仅在返回率≥50%的评估中计算其他指标以减少估计方差。
- **实验设置**：
  1. **基线实验**：使用SAC算法与BRONet网络架构。
  2. **算法与架构效应**：使用SimBaV2（分布性批评者、更大网络，容量增至1.71×和8.07×）。
  3. **低RL频率效应**：将决策频率从31.25 Hz降至7.8125 Hz（轨迹段长128 ms）。
- 每实验对每种方法训练5个不同随机种子的智能体，每个种子进行1000次评估。

## 四、资源与算力
- 文中未明确说明使用的GPU型号、数量及总训练时长。仅提及使用了随机种子并进行了多次运行，但未提供具体硬件配置或实际能耗信息。

## 五、实验数量与充分性
- 共三个主要实验场景（基线、算法架构、低频率），每个场景对比五种方法（FAOC, 2Dsoft, 1Dpos, 1Dvel, 1Dacc），每种方法5个种子，共计至少5×5×3 = 75个独立训练运行，每个运行1000次评估。
- 统计方法：使用Mann-Whitney U检验进行两两比较，显著性水平p<0.05标记为`*`。
- **充分性评价**：实验设计较为全面，覆盖了不同RL算法、架构和决策频率，统计检验增加了结果可信度。但缺少在真实物理机器人上的实验对比（仅提及部署于真实机器人并赢得比赛，但主要定量结果来自仿真）。此外，未在其他机器人任务或更一般约束控制问题上验证泛化性。

## 六、论文的主要结论与发现
- FAOC在所有实验设置中在样本效率和最终性能上均优于所有对比方法（1Dpos, 1Dvel, 1Dacc, 2Dsoft）。
- 在基线实验中，FAOC返回率、生成上旋和速度显著优于其他方法；下旋和目标距离无显著差异（所有方法表现接近）。
- 使用更强RL算法（SimBaV2）时，所有方法均有提升，但FAOC仍保持统计显著优势多数指标。
- 低RL频率实验中，FAOC性能下降最小，而其他方法退化更严重，说明FAOC通过状态依赖的可行动作空间提供了更好的轨迹段可控性。
- 2D控制器（FAOC, 2Dsoft）总体优于1D控制器；1Dacc在所有设置中表现最差。

## 七、优点
- **严格约束满足**：通过硬约束OCP保证动作始终可行，无需RL学习约束边界，降低了探索难度。
- **无需专家动作空间设计**：RL动作空间是静态简单凸集，避免手动调节动作范围超参数。
- **可逆映射**：支持将专家演示或真实轨迹反向映射到抽象动作空间，便于利用演示数据。
- **计算高效**：利用线搜索解析解（H-representation）和离线计算线性变换，适合实时控制（采用DAQP求解器）。
- **理论严谨**：提供了凸集间可逆映射的拓扑保证（Lemma 1, Proposition 1），并针对二维和高维分别给出了畸变缓解方案。
- **开源贡献**：映射算法和OCP实现已公开。

## 八、不足与局限
- **凸性假设限制**：映射算法假设`P(x)`为凸集。虽然文中指出RL可处理战略非凸性（如避障），但若OCP可行域本身非凸，映射可能失效或需近似处理。
- **二维面积匹配不通用**：精确面积匹配仅在R²有效，高维依赖线性变换近似，无法完全消除点累积。
- **实验范围有限**：仅在机器人乒乓球这一特定场景下验证，且主要结果来自仿真（simulated experiments），真实机器人实验结果定性描述为主，缺乏统计比较。
- **未充分探讨可扩展性**：未说明在高维动作空间（如n>16）下映射的计算复杂度或能否实时求解。
- **忽略状态依赖变换矩阵的计算代价**：虽然文中通过离线构造状态无关矩阵避免在线SDP，但对一般OCP隐式集合，形状矩阵的离线计算可能依赖于问题结构，缺乏通用步骤。
- **存在一定偏差风险**：实验中对2Dsoft的软约束权重`w_s`和`W_t`进行了手工调节，但未详细讨论调参过程是否公平；1D控制器仅允许指定一个自由度，可能导致先天劣势。
- **未消融映射畸变缓解**：未对比FAOC使用恒等映射与使用线性/面积匹配映射的效果，难以分离映射质量对性能的贡献。

（完）
