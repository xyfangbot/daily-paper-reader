---
title: "RGB: RL Guided Whole-Body MPPI for Humanoid Control"
title_zh: RGB：强化学习引导的全身MPPI仿人控制
authors: "Yunsoo Seo, Sol Choi, Euncheol Im, Myo Taeg Lim, Yisoo Lee"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25123v1"
arxiv_id: 2606.25123v1
arxiv_url: "http://arxiv.org/abs/2606.25123v1"
tags: ["query:热点论文筛选", "query:具身智能", "query:具身智能公司相关", "paper:arXiv:2606.25123v1", "query:unitree"]
score: 8.0
evidence: "hot-paper-scout: arXiv fallback; window=30d; company_relation_match=unitree; relation_source=abstract; query=(all:\"Agility Robotics\" OR all:\"Unitree\" OR all:\"Apptronik\" OR all:\"1X Technologies\" OR all:\"Sanctuary AI\") AND (all:\"embodied intelligence\" OR all:\"embodied AI\" OR all:\"embodied agents\" OR all:\"vision-language-action model\" OR all:\"robot foundation model\" OR all:\"vision-language-action\" OR all:\"humanoid robot\" OR all:\"robot learning\" OR all:\"physical AI\"); institution_source=arxiv-abstract"
tldr: 人形机器人需要鲁棒且精确的全身控制器。深度强化学习策略虽稳定，但其行为与训练目标耦合，难以添加新反馈目标。本文提出RL引导的全身模型预测路径积分（MPPI）框架，将预训练RL策略作为采样先验，通过模块化成本项指定任务目标，在线校正RL先验。在Unitree G1人形机器人MuJoCo仿真中实现280Hz控制，相比纯RL基线显著提升直行和全身参考跟踪精度，且无需重新训练策略。
source: arxiv
selection_source: hot_paper_scout
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25123v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 4060, \"height\": 1613}, {\"url\": \"assets/figures/arxiv/2606.25123v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 1052, \"height\": 249}]"
motivation: 深度RL策略与训练目标紧密耦合，添加新反馈目标需重新训练，缺乏灵活性。
method: 将预训练RL策略作为MPPI的采样先验，用模块化成本项定义任务目标，在线校正RL先验以满足目标。
result: 在29自由度Unitree G1仿真中实现280Hz高频控制，直行漂移得到修正，任务精度优于纯RL基线。
conclusion: 该框架无需重新训练即可灵活添加任务目标，实现了鲁棒且精确的全身控制。
---

## 摘要
仿人机器人在接触密集的环境中需要既鲁棒又精确的全身控制器。虽然深度强化学习（RL）实现了鲁棒稳定性，但其行为与训练目标和指令接口紧密耦合，使得在不重新训练的情况下难以添加新的反馈目标。在本研究中，我们提出了一种强化学习引导的全身模型预测路径积分（MPPI）框架，该框架作为预训练RL策略之上的附加反馈控制器。我们不将RL策略作为最终控制器，而是将其用作采样先验，使MPPI滚动向动态可行的行为倾斜。任务目标通过模块化的MPPI成本项来指定，并且MPPI通过连续在线修正RL先验来满足这些目标，从而无需重新训练策略即可实现闭环。在MuJoCo中对29自由度Unitree G1仿人机器人的仿真展示了稳定的高速控制（平均280 Hz）。在相同指令接口下，所提出的方法比纯RL基线提高了任务级精度。这是通过修正直行过程中的系统漂移以及跟踪通过成本施加的额外全身参考信号来实现的。

## Abstract
Humanoid robots require whole-body controllers that are both robust and precise in contact-rich environments. While deep reinforcement learning (RL) achieves robust stability, its behavior is tightly coupled to the training objective and command interface, making it difficult to add new feedback objectives without retraining. In this study, we propose an RL guided whole-body model predictive path integral (MPPI) framework that acts as an add-on feedback controller on top of a pretrained RL policy. Instead of using RL policy as the final controller, we use it as a sampling prior that biases MPPI rollouts toward dynamically feasible behaviors. Task objectives are specified through modular MPPI cost terms, and MPPI closes the loop by continuously correcting the RL prior online to satisfy these objectives without retraining the policy. Simulations on a 29-DoF Unitree G1 humanoid in MuJoCo demonstrate stable high-rate control (average 280~Hz). The proposed method improves task-level precision over a pure RL baseline under the same command interface. This is achieved by correcting systematic drift during straight walking and tracking additional whole-body reference signals imposed through the cost.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- 仿人机器人需要在接触密集环境中实现既鲁棒又精确的全身控制。
- 深度强化学习（RL）策略虽然能提供鲁棒的运动稳定性，但其行为与训练目标和指令接口紧密耦合，导致添加新的反馈目标（如摆动脚高度调节、骨盆高度控制）需要重新设计奖励并重新训练，缺乏灵活性。
- 模型预测路径积分（MPPI）等模型方法可在线优化复合任务目标并显式满足约束，但对高自由度仿人机器人而言，标准MPPI对采样分布敏感，容易产生不可行的滚转，且需要大量手工调参。
- 为此，本文提出将预训练RL策略作为MPPI的采样先验，通过模块化成本项指定额外任务目标，形成一种即插即用的附加控制器，在不重训练RL策略的前提下提升任务级精度。

## 二、论文提出的方法论
- **核心思想**：利用预训练RL策略输出作为名义运动先验，MPPI在此先验附近采样扰动，并通过物理引擎滚转评估模块化成本，在线更新控制序列；RL策略保持不变，仅作为采样分布的均值。
- **关键技术细节**  
  - **RL策略作为采样先验**：RL策略以50 Hz输出名义关节位置参考 \(\mathbf{q}_{\text{RL}}\)，MPPI以该参考为均值加入高斯扰动得到采样轨迹。MPPI滚转时使用与RL策略相同的命令向量，确保细化围绕预期行为进行。
  - **Knot基插值**：采用H个Knot点参数化控制序列（本文H=2），通过三次插值重构高分辨率控制序列，降低采样维度并保证时间平滑性。
  - **物理引擎滚转**：利用MuJoCo作为滚转预言机，对每个候选控制序列进行正向仿真（包含接触动力学），无需解析梯度，通过成本函数评估并计算重要性权重。
  - **成本函数设计**：任务目标通过模块化成本项 \(\ell\) 指定（如横向位置偏差、航向偏差、基座高度偏差），与RL策略原有命令无关，可灵活组合。
  - **控制循环**：RL策略50 Hz → MPPI异步规划（平均280 Hz） → 关节PD控制器500 Hz执行。
- **公式/算法流程**（文字描述）  
  1. 初始化名义Knot \(\bar{\mathbf{z}}\)（可为零或上一时刻结果）。  
  2. 每步读取状态 \(\mathbf{x}_t\)。  
  3. 若RL策略更新（50 Hz），则 \(\bar{\mathbf{z}} \leftarrow \pi_{\text{RL}}(\mathbf{o}_t)\)。  
  4. 若MPPI规划完成，对 \(J\) 次迭代（论文 \(J=1\)？实际算法中内层对 \(j\) 循环，但描述可能指多次更新）和 \(N\)（128）个样本：  
     - 采样扰动 \(\epsilon^{(n)} \sim \mathcal{N}(0, \nu I)\)，得到候选 \(\mathbf{z}^{(n)} = \bar{\mathbf{z}} + \epsilon^{(n)}\)。  
     - 插值得到全时序控制序列 \(\mathbf{U}^{(n)}\)，进行滚转并计算成本 \(S^{(n)}\)。  
     - 计算重要性权重 \(w^{(n)}\)，更新 \(\bar{\mathbf{z}}\)。  
  5. 插值最终 \(\bar{\mathbf{z}}\) 得到第一时刻关节目标 \(\mathbf{q}_t^{\text{des}}\)。  
  6. PD控制器跟踪 \(\mathbf{q}_t^{\text{des}}\)。

## 三、实验设计
- **平台**：MuJoCo仿真，使用29自由度Unitree G1仿人机器人模型。滚转评估通过MJPC利用CPU并行计算。
- **任务场景**  
  - **直行漂移抑制**：给定命令 \((v_x^{\text{des}}, v_y^{\text{des}}, \psi^{\text{des}}) = (1,0,0)\)，RL策略存在左右偏置导致漂移；MPPI增加成本项 \(\ell = \beta_y \|y_{\text{base}} - y_{\text{des}}\|^2 + \beta_\psi \|\psi_{\text{base}} - \psi_{\text{des}}\|^2\) 抑制漂移。  
  - **蹲起任务（基座高度调节）**：增加成本项 \(\ell = \beta_z \|z_{\text{base}} - z_{\text{des}}(t)\|^2\)，使机器人跟踪分段时间变化的基座高度参考（0.71–0.78 m），而RL原始命令接口不包含垂直分量。
- **对比方法**：  
  - **纯RL基线**：使用相同的预训练RL策略直接输出关节目标（无MPPI细化）。  
  - **RL引导的MPPI**：保持RL策略不变，MPPI添加模块化成本项。
- **指标**：横向位置误差RMSE、前向速度跟踪RMSE、横向速度跟踪RMSE、基座高度跟踪误差（定性展示）。

## 四、资源与算力
- **硬件**：工作站配备Intel Core i9-14900KF CPU（24核）、32 GB RAM、NVIDIA GeForce RTX 4070 Ti GPU（但滚转使用CPU并行，GPU未明确用于训练，仅用于显示或可能加速？文中提到“CPU-based parallel rollouts”）。
- **训练时长**：RL策略预训练使用PPO在IsaacLab中进行，但论文未报告具体训练时间或GPU数量。MPPI推理在仿真中平均280 Hz，使用128个滚转。
- **说明**：资源消耗部分信息不完整，尤其是RL预训练的成本未明确。MPPI运行时仅使用CPU，但GPU可用于加速（未来工作提及）。

## 五、实验数量与充分性
- **实验数量**：仅两个任务（直行、蹲起），每个任务与纯RL基线对比。未进行大规模消融实验（如不同采样数N、Knot数量H、成本权重的影响）、不同命令速度下的泛化测试、噪声鲁棒性测试。
- **充分性**：实验覆盖了“相同命令接口下误差修正”和“超越命令接口的任务增强”两种典型场景，验证了核心思想。但实验规模较小，仅在一个机器人模型上运行短暂时间（几秒到十几秒）。缺乏对不同训练随机种子的重复实验、统计显著性分析、真实硬件转移测试。结论的泛化能力有限。
- **客观性**：对比公平，RL基线与本文方法使用相同的预训练策略和命令。但纯RL基线本身存在已知缺陷（漂移），本文方法直接针对该缺陷修正，受益明显。

## 六、论文的主要结论与发现
- RL引导的MPPI作为附加控制器，在不重训练RL策略的前提下，通过模块化成本项显著提升了任务级精度。
- 在直行任务中，横向位置RMSE从0.339 m（纯RL）降至0.022 m；横向速度跟踪RMSE从0.046 m/s降至0.021 m/s；前向速度跟踪精度保持相当。
- 在蹲起任务中，成功跟踪了时变的基座高度参考，而纯RL无法响应垂直指令，证明了超越原始指令接口的任务增强能力。
- 实现了平均280 Hz的实时控制频率（CPU并行），具有实际部署潜力。

## 七、优点
- **灵活性高**：模块化成本设计，可即插即用添加新任务目标，无需重训练RL策略。
- **保持鲁棒性**：利用RL先验保证动态可行性，MPPI在此基础上进行局部优化，避免了纯MPPI的采样效率问题。
- **无梯度优化**：物理引擎滚转无需解析导数，兼容非光滑接触动力学。
- **实时性**：Knot插值降低维度，异步架构实现280 Hz更新率。
- **算法通用性**：对RL算法、训练过程、动作接口无特定要求；可适配不同策略。

## 八、不足与局限
- **行为变化有限**：MPPI细化受限于RL先验附近，无法支持大幅偏离先验的行为。如需显著改变步态或全身动作，可能仍需重训或引入多先验机制。
- **实验规模小**：仅在单一机器人（G1）上的仿真中验证两个简单任务，未评估复杂地形、动态干扰、变速度命令等场景。缺乏消融实验和统计显著性分析。
- **硬件未部署**：未在真实机器人上验证，忽略建模误差、接触估计不确定性、计算延迟等实际问题。
- **计算开销**：虽达280 Hz，但使用128个CPU并行滚转，更高采样数或复杂成本可能降低频率。GPU加速可缓解，但未在本工作中实现。
- **成本权重敏感性**：模块化成本权重（\(\beta_y, \beta_\psi, \beta_z\)）需要手动调节，未提供自动化调优方法。
- **策略先验局限**：若RL策略本身不稳定或存在显著偏差，MPPI可能无法完全纠正，且可能受到RL策略更新频率（50 Hz）的限制。
- **无理论保证**：缺乏对收敛性、最优性或稳定性的理论分析。

（完）
