---
title: "MARCH: Model-Assisted Reinforcement Learning for the Perceptive Control of Humanoids over Sparse Footholds"
title_zh: MARCH：面向稀疏立足点的人形机器人感知控制的模型辅助强化学习
authors: "Codrin Crismariu, Ryan K. Cosner"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10288v1"
arxiv_id: 2606.10288v1
arxiv_url: "http://arxiv.org/abs/2606.10288v1"
tags: ["query:热点论文筛选", "query:具身智能", "query:具身智能公司相关", "paper:arXiv:2606.10288v1", "query:unitree"]
score: 8.0
evidence: "hot-paper-scout: arXiv fallback; window=30d; company_relation_match=unitree; relation_source=abstract; query=(all:\"Agility Robotics\" OR all:\"Unitree\" OR all:\"Apptronik\" OR all:\"1X Technologies\" OR all:\"Sanctuary AI\") AND (all:\"embodied intelligence\" OR all:\"embodied AI\" OR all:\"embodied agents\" OR all:\"vision-language-action model\" OR all:\"robot foundation model\" OR all:\"vision-language-action\" OR all:\"humanoid robot\" OR all:\"robot learning\" OR all:\"physical AI\"); institution_source=arxiv-abstract"
tldr: 稀疏地形上的双足感知行走面临模型方法精准但脆弱、无模型方法鲁棒但难以发现精确约束运动的困境。本文提出模型辅助强化学习框架，先由简化模型生成安全参考轨迹，再利用控制李雅普诺夫函数(CLF)奖励训练特权教师策略，最后蒸馏为视觉学生策略。该方法提升了样本效率，减少复杂课程需求，实现平滑运动且步进性能与无模型基线相当。在Unitree G1人形机器人上成功验证了稀疏落脚点感知导航。
source: arxiv
selection_source: hot_paper_scout
motivation: 解决稀疏地形双足行走中模型方法易受不确定性影响、无模型方法难以学习精确约束运动的问题。
method: 提出模型辅助RL框架：简化模型生成参考轨迹，CLF奖励训练教师策略，蒸馏为视觉学生策略。
result: 在仿真与Unitree G1机器人上验证了样本效率提升、课程需求减少、运动平滑，步进性能与无模型基线持平。
conclusion: 模型辅助RL融合模型与无模型优势，为人形机器人稀疏落脚点感知行走提供了有效且鲁棒的策略。
---

## 摘要
稀疏地形上的有感知双足运动仍然是一个难题：基于模型的方法精确但对不确定性脆弱，而无模型方法鲁棒但难以发现安全关键运动所需的精确约束动作——在这些运动中，微小误差可能导致灾难性失败。我们提出了一个模型辅助强化学习框架，通过三个步骤结合两种视角：（1）使用简化模型生成安全参考轨迹；（2）训练一个由基于安全参考轨迹的控制李雅普诺夫函数奖励引导的特权教师策略；（3）将教师策略蒸馏为基于视觉的学生策略。我们表明，这种模型辅助过程产生了物理基础的运动，提高了样本效率，减少了对复杂学习课程的需求，并在踏步石性能上与无模型基线相当的同时实现了更平滑的运动行为。我们在仿真中验证了该方法，并在Unitree G1人形机器人上成功部署，该机器人在具有横向约束的稀疏立足点上导航。

## Abstract
Perceptive bipedal locomotion over sparse terrain remains a difficult challenge: model-based methods are precise but brittle to uncertainty, while model-free methods are robust but struggle to discover the precise, constrained motions required for safety-critical locomotion where small errors can cause catastrophic failures. We propose a model-assisted reinforcement learning (RL) framework that combines both perspectives in three steps: (1) generate a safe reference trajectory using simplified models; (2) train a privileged teacher policy guided by a control Lyapunov function (CLF) reward built around the safe reference trajectory; and (3) distill the teacher into a vision-based student policy. We show that this model-assistance procedure produces physically grounded locomotion, improving sample efficiency, reducing the need for a complex learning curriculum, and achieving smoother locomotion behavior alongside stepping stone performance comparable to model-free baselines. We validate our approach in simulation and demonstrate successful deployment on a Unitree G1 humanoid robot navigating sparse footholds with lateral constraints.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- 稀疏地形上的双足有感知行走是机器人领域的难题：基于模型的方法（如MPC、CBF）能提供精确的足部放置和安全性保证，但依赖于精确的动力学模型和环境地图，对不确定性和模型误差脆弱，难以泛化到真实部署；基于无模型的强化学习方法鲁棒性强，但难以发现安全关键运动所需的精确、受约束的动作（如窄踏脚石上的横向约束），微小误差可能导致灾难性失败。
- 现有工作：CLF-RL[7]已展示用控制李雅普诺夫函数引导强化学习可提升样本效率，但仅用于前向约束；[6]实现了基于视觉的稀疏踏脚石行走，但需要大量计算和结构化课程。本文旨在将模型辅助RL扩展到同时包含前向和横向约束的稀疏立足点场景，提升样本效率、减少课程依赖，并实现更平滑的运动。

## 二、论文提出的方法论
- **核心思想**：结合模型方法和无模型方法的优势，通过三阶段流程实现有感知的稀疏立足点行走：
  1. **模型参考轨迹生成**：利用简化动力学模型（离散时间单积分器用于足部规划、混合线性倒立摆（HLIP）用于质心运动）生成安全参考轨迹，包括足部位置/速度、CoM位置/速度、骨盆方向、摆动足方向、上半身关节位置等。
  2. **特权教师策略训练**：在仿真中，教师策略除了拥有标准观测（5步本体感觉历史）外，还拥有特权信息（参考轨迹）。训练采用PPO算法，奖励函数中加入基于控制李雅普诺夫函数（CLF）的塑造奖励：$r_{clf} = \exp(-\min(V(\eta), V_{max})/V_{max})$ 和 $r_{dclf} = -\max(\dot{V}(\eta,a)+\alpha V(\eta),0)/V_{d max}$，激励状态误差 $\eta$ 收敛到参考轨迹。
  3. **学生策略蒸馏**：将教师策略蒸馏为仅有本体感觉和体载深度相机图像的学生策略。学生网络结构：CNN处理深度图像 → Transformer融合CNN嵌入和本体感觉历史 → 混合密度网络（MDN）输出双峰动作分布，允许策略在多个安全步态模式间选择。
- **关键技术细节**：
  - 参考足部规划求解有限时域优化问题（7），通过随机射弹（16次贪婪展开，N=4步）快速近似解决高度非凸的约束问题。
  - CLF函数 $V(\eta)=\frac{1}{2}\eta^T P\eta$，P从连续时间代数Riccati方程求解，假设误差具有二阶反馈线性化动力学。
  - 教师策略使用MLP；学生策略使用CNN+Transformer+MDN（2个模式）。
- **算法流程**：生成参考轨迹 → 教师PPO训练（含CLF奖励） → 蒸馏学生（最小化负对数似然损失）。

## 三、实验设计
- **仿真环境**：基于mjlab框架和MuJoCo物理引擎，使用Unitree G1人形机器人模型。地形为由起始平台、结束平台和一系列踏脚石组成的程序化生成地形，石头尺寸、间隙、高度变化，并包含随机左右并排石对以造成左/右足选择决策。
- **基准对比**：
  - 消融实验：对比完整学生策略（CNN+Transformer+MDN）与两种变体——Transformer替换为MLP，MDN替换为确定性MLP（使用Huber损失）。使用固定教师策略训练学生，以教师策略的完整奖励评估学生滚动输出。
  - 基线比较：对比模型知情方法（本文）与标准无模型PPO基线（类似[6]），后者使用相同的教师-学生蒸馏框架但无CLF奖励。评估指标包括平均行走距离、平均绝对关节扭矩、平均绝对角加速度（jerk）。
- **硬件实验**：Unitree G1人形机器人，搭载Jetson Orin Nx机载计算机和Intel Realsense D435i深度相机。实验设置包括起始平台、结束平台和4个踏脚石（伪随机左右交替排列），所有石头高度相同，表面覆以刚性木板。学生策略以约50Hz运行完整视觉-本体感觉流水线。

## 四、资源与算力
- 训练使用单张NVIDIA RTX 5090 GPU。
- 并行环境数量：4,096个并行仿真环境。
- 总体收敛时间：约24小时（包括教师策略训练和学生蒸馏）。
- 学习框架：基于mjlab和GPU加速的rsl-rl库。

## 五、实验数量与充分性
- **仿真实验**：
  - 消融实验：每组条件（完整方法、无Transformer、无MDN）各进行1000个episode的学生训练，并绘制奖励曲线（图3右）。所示结果基于固定教师策略。
  - 基线比较：教师策略训练（模型知情10,000 episodes vs 无模型20,000 episodes），学生蒸馏迭代1500次，每迭代评估2048次滚动输出，绘制20-episode移动平均的行走距离、扭矩和角加速度（图4）。数据具有代表性，误差条（4倍标准误）显示统计稳定性。
- **硬件实验**：在Unitree G1上进行了物理实验，展示了穿越4个踏脚石的成功案例，提供视频链接（tinyurl.com/stepping-stones-corl26）。未报告多次试验的统计成功率。
- **充分性判断**：仿真实验数量充分（2048滚动评估、消融和多指标对比），消融实验合理。硬件实验仅展示单次成功，缺乏重复性和统计评估，是主要局限。整体上实验设计覆盖了方法论验证的核心维度，但硬件部分充分性不足。

## 六、论文的主要结论与发现
- 模型辅助RL框架（模型知情）在样本效率上优于无模型基线：模型知情教师仅需10,000 episodes训练，而无模型需20,000 episodes，两者最终平均行走距离相当。
- 模型知情方法产生更平滑的运动：蒸馏后，模型知情学生策略的平均绝对关节扭矩至少降低12%，平均绝对角加速度至少降低39%（500次蒸馏迭代后）。
- 消融实验表明，学生策略中的Transformer和MDN均显著提升性能（奖励更高）。
- 模型知情方法减少了对复杂课程的需求（无模型方法需要从平地上开始逐渐缩放）。
- 硬件实验验证了该框架在真实Unitree G1上成功穿越具有横向约束的稀疏踏脚石。

## 七、优点
- **方法学亮点**：
  - 巧妙融合模型精确性和强化学习鲁棒性：利用简化模型生成参考轨迹，通过CLF奖励引导RL，避免了纯模型方法的脆弱性和纯无模型方法的样本低效。
  - 提出三阶段流水线（参考生成→教师→学生），使最终策略仅依赖视觉和本体感觉，适合实际部署。
  - 使用MDN输出多峰动作分布，使策略能够在多个候选步态（如左/右足选择）间灵活切换，提升了决策能力。
- **实验设计亮点**：
  - 仿真消融实验系统地验证了关键架构组件（Transformer、MDN）的必要性。
  - 与无模型基线的对比不仅关注最终性能，还比较了样本效率、扭矩和光滑度等质量指标，更全面地展示了优势。
  - 在Unitree G1上进行了实物演示，验证了从仿真到真实世界的迁移能力。

## 八、不足与局限
- **方法局限**：
  - 相机固定朝前下方，无法支持向后或侧向行走，且视线角度限制了长视野规划（超过4步）。
  - HLIP模型不包含飞行相，因此参考轨迹无法涵盖跳跃等动态动作。
  - 虽然样本效率提高，但总体训练时间（约24小时）并未显著缩短，未来需提高每样本计算效率。
- **实验局限**：
  - 硬件实验未提供重复试验的统计成功率，仅展示了单次成功，鲁棒性评估不足。作者承认物理部署的重复性不如仿真，归因于相机延迟和精度。
  - 仿真域随机化参数虽然详细，但未评估对未见环境（如不同光照、表面材质）的泛化能力。
  - 与[6]的对比非直接（因[6]代码未公开），仅是基于其描述的最佳近似，可能存在差异。
- **偏差风险**：所有实验在同一仿真环境和同一机器人平台（Unitree G1）上进行，缺乏跨平台或跨场景验证，可能存在过拟合。

（完）
