---
title: "RGB: RL Guided Whole-Body MPPI for Humanoid Control"
title_zh: RGB：面向人形机器人控制的强化学习引导全身MPPI
authors: "Yunsoo Seo, Sol Choi, Euncheol Im, Myo Taeg Lim, Yisoo Lee"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25123v1"
arxiv_id: 2606.25123v1
arxiv_url: "http://arxiv.org/abs/2606.25123v1"
tags: ["query:热点论文筛选", "query:具身智能", "query:具身智能公司相关", "paper:arXiv:2606.25123v1", "query:unitree"]
score: 8.0
evidence: "hot-paper-scout: arXiv fallback; window=30d; company_relation_match=unitree; relation_source=abstract; query=(all:\"Agility Robotics\" OR all:\"Unitree\" OR all:\"Apptronik\" OR all:\"1X Technologies\" OR all:\"Sanctuary AI\") AND (all:\"embodied intelligence\" OR all:\"embodied AI\" OR all:\"embodied agents\" OR all:\"vision-language-action model\" OR all:\"robot foundation model\" OR all:\"vision-language-action\" OR all:\"humanoid robot\" OR all:\"robot learning\" OR all:\"physical AI\"); institution_source=arxiv-abstract"
tldr: 人形机器人全身控制需兼顾鲁棒性与精确性。针对深度强化学习策略难以灵活添加新反馈目标的问题，提出RL引导的MPPI框架：以预训练RL策略作为采样先验，通过MPPI在线校正满足模块化任务目标，无需重新训练。在29自由度Unitree G1仿真中实现280Hz稳定控制，相比纯RL基线提升任务精度，能校正直走漂移并跟踪全身参考信号。
source: arxiv
selection_source: hot_paper_scout
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25123v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 4060, \"height\": 1613}, {\"url\": \"assets/figures/arxiv/2606.25123v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 1052, \"height\": 249}]"
motivation: 深度强化学习策略与训练目标紧耦合，难以在不重训情况下增加新反馈目标，限制灵活性。
method: 将预训练RL策略作为MPPI采样的先验，通过MPPI成本项指定任务目标，在线校正策略输出以达成目标。
result: 在MuJoCo中Unitree G1人形实现280Hz高频控制，任务精度优于纯RL基线，成功校正直走漂移并跟踪全身参考。
conclusion: RL引导的MPPI框架无需重训即可灵活添加任务目标，为接触丰富环境中的人形机器人提供精确鲁棒的全身控制方案。
---

## 摘要
人形机器人在接触密集环境中需要既鲁棒又精确的全身控制器。虽然深度强化学习（RL）能够实现鲁棒稳定性，但其行为与训练目标和指令接口紧密耦合，导致在不重新训练的情况下难以添加新的反馈目标。本研究提出了一种强化学习引导的全身模型预测路径积分（MPPI）框架，该框架作为预训练强化学习策略之上的附加反馈控制器。我们并非将强化学习策略作为最终控制器，而是将其用作采样先验，使MPPI滚动预测偏向于动态可行的行为。任务目标通过模块化的MPPI成本项指定，MPPI通过在线持续修正强化学习先验来满足这些目标，而无需重新训练策略。在MuJoCo中对29自由度Unitree G1人形机器人进行的仿真展示了稳定的高速率控制（平均280 Hz）。在相同指令接口下，所提方法相比纯强化学习基线提高了任务级精度。这是通过修正直行过程中的系统性漂移以及跟踪通过成本施加的额外全身参考信号来实现的。

## Abstract
Humanoid robots require whole-body controllers that are both robust and precise in contact-rich environments. While deep reinforcement learning (RL) achieves robust stability, its behavior is tightly coupled to the training objective and command interface, making it difficult to add new feedback objectives without retraining. In this study, we propose an RL guided whole-body model predictive path integral (MPPI) framework that acts as an add-on feedback controller on top of a pretrained RL policy. Instead of using RL policy as the final controller, we use it as a sampling prior that biases MPPI rollouts toward dynamically feasible behaviors. Task objectives are specified through modular MPPI cost terms, and MPPI closes the loop by continuously correcting the RL prior online to satisfy these objectives without retraining the policy. Simulations on a 29-DoF Unitree G1 humanoid in MuJoCo demonstrate stable high-rate control (average 280~Hz). The proposed method improves task-level precision over a pure RL baseline under the same command interface. This is achieved by correcting systematic drift during straight walking and tracking additional whole-body reference signals imposed through the cost.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、论文的核心问题与整体含义（研究动机和背景）
- 人形机器人需要既鲁棒又精确的全身控制器，尤其是在接触密集环境中。
- 深度强化学习（RL）策略虽然能通过域随机化等训练实现鲁棒稳定性，但其行为与训练目标和指令接口紧密耦合，使得在不重新训练策略的情况下难以添加新的反馈目标（如摆动脚离地高度调节、骨盆高度控制等）。
- 模型预测控制（MPC）方法（特别是采样型MPPI）能通过在线优化组合任务目标并显式约束，但对高维人形机器人敏感于采样分布初始化，且效率低。
- 因此，本文旨在结合RL的鲁棒性与MPPI的灵活目标塑形能力，提出一种即插即用的附加控制器，使预训练RL策略保持不变，仅通过MPPI成本项添加新目标。

## 二、论文提出的方法论
- **核心思想**：将预训练RL策略作为MPPI采样的先验（即采样均值），MPPI通过物理引擎rollout评估带任务成本项的候选轨迹，并用重要性加权更新控制序列，在线校正RL先验以达成模块化任务目标。
- **关键技术细节**：
  - 整体控制栈分三层异步运行：RL策略（50Hz）输出标称关节位置q<sup>RL</sup>；MPPI（有效平均280Hz）在此基础上进行节点（knot）采样的优化；底层PD控制器（500Hz）跟踪MPPI修正后的目标位置。
  - **Knot-based插值**：将MPPI控制序列参数化为H个节点（knot），降低采样维度，并通过三次插值恢复全分辨率序列，保证时间平滑。
  - **采样生成**：在RL策略输出周围施加高斯噪声：z<sup>(n)</sup> = π<sup>RL</sup>(o<sub>t</sub>) + ε<sup>(n)</sup>，ε<sup>(n)</sup> ~ N(0, νI)。N=128个样本，H=2个knot。
  - **物理引擎rollout**：每个候选通过MuJoCo进行前向仿真（包括接触动力学），计算累积成本S<sup>(n)</sup> = φ(x<sup>(n)</sup><sub>H</sub>) + Σ ℓ(x<sup>(n)</sup><sub>h</sub>, u<sup>(n)</sup><sub>h</sub>)。
  - **权重更新**：采用标准MPPI重要性加权公式，计算归一化权重w<sup>(n)</sup>（见公式8），更新knot均值：z<sup>*</sup> = ¯z + Σ w<sup>(n)</sup> ε<sup>(n)</sup>。
  - 仅执行z<sup>*</sup>插值后的第一个控制量，然后滚动时域重复。
- **设计原则**：解耦学习与任务规范（RL仅提供可行先验，任务由成本项定义）；隐含接触处理（物理引擎自动处理不稳定的接触，低成本自然淘汰）。

## 三、实验设计
- **仿真平台**：MuJoCo物理学引擎，使用MJPC（MuJoCo MPC）进行并行rollout。
- **机器人模型**：29自由度Unitree G1人形机器人。
- **RL策略**：使用PPO在IsaacLab独立训练，输出12个下肢关节目标位置。奖励包括速度跟踪、正则化、稳定性、步态等。未针对MPPI调整。
- **对比方法**：仅与纯RL策略（无MPPI）对比，保持相同指令接口（线速度+偏航率）。
- **任务场景**：
  1. **直行走纠正漂移**：指令(v<sub>x</sub><sup>des</sup>, v<sub>y</sub><sup>des</sup>, ψ<sup>des</sup>) = (1, 0, 0)。RL策略存在固有左右偏，MPPI添加成本项ℓ=β<sub>y</sub>∥y<sub>base</sub>-y<sub>des</sub>∥²+β<sub>ψ</sub>∥ψ<sub>base</sub>-ψ<sub>des</sub>∥²，权重β=25。
  2. **深蹲任务（基座高度调节）**：指令为纯平面速度跟踪，但MPPI添加成本ℓ=β<sub>z</sub>∥z<sub>base</sub>-z<sub>des</sub>(t)∥²，β<sub>z</sub>=100，目标z<sub>des</sub>在0.71-0.78m间变化。RL策略无法直接控制高度。
- **评估指标**：横向位置误差RMSE、前向速度RMSE、横向速度RMSE、基座高度跟踪效果。

## 四、资源与算力
- **计算平台**：Intel Core i9-14900KF CPU，32 GB RAM，NVIDIA GeForce RTX 4070 Ti GPU（但文中只说CPU并行rollout，未明确使用GPU加速MPPI）。
- **训练资源**：RL策略在IsaacLab中单独训练，但未说明训练时长、使用GPU型号或数量。
- **推理效率**：MPPI有效平均更新率约280Hz（CPU并行128个rollout），RL策略50Hz，PD控制器500Hz。
- **注**：文中未提供任何训练时长数据，也未提及MPPI训练阶段（仅说使用预训练策略）。

## 五、实验数量与充分性
- **实验组数**：仅两个任务（直走漂移纠正和深蹲），每个任务进行了单次较长时间的轨迹对比（11~12秒），并报告了RMSE指标。
- **充分性评估**：
  - 未进行消融实验（如不同采样数、不同knot数、不同成本权重的敏感性分析）。
  - 未与任何其他model-based方法（如纯MPC）或混合方法对比。
  - 仅在仿真中验证，未在真实硬件上部署。
  - 每个任务仅展示一条代表性轨迹（图2和图3），未做多次随机种子或不同初始条件下的统计分析。
  - 总体而言，实验数量有限，验证不够全面，但针对提出的核心论点是初步有效的。

## 六、论文的主要结论与发现
- RL引导的MPPI框架能有效抑制预训练RL策略的直走漂移：横向位置RMSE从0.339m降至0.022m，横向速度RMSE从0.046m/s降至0.021m/s，同时保持前向速度跟踪精度（RMSE 0.773 vs 0.806）。
- 能够通过成本增广实现RL策略指令接口之外的任务（如基座高度跟踪），无需任何重新训练或策略修改。
- 实现实时速率（平均280Hz），验证了knot采样和CPU并行rollout的实际可行性。
- 核心贡献：将学习与任务规范解耦，形成即插即用的附加控制器。

## 七、优点
- **模块化与可扩展性**：任务目标通过MPPI成本项灵活组合，无需修改或重训RL策略，真正实现“即插即用”。
- **鲁棒性保持**：RL策略提供动态可行的采样先验，MPPI在线校正不会破坏原有的鲁棒行走能力。
- **隐式接触处理**：利用物理引擎rollout，避免手动设计接触约束，能自然处理非光滑动力学。
- **计算效率**：knot参数化降低采样维度，异步架构使MPPI能以较高频率（280Hz）运行，接近实时。
- **算法通用性**：对RL算法和策略架构无特定要求，可适配不同训练方法和输出接口。

## 八、不足与局限
- **行为依赖先验**：MPPI始终围绕RL先验采样，若目标任务与先验行为差异过大，可能难以实现大幅行为变化（如从步行切换到跳跃）。文中也承认需要多先验或混合机制。
- **仿真验证局限**：仅在MuJoCo仿真中测试，未进行真实硬件实验，缺少对建模误差、接触估计误差、延迟等实际因素的鲁棒性评估。
- **计算资源要求**：虽然达到280Hz，但需要128个并行CPU rollout，对硬件资源有较高需求；文中未测试更低采样数或更高频率下的性能退化情况。
- **实验全面性不足**：只做了两个任务，缺乏消融研究、不同扰动条件、不同运动速度、复杂地形等更丰富的测试场景；也未与其他现有方法（如纯MPC、MPC+RL其他混合方式）进行定量比较。
- **成本函数设计依赖人工**：虽然模块化，但成本权重（β<sub>y</sub>=25, β<sub>z</sub>=100）需手动调节，未讨论自适应或学习成本权重的方法。
- **忽略状态估计与同步问题**：文中提到未来需建立可靠的状态估计和同步管线，暗示当前方法对状态信息准确性敏感。

（完）
